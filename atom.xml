<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>徐靖峰|个人博客</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://lexburner.github.io/"/>
  <updated>2020-12-20T10:36:26.134Z</updated>
  <id>https://lexburner.github.io/</id>
  
  <author>
    <name>徐靖峰</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>一文详解 Nacos 高可用特性</title>
    <link href="https://lexburner.github.io/nacos-high-available/"/>
    <id>https://lexburner.github.io/nacos-high-available/</id>
    <published>2020-12-18T07:38:25.000Z</published>
    <updated>2020-12-20T10:36:26.134Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>服务注册发现是一个经久不衰的话题，Dubbo 早期开源时默认的注册中心 Zookeeper 最早进入人们的视线，并且在很长一段时间里，人们将注册中心和 Zookeeper 划上了等号，可能 Zookeeper 的设计者都没有想到这款产品对微服务领域造成了如此深厚的影响，直到 SpringCloud 开始流行，其自带的 Eureka 进入了人们的视野，人们这才意识到原来注册中心还可以有其他的选择。再到后来，热衷于开源的阿里把目光也聚焦在了注册中心这个领域，Nacos 横空出世。</p><p><img src="https://kirito.iocoder.cn/image-20201220140934757.png" alt="注册中心"></p><p>Kirito 在做注册中心选型时的思考：曾经我没得选，现在我只想选择一个好的注册中心，它最好是开源的，这样开放透明，有自我的掌控力；不仅要开源，它还要有活跃的社区，以确保特性演进能够满足日益增长的业务需求，出现问题也能即使修复；最好…它的功能还要很强大，除了满足注册服务、推送服务外，还要有完善的微服务体系中所需的功能；最重要的，它还要稳定，最好有大厂的实际使用场景背书，证明这是一个经得起实战考验的产品；当然，云原生特性，安全特性也是很重要的…</p><p>似乎 Kirito 对注册中心的要求实在是太高了，但这些五花八门的注册中心呈现在用户眼前，总是免不了一番比较。正如上面所言，功能特性、成熟度、可用性、用户体验度、云原生特性、安全都是可以拉出来做比较的话题。今天这篇文章重点介绍的是 Nacos 在可用性上体现，希望借助于这篇文章，能够让你对 Nacos 有一个更加深刻的认识。</p><a id="more"></a><h2 id="高可用介绍"><a href="#高可用介绍" class="headerlink" title="高可用介绍"></a>高可用介绍</h2><p>当我们在聊高可用时，我们在聊什么？</p><ul><li>系统可用性达到 99.99%</li><li>在分布式系统中，部分节点宕机，依旧不影响系统整体运行</li><li>服务端集群化部署多个节点</li></ul><p>这些都可以认为是高可用，而我今天介绍的 Nacos 高可用，则是一些 Nacos 为了提升系统稳定性而采取的一系列手段。Nacos 的高可用不仅仅存在于服务端，同时它也存在于客户端，以及一些与可用性相关的功能特性中。这些点组装起来，共同构成了 Nacos 的高可用。</p><h2 id="客户端重试"><a href="#客户端重试" class="headerlink" title="客户端重试"></a>客户端重试</h2><p>先统一一下语义，在微服务架构中一般会有三个角色：Consumer、Provider 和 Registry，在今天注册中心的主题中，Registry 是 nacos-server，而 Consumer 和 Provider 都是 nacos-client。</p><p>在生产环境，我们往往需要搭建 Nacos 集群，在 Dubbo 也需要显式地配置上集群地址：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:registry</span> <span class="attr">protocol</span>=<span class="string">"nacos"</span> <span class="attr">address</span>=<span class="string">"192.168.0.1:8848,192.168.0.2:8848,192.168.0.3:8848"</span>/&gt;</span></span><br></pre></td></tr></table></figure><p>当其中一台机器宕机时，为了不影响整体运行，客户端会存在重试机制</p><p><img src="https://kirito.iocoder.cn/image-20201220151901675.png" alt="轮询 server"></p><p>逻辑非常简单，拿到地址列表，在请求成功之前逐个尝试，直到成功为止。</p><p>该可用性保证存在于 nacos-client 端。</p><h2 id="一致性协议-distro"><a href="#一致性协议-distro" class="headerlink" title="一致性协议 distro"></a>一致性协议 distro</h2><p>首先给各位读者打个强心剂，不用看到”一致性协议“这几个字就被劝退，本节不会探讨一致性协议的实现过程，而是重点介绍其余高可用相关的特性。有的文章介绍 Nacos 的一致性模型是 AP + CP，这么说很容易让人误解，其实 Nacos 并不是支持两种一致性模型，也并不是支持两种模型的切换，介绍一致性模型之前，需要先了解到 Nacos 中的两个概念：临时服务和持久化服务。</p><ul><li>临时服务（Ephemeral）：临时服务健康检查失败后会从列表中删除，常用于服务注册发现场景。</li><li>持久化服务（Persistent）：持久化服务健康检查失败后会被标记成不健康，常用于 DNS 场景。</li></ul><p>临时服务使用的是 Nacos 为服务注册发现场景定制化的私有协议 distro，其一致性模型是 AP；而持久化服务使用的是 raft 协议，其一致性模型是 CP。所以以后不要再说 Nacos 是 AP + CP 了，更建议加上服务节点状态或者使用场景的约束。</p><p>distro 协议与高可用有什么关系呢？上一节我们提到 nacos-server 节点宕机后，客户端会重试，但少了一个前提，即 nacos-server 少了一个节点后依旧可以正常工作。Nacos 这种有状态的应用和一般无状态的 Web 应用不同，并不是说只要存活一个节点就可以对外提供服务的，需要分 case 讨论，这与其一致性协议的设计有关。distro 协议的工作流程如下：</p><ul><li>Nacos 启动时首先从其他远程节点同步全部数据</li><li>Nacos 每个节点是平等的都可以处理写入请求，同时把新数据同步到其他节点</li><li>每个节点只负责部分数据，定时发送自己负责数据校验值到其他节点来保持数据一致性</li></ul><p><img src="https://kirito.iocoder.cn/image-20201220162434565.png" alt="集群正常状态"></p><p>如上图所示，每个节点服务一部分服务的读写，但每个节点都可以接收到读写请求，这时就存在两种读写情况：</p><ol><li>当该节点接收到属于该节点负责的服务时，直接读写。</li><li>当该节点接收到不属于该节点负责的服务时，将在集群内部路由，转发给对应的节点，从而完成读写。</li></ol><p>而当节点发生宕机后，原本该节点负责的一部分服务的读写任务会转移到其他节点，从而保证 Nacos 集群整体的可用性。</p><p><img src="https://kirito.iocoder.cn/image-20201220162345826.png" alt="部分节点宕机"></p><p>一个比较复杂的情况是，节点没有宕机，但是出现了网络分区，即下图所示：</p><p><img src="https://kirito.iocoder.cn/image-20201220162936471.png" alt="网络分区"></p><p>这个情况会损害可用性，客户端会表现为有时候服务存在有时候服务不存在。</p><p>综上，Nacos 的 distro 一致性协议可以保证在大多数情况下，集群中的机器宕机后依旧不损害整体的可用性。该可用性保证存在于 nacos-server 端。</p><h2 id="本地缓存文件-Failover-机制"><a href="#本地缓存文件-Failover-机制" class="headerlink" title="本地缓存文件 Failover 机制"></a>本地缓存文件 Failover 机制</h2><p>注册中心发生故障最坏的一个情况是整个 Server 端宕机，这时候 Nacos 依旧有高可用机制做兜底。</p><p>一道经典的 Dubbo 面试题：当 Dubbo 应用运行时，Nacos 注册中心宕机，会不会影响 RPC 调用。这个题目大多数应该都能回答出来，因为 Dubbo 内存里面是存了一份地址的，一方面这样的设计是为了性能，因为不可能每次 RPC 调用时都读取一次注册中心，另一面，这也起到了可用性的保障（尽管可能 Dubbo 设计者并没有考虑这个因素）。</p><p>那如果，我在此基础上再出一道 Dubbo 面试题：Nacos 注册中心宕机，Dubbo 应用发生重启，会不会影响 RPC 调用。如果了解了 Nacos 的 Failover 机制，应当得到和上一题同样的回答：不会。</p><p>Nacos 存在本地文件缓存机制，nacos-client 在接收到 nacos-server 的服务推送之后，会在内存中保存一份，随后会落盘存储一份快照。snapshot 默认的存储路径为：{USER_HOME}/nacos/naming/ 中</p><p><img src="https://kirito.iocoder.cn/image-20201220165548542.png" alt="Nacos snapshot 文件目录"></p><p>这份文件有两种价值，一是用来排查服务端是否正常推送了服务；二是当客户端加载服务时，如果无法从服务端拉取到数据，会默认从本地文件中加载。</p><blockquote><p>前提是构建 NacosNaming 时传入了该参数：namingLoadCacheAtStart=true</p><p>Dubbo 2.7.4 及以上版本支持该 Nacos 参数；开启该参数的方式：dubbo.registry.address=nacos://127.0.0.1:8848?namingLoadCacheAtStart=true</p></blockquote><p>在生产环境，推荐开启该参数，以避免注册中心宕机后，导致服务不可用的稳定，在服务注册发现场景，可用性和一致性 trade off 时，我们大多数时候会优先考虑可用性。</p><p>细心的读者还注意到 {USER_HOME}/nacos/naming/{namespace} 下除了缓存文件之外还有一个 failover 文件夹，里面存放着和 snapshot 一致的文件夹。这是 Nacos 的另一个 failover 机制，snapshot 是按照某个历史时刻的服务快照恢复恢复，而 failover 中的服务可以人为修改，以应对一些极端场景。</p><p>该可用性保证存在于 nacos-client 端。</p><h2 id="心跳同步服务"><a href="#心跳同步服务" class="headerlink" title="心跳同步服务"></a>心跳同步服务</h2><p>心跳机制一般广泛存在于分布式通信领域，用于确认存活状态。一般心跳请求和普通请求的设计是有差异的，心跳请求一般被设计的足够精简，这样在定时探测时可以尽可能避免性能下降。而在 Nacos 中，处于可用性的考虑，一个心跳报文包含了全部的服务信息，这样相比仅仅发送探测信息降低了吞吐量，而提升了可用性，怎么理解呢？考虑以下的两种场景：</p><ul><li>nacos-server 节点全部宕机，服务数据全部丢失。nacos-server 即使恢复运作，也无法恢复出服务，而心跳包含全部内容可以在心跳期间就恢复出服务，保证可用性。</li><li>nacos-server 出现网络分区。由于心跳可以创建服务，从而在极端网络故障下，依旧保证基础的可用性。</li></ul><p>以下是对心跳同步服务的测试，使用阿里云 MSE 提供 Nacos 集群进行测试</p><p><img src="https://kirito.iocoder.cn/image-20201220173117425.png" alt=""></p><p>调用 OpenApi：<code>curl -X &quot;DELETE mse-xxx-p.nacos-ans.mse.aliyuncs.com:8848/nacos/v1/ns/service?serviceName=providers:com.alibaba.edas.boot.EchoService:1.0.0:DUBBO&amp;groupName=DEFAULT_GROUP&quot;</code> 依次删除各个服务</p><p><img src="https://kirito.iocoder.cn/image-20201220173637589.png" alt=""></p><p>过 5s 后刷新，服务又再次被注册了上来，符合我们对心跳注册服务的预期。</p><h2 id="集群部署模式高可用"><a href="#集群部署模式高可用" class="headerlink" title="集群部署模式高可用"></a>集群部署模式高可用</h2><p>最后给大家分享的 Nacos 高可用特性来自于其部署架构。</p><h3 id="节点数量"><a href="#节点数量" class="headerlink" title="节点数量"></a>节点数量</h3><p>我们知道在生产集群中肯定不能以单机模式运行 Nacos，那么第一个问题便是：我应该部署几台机器？前面我们提到 Nacos 有两个一致性协议：distro 和 raft，distro 协议不会有脑裂问题，所以理论来说，节点数大于等于 2 即可；raft 协议的投票选举机制则建议是 2n+1 个节点。综合来看，选择 3 个节点是起码的，其次处于吞吐量和更高可用性的考量，可以选择 5 个，7 个，甚至 9 个节点的集群。</p><h3 id="多可用区部署"><a href="#多可用区部署" class="headerlink" title="多可用区部署"></a>多可用区部署</h3><p>组成集群的 Nacos 节点，应该尽可能考虑两个因素：</p><ol><li>各个节点之间的网络时延不能很高，否则会影响数据同步</li><li>各个节点所处机房、可用区应当尽可能分散，以避免单点故障</li></ol><p>以阿里云的 ECS 为例，选择同一个 Region 的不同可用区就是一个很好的实践</p><h3 id="部署模式"><a href="#部署模式" class="headerlink" title="部署模式"></a>部署模式</h3><p>主要分为 K8s 部署和 ECS 部署两种模式。</p><p>ECS 部署的优点在于简单，购买三台机器即可搭建集群，如果你熟练 Nacos 集群部署的话，这不是难事，但无法解决运维问题，如果 Nacos 某个节点出现 OOM 或者磁盘问题，很难迅速摘除，无法实现自运维。</p><p>K8s 部署的有点在于云原生运维能力强，可以在节点宕机后实现自恢复，保障 Nacos 的平稳运行。前面提到过，Nacos 和无状态的 Web 应用不同，它是一个有状态的应用，所以在 K8s 中部署，往往要借助于 StatefulSet 和 Operator 等组件才能实现 Nacos 集群的部署和运维。</p><h2 id="MSE-Nacos-的高可用最佳实践"><a href="#MSE-Nacos-的高可用最佳实践" class="headerlink" title="MSE Nacos 的高可用最佳实践"></a>MSE Nacos 的高可用最佳实践</h2><p>阿里云 MSE（微服务引擎）提供了 Nacos 集群的托管能力，实现了集群部署模式的高可用。</p><ul><li>当创建多个节点的集群时，系统会默认分配在不同可用区。同时，这对于用户来说又是透明的，用户只需要关心 Nacos 的功能即可，MSE 替用户兜底可用性。</li><li>MSE 底层使用 K8s 运维模式部署 Nacos。历史上出现过用户误用 Nacos 导致部分节点宕机的问题，但借助于 K8s 的自运维模式，宕机节点迅速被拉起，以至于用户可能都没有意识到自己发生宕机。</li></ul><p>下面模拟一个节点宕机的场景，来看看 K8s 如何实现自恢复。</p><p>一个三节点的 Nacos 集群：</p><p><img src="https://kirito.iocoder.cn/image-20201220180757477.png" alt="正常状态"></p><p>执行 <code>kubectl delete pod mse-7654c960-1605278296312-reg-center-0-2</code> 以模拟部分节点宕机的场景。</p><p><img src="https://kirito.iocoder.cn/image-20201220181056737.png" alt="恢复中"></p><p>大概 2 分钟后，节点恢复，并且角色发生了转换，Leader 从杀死的 2 号节点转给 1 号节点</p><p><img src="https://kirito.iocoder.cn/image-20201220181330884.png" alt="恢复后 leader 重选"></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文从多个角度出发，总结了一下 Nacos 是如何保障高可用的。高可用特性绝不是靠服务端多部署几个节点就可以获得的，而是要结合客户端使用方式、服务端部署模式、使用场景综合来考虑的一件事。</p><p>特别是在服务注册发现场景，Nacos 为可用性做了非常多的努力，而这些保障，Zookeeper 是不一定有的。在做注册中心选型时，可用性保障上，Nacos 绝对是优秀的。</p><p><img src="https://www.cnkirito.moe/css/images/wechat_public.jpg" alt="微信公众号"></p><p><em>「技术分享」<strong>某种程度上，是让作者和读者，不那么孤独的东西。</strong>欢迎关注我的微信公众号：<strong>「</strong>Kirito的技术分享」</em></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;服务注册发现是一个经久不衰的话题，Dubbo 早期开源时默认的注册中心 Zookeeper 最早进入人们的视线，并且在很长一段时间里，人们将注册中心和 Zookeeper 划上了等号，可能 Zookeeper 的设计者都没有想到这款产品对微服务领域造成了如此深厚的影响，直到 SpringCloud 开始流行，其自带的 Eureka 进入了人们的视野，人们这才意识到原来注册中心还可以有其他的选择。再到后来，热衷于开源的阿里把目光也聚焦在了注册中心这个领域，Nacos 横空出世。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://kirito.iocoder.cn/image-20201220140934757.png&quot; alt=&quot;注册中心&quot;&gt;&lt;/p&gt;
&lt;p&gt;Kirito 在做注册中心选型时的思考：曾经我没得选，现在我只想选择一个好的注册中心，它最好是开源的，这样开放透明，有自我的掌控力；不仅要开源，它还要有活跃的社区，以确保特性演进能够满足日益增长的业务需求，出现问题也能即使修复；最好…它的功能还要很强大，除了满足注册服务、推送服务外，还要有完善的微服务体系中所需的功能；最重要的，它还要稳定，最好有大厂的实际使用场景背书，证明这是一个经得起实战考验的产品；当然，云原生特性，安全特性也是很重要的…&lt;/p&gt;
&lt;p&gt;似乎 Kirito 对注册中心的要求实在是太高了，但这些五花八门的注册中心呈现在用户眼前，总是免不了一番比较。正如上面所言，功能特性、成熟度、可用性、用户体验度、云原生特性、安全都是可以拉出来做比较的话题。今天这篇文章重点介绍的是 Nacos 在可用性上体现，希望借助于这篇文章，能够让你对 Nacos 有一个更加深刻的认识。&lt;/p&gt;
    
    </summary>
    
      <category term="Nacos" scheme="https://lexburner.github.io/categories/Nacos/"/>
    
    
      <category term="Nacos" scheme="https://lexburner.github.io/tags/Nacos/"/>
    
  </entry>
  
  <entry>
    <title>鱼和熊掌兼得：同时使用 JPA 和 Mybatis</title>
    <link href="https://lexburner.github.io/jpa-and-mybatis/"/>
    <id>https://lexburner.github.io/jpa-and-mybatis/</id>
    <published>2020-11-12T04:30:01.000Z</published>
    <updated>2020-11-13T06:04:01.272Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>JPA 和 Mybatis 的争论由来已久，还记得在 2 年前我就在 spring4all 社区就两者孰优孰劣的话题发表了观点，我当时是力挺 JPA 的，这当然跟自己对 JPA 熟悉程度有关，但也有深层次的原因，便是 JPA 的设计理念契合了领域驱动设计的思想，可以很好地指导我们设计数据库交互接口。这两年工作中，逐渐接触了一些使用 Mybatis 的项目，也对其有了一定新的认知。都说认知是一个螺旋上升的过程，随着经验的累积，人们会轻易推翻过去，到了两年后的今天，我也有了新的观点。本文不是为了告诉你 JPA 和 Mybatis 到底谁更好，而是尝试求同存异，甚至是在项目中同时使用 JPA 和 Mybatis。什么？要同时使用两个 ORM 框架，有这个必要吗？别急着吐槽我，希望看完本文后，你也可以考虑在某些场合下同时使用这两个框架。</p><p>ps. 本文讨论的 JPA 特指 spring-data-jpa。</p><a id="more"></a><h2 id="建模"><a href="#建模" class="headerlink" title="建模"></a>建模</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"t_order"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Order</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">  <span class="meta">@Id</span></span><br><span class="line">  <span class="keyword">private</span> String oid;</span><br><span class="line">  </span><br><span class="line">  <span class="meta">@Embedded</span></span><br><span class="line">  <span class="keyword">private</span> CustomerVo customer;</span><br><span class="line">  </span><br><span class="line">  <span class="meta">@OneToMany</span>(cascade = &#123;CascadeType.ALL&#125;, orphanRemoval = <span class="keyword">true</span>, fetch = FetchType.LAZY, mappedBy = <span class="string">"order"</span>)</span><br><span class="line">  <span class="keyword">private</span> List&lt;OrderItem&gt; orderItems;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>JPA 最大的特点是 sqlless，如上述的实体定义，便将数据库的表和 Java 中的类型关联起来了，JPA 可以做到根据 @Entity 注解，自动创建表结构；基于这个实体实现的 Repository 接口，又使得 JPA 用户可以很方便地实现数据的 CRUD。所以，使用 JPA 的项目，人们很少会提到”数据库设计“，人们更关心的是领域建模，而不是数据建模。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">generatorConfiguration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context</span> <span class="attr">id</span>=<span class="string">"my"</span> <span class="attr">targetRuntime</span>=<span class="string">"MyBatis3"</span>&gt;</span></span><br><span class="line">      </span><br><span class="line">      <span class="tag">&lt;<span class="name">jdbcConnection</span> <span class="attr">driverClass</span>=<span class="string">"com.mysql.jdbc.Driver"</span> <span class="attr">connectionURL</span>=<span class="string">""</span></span></span><br><span class="line"><span class="tag">           <span class="attr">userId</span>=<span class="string">""</span> <span class="attr">password</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="tag">&lt;<span class="name">javaModelGenerator</span> <span class="attr">targetPackage</span>=<span class="string">""</span> <span class="attr">targetProject</span>=<span class="string">""</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="tag">&lt;<span class="name">sqlMapGenerator</span> <span class="attr">targetPackage</span>=<span class="string">""</span> <span class="attr">targetProject</span>=<span class="string">""</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="tag">&lt;<span class="name">javaClientGenerator</span> <span class="attr">targetPackage</span>=<span class="string">"moe.cnkirito.demo.mapper"</span> /&gt;</span></span><br><span class="line">      </span><br><span class="line">      <span class="tag">&lt;<span class="name">table</span> <span class="attr">tableName</span>=<span class="string">"t_order"</span> <span class="attr">domainObjectName</span>=<span class="string">"Order"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">context</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">generatorConfiguration</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Mybatis 用户更多使用的是逆向工程，例如 mybatis-generator 插件根据如上的 xml 配置，便可以直接将表结构转译成 mapper 文件和实体文件。</p><p>code first 和 table first 从结果来看是没有区别的，差异的是过程，所以设计良好的系统，并不会仅仅因为这个差异而高下立判，但从指导性来看，无疑设计系统时，更应该考虑的是实体和实体，实体和值对象的关联，领域边界的划分，而不是首先着眼于数据库表结构的设计。</p><p>建模角度来看，JPA 的领域建模思想更胜一筹。</p><h2 id="数据更新"><a href="#数据更新" class="headerlink" title="数据更新"></a>数据更新</h2><p>聊数据库自然离不开 CRUD，先来看增删改这些数据更新操作，来看看两个框架一般的习惯是什么。</p><p>JPA 推崇的数据更新只有一种范式，分成三步：</p><ol><li>先 findOne 映射成实体</li><li>内存内修改实体</li><li>实体整体 save</li></ol><p>你可能会反驳我说，@Query 也存在 nativeQuery 和 JPQL 的用法，但这并不是主流用法。JPA 特别强调”整体 save“的思想，这与领域驱动设计所强调的有状态密不可分，即其认为，修改不应该是针对于某一个字段：”update table set a=b where colomonA=xx“ ，而应该反映成实体的变化，save 则代表了实体状态最终的持久化。</p><p>先 find 后 save 显然也适用于 Mybatis，而 Mybatis 的灵活性，使得其数据更新方式更加地百花齐放。路人甲可以认为 JPA 墨守成规不懂变通，认为 Mybatis 不羁放纵爱自由；路人乙也可以认为 JPA 格式规范易维护，Mybatis 不成方圆。这点不多加评判，留后人说。</p><p>从个人习惯来说，我还是偏爱先 find 后整体 save 这种习惯的，不是说这是 JPA 的专利，Mybatis 不具备；而是 JPA 的强制性，让我有了这个习惯。 </p><p>数据更新角度来看，JPA 强制使用 find+save，mybatis 也可以做到这一点，胜者：无。</p><h2 id="数据查询"><a href="#数据查询" class="headerlink" title="数据查询"></a>数据查询</h2><p>JPA 提供的查询方式主要分为两种</p><ol><li>简单查询：findBy + 属性名</li><li>复杂查询：JpaSpecificationExecutor</li></ol><p>简单查询在一些简单的业务场景下提供了非常大的便捷性，findBy + 属性名可以自动转译成 sql，试问如果可以少写代码，有谁不愿意呢？</p><p>复杂查询则是 JPA 为了解决复杂的查询场景，提供的解决方案，硬是把数据库的一些聚合函数，连接操作，转换成了 Java 的方法，虽然做到了 sqlless，但写出来的代码又臭又长，也不见得有多么的易读易维护。这算是我最不喜欢 JPA 的一个地方了，但要解决复杂查询，又别无他法。</p><p>而 Mybatis 可以执行任意的查询 sql，灵活性是 JPA 比不了的。数据库小白搜索的最多的两个问题：</p><ol><li>数据库分页怎么做</li><li><p>条件查询怎么做</p><p>Mybatis 都可以轻松的解决。</p></li></ol><p>千万不要否认复杂查询：如聚合查询、Join 查询的场景。令一个 JPA 用户抓狂的最简单方式，就是给他一个复杂查询的 case。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> a,b,c,<span class="keyword">sum</span>(a) <span class="keyword">where</span> a=xx <span class="keyword">and</span> d=xx <span class="keyword">group</span> <span class="keyword">by</span> a,b,c;</span><br></pre></td></tr></table></figure><p>来吧，展示。可能 JPA 的确可以完成上述 sql 的转义，但要知道不是所有开发都是 JPA 专家，没人关心你用 JPA 解决了多么复杂的查询语句，更多的人关心地是，能不能下班前把这个复杂查询搞定，早点回家。</p><p>在回到复杂数据查询需求本身的来分析下。我们假设需求是合理的，毕竟项目的复杂性难以估计，可能有 1000 个数据查询需求 JPA 都可以很方便的实现，但就是有那么 10 几个复杂查询 JPA hold 不住。这个时候你只能乖乖地去写 sql 了，如果这个时候又出现一个条件查询的场景，出现了 if else 意味着连 @Query 都用不了，完全退化成了 JdbcTemplate 的时代。</p><p>那为什么不使用 Mybatis 呢？Mybatis 使用者从来没有纠结过复杂查询，它简直就是为之而生的。</p><p>如今很多 Mybatis 的插件，也可以帮助使用者快速的生成基础方法，虽然仍然需要写 sql，但是这对于开发者来说，并不是一件难事。</p><p>不要质疑高并发下，JOIN 操作和聚合函数存在的可能性，数据查询场景下，Mybatis 完胜。</p><h2 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h2><p>本质上 ORM 框架并没有性能的区分度，因为最终都是转换成 sql 交给数据库引擎去执行，ORM 层面那层性能损耗几乎可以忽略不计。</p><p>但从实际出发，Mybatis 提供给了开发者更高的 sql 自由度，所以在一些需要 sql 调优的场景下会更加灵活。</p><h2 id="可维护性"><a href="#可维护性" class="headerlink" title="可维护性"></a>可维护性</h2><p>前面我们提到 JPA 相比 Mybatis 丧失了 sql 的自由度，凡事必有 trade off，从另一个层面上来看，其提供了高层次的抽象，尝试用统一的模型去解决数据层面的问题。sqlless 同时也屏蔽了数据库的实现，屏蔽了数据库高低版本的兼容性问题，这对可能存在的数据库迁移以及数据库升级提供了很大的便捷性。</p><h2 id="同时使用两者"><a href="#同时使用两者" class="headerlink" title="同时使用两者"></a>同时使用两者</h2><p>其他细节我就不做分析了，相信还有很多点可以拿过来做对比，但我相信主要的点上文都应该有所提及了。进行以上维度的对比并不是我写这篇文章的初衷，更多地是想从实际开发角度出发，为大家使用这两个框架提供一些参考建议。</p><p>在大多数场景下，我习惯使用 JPA，例如设计领域对象时，得益于 JPA 的正向模型，我会优先考虑实体和值对象的关联性以及领域上下文的边界，而不用过多关注如何去设计表结构；在增删改和简单查询场景下，JPA 提供的 API 已经是刻在我 DNA 里面的范式了，使用起来非常的舒服。</p><p>在复杂查询场景下，例如</p><ol><li>包含不存在领域关联的 join 查询</li><li>包含多个聚合函数的复杂查询</li><li>其他 JPA 较难实现的查询</li></ol><p>我会选择使用 Mybatis，有点将 Mybatis 当做数据库视图生成器的意味。坚定不移的 JPA 拥趸者可能会质疑这些场景的存在的真实性，会质疑是不是设计的漏洞，但按照经验来看，哪怕是短期方案，这些场景也是客观存在的，所以听我一言，尝试拥抱一下 Mybatis 吧。</p><p>随着各类存储中间件的流行，例如 mongodb、ES，取代了数据库的一部分地位，重新思考下，本质上都是在用专业的工具解决特定场景的问题，最终目的都是为了解放生产力。数据库作为最古老，最基础的存储组件，的确承载了很多它本不应该承受的东西，那又何必让一个工具或者一个框架成为限制我们想象力的沟壑呢？</p><p>两个框架其实都不重，在 springboot 的加持下，引入几行配置就可以实现两者共存了。</p><p>我自己在最近的项目中便同时使用了两者，遵循的便是本文前面聊到的这些规范，我也推荐给你，不妨试试。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;JPA 和 Mybatis 的争论由来已久，还记得在 2 年前我就在 spring4all 社区就两者孰优孰劣的话题发表了观点，我当时是力挺 JPA 的，这当然跟自己对 JPA 熟悉程度有关，但也有深层次的原因，便是 JPA 的设计理念契合了领域驱动设计的思想，可以很好地指导我们设计数据库交互接口。这两年工作中，逐渐接触了一些使用 Mybatis 的项目，也对其有了一定新的认知。都说认知是一个螺旋上升的过程，随着经验的累积，人们会轻易推翻过去，到了两年后的今天，我也有了新的观点。本文不是为了告诉你 JPA 和 Mybatis 到底谁更好，而是尝试求同存异，甚至是在项目中同时使用 JPA 和 Mybatis。什么？要同时使用两个 ORM 框架，有这个必要吗？别急着吐槽我，希望看完本文后，你也可以考虑在某些场合下同时使用这两个框架。&lt;/p&gt;
&lt;p&gt;ps. 本文讨论的 JPA 特指 spring-data-jpa。&lt;/p&gt;
    
    </summary>
    
      <category term="JAVA" scheme="https://lexburner.github.io/categories/JAVA/"/>
    
    
      <category term="JAVA" scheme="https://lexburner.github.io/tags/JAVA/"/>
    
  </entry>
  
  <entry>
    <title>聊聊算法在面试中的地位</title>
    <link href="https://lexburner.github.io/talk-about-algorithm/"/>
    <id>https://lexburner.github.io/talk-about-algorithm/</id>
    <published>2020-10-08T07:43:47.000Z</published>
    <updated>2020-10-18T10:45:54.667Z</updated>
    
    <content type="html"><![CDATA[<p>前段时间，有一位好友找到我，向我打听阿里社招笔试是否看重算法题的考察，我给予了肯定的答复。他表现的有些沮丧，表示自己工程底子很扎实，框架源码也研究地很透彻，唯独算法能力不行，leetcode 上的简单题做起来都有点吃力。以至于面试一些公司时，基本都是前几面和面试官聊工程，相聊甚欢，一到笔试就 GG。鉴于我个人在学生时代有过 ACM 经历，对算法还是相当感冒的，个人算法能力不算出众，也不算弱，最好成绩是省赛金牌，区域赛铜牌（主要还是抱得队友的大腿），后来实在是写不动 C++ 了，中途转了 Java，借这个机会跟大家聊一聊，分享下个人对算法的一些认识。</p><a id="more"></a><p>我发现很多人有的一个观念是刷算法并不能很好地帮助他工作，他们中有些人是有了很多学校或者公司的项目经验，有些则是在数据库、RPC、大数据等某个垂直领域有了比较长时间的沉淀，他们会觉得刻意地刷算法题比较偏门，没有太大的价值。一方面有些人会比较自信，不认为需要靠算法来证明自己的价值，另一方面，有些人会认为刷算法题是应届生面试才需要考察的技能，对于社招来说，公司应该更注重考察项目经验和系统设计层面的技能。以我个人经验来看，面试互联网公司时，算法题几乎都是必考的一个环节，从公司的考察点出发，就可以佐证出，算法不重要这个观点的确是有待商榷的。还有一些人轻视算法，是觉得只有大厂才看重算法，一些小公司的面试根本不 care 算法，而且特别是像我文章开头提到那个朋友一样的人，有着比较强的工程能力，我相信在面试中一定可以凭借着这个优势，赢得面试官的好感，那我不妨再反问一句，为什么要让算法成为你的软肋呢？</p><p>我已经表露了我对面试中算法重要程度的态度，而且我也认为面试中考察算法能力是非常重要的一环。在公司里做项目，我们往往需要花费数个月去落地，而面试中完成算法题最多只限制在半小时内，虽然时间区间不同，但本质上都是在考察一个人在一个固定的时间内完成某个任务的能力。读题考察了候选人的理解能力，期间我会与候选人沟通，以确保他正确理解的题意，并且在码字之前，我会要求对方先讲解题思路，这考察了沟通能力，有的候选人可能没有经历过刷题训练，缺少一些常见的算法思维，但经过提示后，如果能快速地完成 coding，在笔试中或许也能够通过。所以你看，其实考察算法题其实和也是借此考验了你的工作能力，它要求你在短短的半个小时之内做到 Buf Free，一定程度上这比做工程更难，因为没有人为你测试，而你要想通过这一环节，是需要额外花费精力去训练的。</p><p>虽然我认为面试中算法很重要，推荐大家准备面试时多去刷刷题，但我也确实抵制一些偏题、怪题。以我的刷题经验和工作经验结合来看，推荐的难度为 leetcode 简单、中等题，ACM 铜牌、银牌题，仅供参考。记得有一次瞄了一眼阿里的校招在线笔试题，具体是哪个部门不清楚，那个难度估计得是地狱难度了，这类情况仅仅是小概率会发生，至少在我们大部门不会出现特别难的算法题。</p><p>很多人说面试造火箭，入职拧螺丝，以此来讽刺面试中算法面是不必要的，我是不赞同的。抛开面试，算法能力也的确是工作中帮助了我。简单举几个例子吧，我通过算法题接触到了欧拉函数、GCD 等数论知识，让我可以非常好地理解 RSA 加密的原理和实现过程，而 RSA 加密是很有可能在工程中被使用到的一种非对称加密方式；通过解决常见的数据结构类算法题，我了解到了跳表的实现，这方便了我去理解 Redis 的 Set 结构；熟练地解决贪心和 DP 等问题，也潜移默化地影响着我在工程项目中的代码逻辑。</p><p>字节跳动可以说是业内有名的看重算法面的公司了，但鉴于本人并不了解实际的情况，只能跟大家聊聊阿里的算法面试。分成两部分：实习生面试和社招面试。我这里的经验主要都是基于我所了解的情况，在阿里其他部门（非阿里云）可能情况就不一样了。先说实习生面试吧，算法主要考察的是简单题，主要以贪心、数据结构、模拟为主，可以说非常友好了，主要考验学生对于基础知识的掌握程度，但也要求候选人能够在较短时间内完成，否则很难在整体面试中获得 A 评价。而社招，算法面试的地位肯定是要低于工程能力的考核的，但是对于能不能发 offer 又起着决定性的作用，等于说，即使你的工作履历很 match 岗位，工程经验也很丰富，但算法面一塌糊涂，往往用人部门只能忍痛割爱了。</p><p>如果你正在准备面试，我是建议准备下算法，刷一些题目找下手感，leetcode 和各种在线 OJ 都是不错的选择，B 站也有很多视频，具体的刷题列表，我这儿没准备，相信你可以在网上找到很多的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前段时间，有一位好友找到我，向我打听阿里社招笔试是否看重算法题的考察，我给予了肯定的答复。他表现的有些沮丧，表示自己工程底子很扎实，框架源码也研究地很透彻，唯独算法能力不行，leetcode 上的简单题做起来都有点吃力。以至于面试一些公司时，基本都是前几面和面试官聊工程，相聊甚欢，一到笔试就 GG。鉴于我个人在学生时代有过 ACM 经历，对算法还是相当感冒的，个人算法能力不算出众，也不算弱，最好成绩是省赛金牌，区域赛铜牌（主要还是抱得队友的大腿），后来实在是写不动 C++ 了，中途转了 Java，借这个机会跟大家聊一聊，分享下个人对算法的一些认识。&lt;/p&gt;
    
    </summary>
    
      <category term="技术杂谈" scheme="https://lexburner.github.io/categories/%E6%8A%80%E6%9C%AF%E6%9D%82%E8%B0%88/"/>
    
    
      <category term="招聘" scheme="https://lexburner.github.io/tags/%E6%8B%9B%E8%81%98/"/>
    
      <category term="算法" scheme="https://lexburner.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>gson 替换 fastjson 引发的线上问题分析</title>
    <link href="https://lexburner.github.io/serialize-practice/"/>
    <id>https://lexburner.github.io/serialize-practice/</id>
    <published>2020-09-15T10:37:56.000Z</published>
    <updated>2020-09-16T02:41:19.992Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Json 序列化框架存在的安全漏洞一直以来都是程序员们挂在嘴边调侃的一个话题，尤其是这两年 fastjson 由于被针对性研究，更是频频地的报出漏洞，出个漏洞不要紧，可安全团队总是用邮件催着线上应用要进行依赖升级，这可就要命了，我相信很多小伙伴也是不胜其苦，考虑了使用其他序列化框架替换 fastjson。这不，最近我们就有一个项目将 fastjson 替换为了 gson，引发了一个线上的问题。分享下这次的经历，以免大家踩到同样的坑，在此警示大家，规范千万条，安全第一条，升级不规范，线上两行泪。</p><h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>线上一个非常简单的逻辑，将对象序列化成 fastjson，再使用 HTTP 请求将字符串发送出去。原本工作的好好的，在将 fastjson 替换为 gson 之后，竟然引发了线上的 OOM。经过内存 dump 分析，发现竟然发送了一个 400 M+ 的报文，由于 HTTP 工具没有做发送大小的校验，强行进行了传输，直接导致了线上服务整体不可用。</p><a id="more"></a><h2 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h2><p>为什么同样是 Json 序列化，fastjson 没出过问题，而换成 gson 之后立马就暴露了呢？通过分析内存 dump 的数据，发现很多字段的值都是重复的，再结合我们业务数据的特点，一下子定位到了问题 – gson 序列化重复对象存在严重的缺陷。</p><p>直接用一个简单的例子，来说明当时的问题。模拟线上的数据特性，使用 <code>List&lt;Foo&gt;</code> 添加进同一个引用对象</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Foo foo = <span class="keyword">new</span> Foo();</span><br><span class="line">Bar bar = <span class="keyword">new</span> Bar();</span><br><span class="line">List&lt;Foo&gt; foos = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">3</span>;i++)&#123;</span><br><span class="line">    foos.add(foo);</span><br><span class="line">&#125;</span><br><span class="line">bar.setFoos(foos);</span><br><span class="line"></span><br><span class="line">Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">String gsonStr = gson.toJson(bar);</span><br><span class="line">System.out.println(gsonStr);</span><br><span class="line"></span><br><span class="line">String fastjsonStr = JSON.toJSONString(bar);</span><br><span class="line">System.out.println(fastjsonStr);</span><br></pre></td></tr></table></figure><p>观察打印结果：</p><p>gson：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;<span class="attr">"foos"</span>:[&#123;<span class="attr">"a"</span>:<span class="string">"aaaaa"</span>&#125;,&#123;<span class="attr">"a"</span>:<span class="string">"aaaaa"</span>&#125;,&#123;<span class="attr">"a"</span>:<span class="string">"aaaaa"</span>&#125;]&#125;</span><br></pre></td></tr></table></figure><p>fastjson：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;<span class="attr">"foos"</span>:[&#123;<span class="attr">"a"</span>:<span class="string">"aaaaa"</span>&#125;,&#123;<span class="attr">"$ref"</span>:<span class="string">"$.foos[0]"</span>&#125;,&#123;<span class="attr">"$ref"</span>:<span class="string">"$.foos[0]"</span>&#125;]&#125;</span><br></pre></td></tr></table></figure><p>可以发现 gson 处理重复对象，是对每个对象都进行了序列化，而 fastjson 处理重复对象，是将除第一个对象外的其他对象使用引用符号 <code>$ref</code> 进行了标记。</p><p>当单个重复对象的数量非常多，以及单个对象的提交较大时，两种不同的序列化策略会导致一个质变，我们不妨来针对特殊的场景进行下对比。 </p><h2 id="压缩比测试"><a href="#压缩比测试" class="headerlink" title="压缩比测试"></a>压缩比测试</h2><ul><li><p>序列化对象：包含大量的属性。以模拟线上的业务数据。</p></li><li><p>重复次数：200。即 List 中包含 200 个同一引用的对象，以模拟线上复杂的对象结构，扩大差异性。</p></li><li>序列化方式：gson、fastjson、Java、Hessian2。额外引入了 Java 和 Hessian2 的对照组，方便我们了解各个序列化框架在这个特殊场景下的表现。</li><li>主要观察各个序列化方式压缩后的字节大小，因为这关系到网络传输时的大小；次要观察反序列后 List 中还是不是同一个对象</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException, ClassNotFoundException </span>&#123;</span><br><span class="line">        Foo foo = <span class="keyword">new</span> Foo();</span><br><span class="line">        Bar bar = <span class="keyword">new</span> Bar();</span><br><span class="line">        List&lt;Foo&gt; foos = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">200</span>;i++)&#123;</span><br><span class="line">            foos.add(foo);</span><br><span class="line">        &#125;</span><br><span class="line">        bar.setFoos(foos);</span><br><span class="line">        <span class="comment">// gson</span></span><br><span class="line">        Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">        String gsonStr = gson.toJson(bar);</span><br><span class="line">        System.out.println(gsonStr.length());</span><br><span class="line">        Bar gsonBar = gson.fromJson(fastjsonStr, Bar.class);</span><br><span class="line">        System.out.println(gsonBar.getFoos().get(<span class="number">0</span>) == gsonBar.getFoos().get(<span class="number">1</span>));  </span><br><span class="line">        <span class="comment">// fastjson</span></span><br><span class="line">        String fastjsonStr = JSON.toJSONString(bar);</span><br><span class="line">        System.out.println(fastjsonStr.length());</span><br><span class="line">        Bar fastjsonBar = JSON.parseObject(fastjsonStr, Bar.class);</span><br><span class="line">        System.out.println(fastjsonBar.getFoos().get(<span class="number">0</span>) == fastjsonBar.getFoos().get(<span class="number">1</span>));</span><br><span class="line"><span class="comment">// java</span></span><br><span class="line">        ByteArrayOutputStream byteArrayOutputStream = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        ObjectOutputStream oos = <span class="keyword">new</span> ObjectOutputStream(byteArrayOutputStream);</span><br><span class="line">        oos.writeObject(bar);</span><br><span class="line">        oos.close();</span><br><span class="line">        System.out.println(byteArrayOutputStream.toByteArray().length);</span><br><span class="line">        ObjectInputStream ois = <span class="keyword">new</span> ObjectInputStream(<span class="keyword">new</span> ByteArrayInputStream(byteArrayOutputStream.toByteArray()));</span><br><span class="line">        Bar javaBar = (Bar) ois.readObject();</span><br><span class="line">        ois.close();</span><br><span class="line">        System.out.println(javaBar.getFoos().get(<span class="number">0</span>) == javaBar.getFoos().get(<span class="number">1</span>));</span><br><span class="line">        <span class="comment">// hessian2</span></span><br><span class="line">        ByteArrayOutputStream hessian2Baos = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        Hessian2Output hessian2Output = <span class="keyword">new</span> Hessian2Output(hessian2Baos);</span><br><span class="line">        hessian2Output.writeObject(bar);</span><br><span class="line">        hessian2Output.close();</span><br><span class="line">        System.out.println(hessian2Baos.toByteArray().length);</span><br><span class="line">        ByteArrayInputStream hessian2Bais = <span class="keyword">new</span> ByteArrayInputStream(hessian2Baos.toByteArray());</span><br><span class="line">        Hessian2Input hessian2Input = <span class="keyword">new</span> Hessian2Input(hessian2Bais);</span><br><span class="line">        Bar hessian2Bar = (Bar) hessian2Input.readObject();</span><br><span class="line">        hessian2Input.close();</span><br><span class="line">        System.out.println(hessian2Bar.getFoos().get(<span class="number">0</span>) == hessian2Bar.getFoos().get(<span class="number">1</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出结果：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">gson:</span><br><span class="line">62810</span><br><span class="line">false</span><br><span class="line"></span><br><span class="line">fastjson:</span><br><span class="line">4503</span><br><span class="line">true</span><br><span class="line"></span><br><span class="line">Java:</span><br><span class="line">1540</span><br><span class="line">true</span><br><span class="line"></span><br><span class="line">Hessian2:</span><br><span class="line">686</span><br><span class="line">true</span><br></pre></td></tr></table></figure><p>结论分析：由于单个对象序列化后的体积较大，采用引用表示的方式可以很好的缩小体积，可以发现 gson 并没有采取这种序列化优化策略，导致体积膨胀。甚至一贯不被看好的 Java 序列化都比其优秀的多，而 Hessian2 更是夸张，直接比 gson 优化了 2个数量级。并且反序列化后，gson 并不能将原本是同一引用的对象还原回去，而其他的序列化框架均可以实现这一点。</p><h2 id="吞吐量测试"><a href="#吞吐量测试" class="headerlink" title="吞吐量测试"></a>吞吐量测试</h2><p>除了关注序列化之后数据量的大小，各个序列化的吞吐量也是我们关心的一个点。使用基准测试可以精准地测试出各个序列化方式的吞吐量。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@BenchmarkMode</span>(&#123;Mode.Throughput&#125;)</span><br><span class="line"><span class="meta">@State</span>(Scope.Benchmark)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MicroBenchmark</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Bar bar;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Setup</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">prepare</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Foo foo = <span class="keyword">new</span> Foo();</span><br><span class="line">        Bar bar = <span class="keyword">new</span> Bar();</span><br><span class="line">        List&lt;Foo&gt; foos = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">200</span>;i++)&#123;</span><br><span class="line">            foos.add(foo);</span><br><span class="line">        &#125;</span><br><span class="line">        bar.setFoos(foos);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Benchmark</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">gson</span><span class="params">()</span></span>&#123;</span><br><span class="line">        String gsonStr = gson.toJson(bar);</span><br><span class="line">        gson.fromJson(gsonStr, Bar.class);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Benchmark</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fastjson</span><span class="params">()</span></span>&#123;</span><br><span class="line">        String fastjsonStr = JSON.toJSONString(bar);</span><br><span class="line">        JSON.parseObject(fastjsonStr, Bar.class);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Benchmark</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">java</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        ByteArrayOutputStream byteArrayOutputStream = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        ObjectOutputStream oos = <span class="keyword">new</span> ObjectOutputStream(byteArrayOutputStream);</span><br><span class="line">        oos.writeObject(bar);</span><br><span class="line">        oos.close();</span><br><span class="line"></span><br><span class="line">        ObjectInputStream ois = <span class="keyword">new</span> ObjectInputStream(<span class="keyword">new</span> ByteArrayInputStream(byteArrayOutputStream.toByteArray()));</span><br><span class="line">        Bar javaBar = (Bar) ois.readObject();</span><br><span class="line">        ois.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Benchmark</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hessian2</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        ByteArrayOutputStream hessian2Baos = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        Hessian2Output hessian2Output = <span class="keyword">new</span> Hessian2Output(hessian2Baos);</span><br><span class="line">        hessian2Output.writeObject(bar);</span><br><span class="line">        hessian2Output.close();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        ByteArrayInputStream hessian2Bais = <span class="keyword">new</span> ByteArrayInputStream(hessian2Baos.toByteArray());</span><br><span class="line">        Hessian2Input hessian2Input = <span class="keyword">new</span> Hessian2Input(hessian2Bais);</span><br><span class="line">        Bar hessian2Bar = (Bar) hessian2Input.readObject();</span><br><span class="line">        hessian2Input.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> RunnerException </span>&#123;</span><br><span class="line">        Options opt = <span class="keyword">new</span> OptionsBuilder()</span><br><span class="line">            .include(MicroBenchmark.class.getSimpleName())</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Runner(opt).run();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>吞吐量报告：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Benchmark                 Mode  Cnt        Score         Error  Units</span><br><span class="line">MicroBenchmark.fastjson  thrpt   25  6724809.416 ± 1542197.448  ops/s</span><br><span class="line">MicroBenchmark.gson      thrpt   25  1508825.440 ±  194148.657  ops/s</span><br><span class="line">MicroBenchmark.hessian2  thrpt   25   758643.567 ±  239754.709  ops/s</span><br><span class="line">MicroBenchmark.java      thrpt   25   734624.615 ±   66892.728  ops/s</span><br></pre></td></tr></table></figure><p>是不是有点出乎意料，fastjson 竟然独领风骚，文本类序列化的吞吐量相比二进制序列化的吞吐量要高出一个数量级，分别是每秒百万级和每秒十万级的吞吐量。</p><h2 id="整体测试结论"><a href="#整体测试结论" class="headerlink" title="整体测试结论"></a>整体测试结论</h2><ul><li>fastjson 序列化过后带有 $ 的引用标记也能够被 gson 正确的反序列化，但笔者并没有找到让 gson 序列化时转换成引用的配置</li><li>fastjson、hessian、java 均支持循环引用的解析；gson 不支持</li><li>fastjson 可以设置 DisableCircularReferenceDetect，关闭循环引用和重复引用的检测</li><li>gson 反序列化之前的同一个引用的对象，在经历了序列化再反序列化回来之后，不会被认为是同一个对象，可能会导致内存对象数量的膨胀；而 fastjson、java、hessian2 等序列化方式由于记录的是引用标记，不存在该问题</li><li>以笔者的测试 case 为例，hessian2 具有非常强大的序列化压缩比，适合大报文序列化后供网络传输的场景使用</li><li>以笔者的测试 case 为例，fastjson 具有非常高的吞吐量，对得起它的 fast，适合需要高吞吐的场景使用</li><li>序列化还需要考虑到是否支持循环引用，是否支持循环对象优化，是否支持枚举类型、集合、数组、子类、多态、内部类、泛型等综合场景，以及是否支持可视化等比较的场景，增删字段后的兼容性等等特性。综合来看，笔者比较推荐 hessian2 和 fastjson 两种序列化方式</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>大家都知道 fastjson 为了快，做了相对一些较为 hack 的逻辑，这也导致其漏洞较多，但我认为编码都是在 trade off 之中进行的，如果有一个完美的框架，那其他竞品框架早就不会存在了。笔者对各个序列化框架的研究也不深，可能你会说 jackson 更加优秀，我只能说能解决你的场景遇到的问题，那就是合适的框架。</p><p>最后，想要替换序列化框架时一定要慎重，了解清楚替代框架的特性，可能原先框架解决的问题，新的框架不一定能很好的 cover。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;Json 序列化框架存在的安全漏洞一直以来都是程序员们挂在嘴边调侃的一个话题，尤其是这两年 fastjson 由于被针对性研究，更是频频地的报出漏洞，出个漏洞不要紧，可安全团队总是用邮件催着线上应用要进行依赖升级，这可就要命了，我相信很多小伙伴也是不胜其苦，考虑了使用其他序列化框架替换 fastjson。这不，最近我们就有一个项目将 fastjson 替换为了 gson，引发了一个线上的问题。分享下这次的经历，以免大家踩到同样的坑，在此警示大家，规范千万条，安全第一条，升级不规范，线上两行泪。&lt;/p&gt;
&lt;h2 id=&quot;问题描述&quot;&gt;&lt;a href=&quot;#问题描述&quot; class=&quot;headerlink&quot; title=&quot;问题描述&quot;&gt;&lt;/a&gt;问题描述&lt;/h2&gt;&lt;p&gt;线上一个非常简单的逻辑，将对象序列化成 fastjson，再使用 HTTP 请求将字符串发送出去。原本工作的好好的，在将 fastjson 替换为 gson 之后，竟然引发了线上的 OOM。经过内存 dump 分析，发现竟然发送了一个 400 M+ 的报文，由于 HTTP 工具没有做发送大小的校验，强行进行了传输，直接导致了线上服务整体不可用。&lt;/p&gt;
    
    </summary>
    
      <category term="JAVA" scheme="https://lexburner.github.io/categories/JAVA/"/>
    
    
      <category term="JAVA" scheme="https://lexburner.github.io/tags/JAVA/"/>
    
  </entry>
  
  <entry>
    <title>三种思维，让我脱胎换骨</title>
    <link href="https://lexburner.github.io/three-thinking-ways/"/>
    <id>https://lexburner.github.io/three-thinking-ways/</id>
    <published>2020-08-30T13:04:52.000Z</published>
    <updated>2020-08-30T14:50:58.799Z</updated>
    
    <content type="html"><![CDATA[<h2 id="闭环思维"><a href="#闭环思维" class="headerlink" title="闭环思维"></a>闭环思维</h2><p><strong>闭环思维简而言之就是，如果别人发起一件事，你不管做的如何，最后都要闭环到这个发起者。</strong></p><p>我曾经就经历过这样的事：</p><p>我让一个实习生写一个问卷。 </p><p>两个小时过去了，没有反馈，三个小时过去了还是没有反馈，直到中午吃饭的时候，我碰到了他。</p><p>我问他，写完了吗？他说写完了。 </p><p>我说，那你怎么没发给我看，他说，写完直接发出去了。 </p><p><strong>你会对这个人怎么评价？不靠谱。</strong></p><p><strong>有闭环思维的人是怎么做呢？</strong></p><p>第一，如果他完成了，那么就要及时反馈，并且说一下当时的情景。 </p><p>第二，如果他没完成，也要及时反馈，是哪里遇到困难了，需不需要帮助。</p><p>以上无论哪一种情况，都有一个闭环，那就是由你本人发起又回到你的身上。</p><p><strong>不管做得如何，一定要给出一个反馈，形成闭环，有太多有才华的人就死在这一环节了。</strong></p><p>这件事我也没怪他，想想曾经的自己，也是一个“反馈黑洞”，这事交给我了，我最后给你弄完了就行了，老反馈多麻烦啊。最主要的是，减少反馈就多了偷懒的机会，自以为蒙混过关，其实已经被打上了不靠谱的标签。</p><p><strong>过去有句话说的分非常好：凡事有交代，件件有着落，事事有回应。这就是闭环思维。</strong></p><h2 id="成长型思维"><a href="#成长型思维" class="headerlink" title="成长型思维"></a>成长型思维</h2><p>成长型思维的人面对挑战与失败总是能够迎难而上；相反，成长型思维对应的另一种思维是固定型思维，他们面对挑战与失败总会下意识地回避。</p><p>曾经的我是一个不折不扣的固定型思维者：</p><p>小学时候觉得英语难，逃避，高中英语没及过格，大四才过四级；</p><p>中学时候因为一次物理考试没考好，对物理失去了兴趣，大学物理考了3次才过；</p><p>高中时候因为被喜欢的女生拒绝了，失去了追求异性的勇气，此后一段时间不敢和女生说话；</p><p>工作之后，领导交代一件事，我第一反应就是这个我不会，我做不了；</p><p>……</p><p>为什么会这样？</p><p><strong>归根结底是因为我害怕失败，害怕被否定</strong>，错误的把别人对某件事的否定当成了对自己这个人的否定。而不被否定的唯一方法就是不去做那件事。所以自己的能力圈就越来越小，进步的脚步也戛然而止。</p><p>后来我转变态度，我承认自己害怕失败，但我不服，越怕什么我就越做什么。</p><p>当初不是觉得自己学不好英语吗？我就去考研，虽然没考上，但我英语分数却不低，而且明显感觉英语并没有那么难学。</p><p>当初不是怕物理吗？我就去读物理相关的读物，我竟然还觉得很有意思，经常给人科普，别人经常会说：你怎么什么都知道啊？</p><p>当初不是不敢跟女生说话吗？我就疯狂和女生聊，线下搭讪，线上社交软件，虽然聊死了很多女生，但我却练就了和任何女生都能聊得来的能力。</p><p>……</p><p>做完之后我发现，失败是一件再正常不过的事，你能把它当成一座挡住你的大山，就可以做一个勇敢的登山人。</p><h2 id="批判性思维"><a href="#批判性思维" class="headerlink" title="批判性思维"></a>批判性思维</h2><p>所谓批判性思维，并不是指：</p><p>凡事先推翻立论，从反面去证明别人是错的；</p><p>以怀疑论者的思维方式，认为谁都是错的，谁说的都不在理；</p><p>传说中的杠精？怼天怼地怼空气。</p><p><strong>而是对自己的批判</strong></p><p>我们无法避免遇事时，总会以自己为标准去评判事物的正确性。这是与生俱来的惰性思维，却也总能通过自身的努力去降低偏见。</p><p>凡事要习惯回过头来三思。比如某个人和你讲一件事，你第一感觉可能觉得他完全在胡说八道，但是，一定要想第二遍，是否我错了，他对了？这一遍思考，一定不能假设自己是对的；</p><p>如果又想了第二遍，还是觉得自己对，对方错，</p><p>要想第三遍，是否是我的境界不够，不能理解他？为什么要想第三遍呢？<strong>因为任何一个想要精进的人，都要承认自己的无知，挑战自己的固有观念，拥抱新的观点。这就是批判性思维的真谛。</strong></p><p>对于自身的批判，最实用的，就是每日对自身的复盘。推敲一天内的工作生活是否符合自己的目标状态，是否还有遗漏或可以改进的地方。</p><p>作者：风茧<br>链接：<a href="https://www.zhihu.com/question/23913984/answer/754533449" target="_blank" rel="noopener">https://www.zhihu.com/question/23913984/answer/754533449</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;闭环思维&quot;&gt;&lt;a href=&quot;#闭环思维&quot; class=&quot;headerlink&quot; title=&quot;闭环思维&quot;&gt;&lt;/a&gt;闭环思维&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;闭环思维简而言之就是，如果别人发起一件事，你不管做的如何，最后都要闭环到这个发起者。&lt;/strong&gt;&lt;/p
      
    
    </summary>
    
      <category term="随笔" scheme="https://lexburner.github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
      <category term="思维方式" scheme="https://lexburner.github.io/tags/%E6%80%9D%E7%BB%B4%E6%96%B9%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>lambda 表达式导致 Arthas 无法 redefine 的问题</title>
    <link href="https://lexburner.github.io/arthas-lambda-redefine/"/>
    <id>https://lexburner.github.io/arthas-lambda-redefine/</id>
    <published>2020-08-26T13:04:52.000Z</published>
    <updated>2020-08-26T13:10:48.300Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文出处：<a href="https://m.jb51.net/article/188155.htm" target="_blank" rel="noopener">https://m.jb51.net/article/188155.htm</a></p><p>作者：鲁严波</p></blockquote><p>这篇文章主要介绍了 lambda 表达式导致 Arthas 无法 redefine 的问题,本文通过图文实例相结合给大家介绍的非常详细，对大家的学习或工作具有一定的参考借鉴价值，需要的朋友可以参考下。</p><p>通过 arthas 的 redefine 命令，可以做到不用重新发布，就可以改变程序行为。</p><p>但是用多了，发现很多时候，我们就改了几行代码，甚至有的时候就添加了一行日志，就无法 redefine 了。提示：</p><blockquote><p>redefine error! java.lang.UnsupportedOperationException: class redefinition failed: attempted to add a method</p></blockquote><a id="more"></a><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602467.png?202055165825" alt="img"></p><p>它提示我们新增加方法，那我们就看看是不是新增加了方法。通过 javap 来查看定义的方法：</p><p>这是老的类：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602468.jpg?202055165825" alt="img"></p><p>这是新的类：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602469.jpg?202055165825" alt="img"></p><p>对比之后发现，新的类，即本地编译的类，其中的 lambda 对应的方法名都是 lambda$getAllCity$0 这样的，最后的编号是从 0 开始的。</p><p>而旧的类，即现在在运行的类，其中的同一个 lambda 的方法名是 lambda$getAllCity$121，最后的编号是一个非常大的数字。</p><p>在仔细对比下，发现是 jdk 的版本问题，不同的 jdk 版本对与 lamdba 的处理可能不一致。</p><p>具体来说，线上编译的 jdk 版本是 1.8.0_66-b17， 而本地是 1.8.0_222-b10，而这两个版本对 lambda 对应的方法命名是不一样的。</p><p>首先，为了调试方便，写一个最小复现用例来看看：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Compile.java</span></span><br><span class="line"><span class="comment">// 编译LamdbaTest1.java和LamdbaTest2.java</span></span><br><span class="line"><span class="keyword">import</span> javax.tools.*;</span><br><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Compile</span> </span>&#123;</span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  String path1 = <span class="string">"/path/to/LamdbaTest1.java"</span>;</span><br><span class="line">  String path2 = <span class="string">"/path/to/LamdbaTest2.java"</span>;</span><br><span class="line">  JavaCompiler javaCompiler = ToolProvider.getSystemJavaCompiler();</span><br><span class="line">  DiagnosticCollector diagnostics = <span class="keyword">new</span> DiagnosticCollector();</span><br><span class="line">  StandardJavaFileManager fileManager = javaCompiler.getStandardFileManager(diagnostics, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">  Iterable&lt;? extends JavaFileObject&gt; compilationUnits = fileManager.getJavaFileObjects(</span><br><span class="line">    <span class="keyword">new</span> File(path1),</span><br><span class="line">    <span class="keyword">new</span> File(path2)</span><br><span class="line">  );</span><br><span class="line">  JavaCompiler.CompilationTask task = javaCompiler.getTask(<span class="keyword">null</span>, fileManager, diagnostics, <span class="keyword">null</span>, <span class="keyword">null</span>,</span><br><span class="line">    compilationUnits);</span><br><span class="line">  <span class="keyword">boolean</span> success = task.call();</span><br><span class="line">  System.out.println(success);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//LamdbaTest1.java</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LamdbaTest1</span> </span>&#123;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">(Runnable runnable)</span> </span>&#123;</span><br><span class="line">  runnable.run();</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">()</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">  test(() -&gt; &#123;</span><br><span class="line">   System.out.println(<span class="number">11</span>);</span><br><span class="line">  &#125;);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//LamdbaTest2.java</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LamdbaTest2</span> </span>&#123;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">(Runnable runnable)</span> </span>&#123;</span><br><span class="line">  runnable.run();</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">()</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">  test(() -&gt; &#123;</span><br><span class="line">   System.out.println(<span class="number">22</span>);</span><br><span class="line">  &#125;);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 1.8.0_222-b10（新版本 jdk）跑完了之后，发现 LamdbaTest2 中的 lambda 方法是：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602470.png?202055165825" alt="img"></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> lambda$main$<span class="number">0</span>();</span><br></pre></td></tr></table></figure><p>而换版本 1.8.0_66-b17（旧版本 jdk）之后，lambda 的方法就成了：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602471.jpg?202055165825" alt="img"></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> lambda$main$<span class="number">1</span>();</span><br></pre></td></tr></table></figure><p>多尝试几个文件同时编译，我们就可以发现：对于旧版本的 javac，末尾这个数字是全局递增的，50 个类有 100 个 lambda，那最后一个 lambda 的编号就是 99；而新的版本是每个类重新计数的，和总共多少个类没有关系。</p><p>确认了问题之后，接下来就是不断的打断点、重试了。后来发现不同版本的 javac 逻辑确实不同。</p><p>首先，查看 jdk 源码可以知道，lambda 的方法名都是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lambda$&lt;methodname&gt;$&lt;lambdaCount&gt;</span><br></pre></td></tr></table></figure><p>代码见 <code>LambdaToMethod.java</code></p><p>不同的地方在于： 新版本的 javac，在处理一个新的类的时候，会保存上一个 lambdaCount，后续再恢复：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602472.jpg?202055165825" alt="img"></p><p>而旧版本则没有这个逻辑：</p><p><img src="https://img.jbzj.com/file_images/article/202006/202065165602473.jpg?202055165825" alt="img"></p><p>这就说明旧版本的编译器确实是 lambda 全局编号的。</p><p>那，问题来了，这个行为是从哪个版本变掉的呢？</p><p>对比之后发现这个变更是 jdk8u74-b02 引入的。对应的 bug 是 <a href="https://bugs.openjdk.java.net/browse/JDK-8067422，基本上就是每个类内的" target="_blank" rel="noopener">https://bugs.openjdk.java.net/browse/JDK-8067422，基本上就是每个类内的</a> lambda 单独编号，确保编译顺序不会影响 lambda 的方法名字。</p><p>所以，解决方案很简单，升级编译环境的 jdk 版本就好。</p><p>非常巧合的是，前两天为了更好的适配 Docker 运行环境（通俗的讲，就是在容器内获取到 docker 的 cpu 配额，而不是物理机器的 cpu 数量），我找运维添加了一个新的j dk 版本 1.8.0_231-b11，这样只需要直接将编译环境的 jdk 版本切换到 8u231 就行！</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文出处：&lt;a href=&quot;https://m.jb51.net/article/188155.htm&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://m.jb51.net/article/188155.htm&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;作者：鲁严波&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这篇文章主要介绍了 lambda 表达式导致 Arthas 无法 redefine 的问题,本文通过图文实例相结合给大家介绍的非常详细，对大家的学习或工作具有一定的参考借鉴价值，需要的朋友可以参考下。&lt;/p&gt;
&lt;p&gt;通过 arthas 的 redefine 命令，可以做到不用重新发布，就可以改变程序行为。&lt;/p&gt;
&lt;p&gt;但是用多了，发现很多时候，我们就改了几行代码，甚至有的时候就添加了一行日志，就无法 redefine 了。提示：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;redefine error! java.lang.UnsupportedOperationException: class redefinition failed: attempted to add a method&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Arthas" scheme="https://lexburner.github.io/categories/Arthas/"/>
    
    
      <category term="Arthas" scheme="https://lexburner.github.io/tags/Arthas/"/>
    
  </entry>
  
  <entry>
    <title>Dubbo 迈向云原生的里程碑 | 应用级服务发现</title>
    <link href="https://lexburner.github.io/dubbo-app-pubsub/"/>
    <id>https://lexburner.github.io/dubbo-app-pubsub/</id>
    <published>2020-06-15T16:33:50.000Z</published>
    <updated>2020-06-15T16:54:13.207Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1 概述"></a>1 概述</h2><p>社区版本 Dubbo 从 2.7.5 版本开始，新引入了一种基于应用粒度的服务发现机制，这是 Dubbo 为适配云原生基础设施的一步重要探索。版本发布到现在已有近半年时间，经过这段时间的探索与总结，我们对这套机制的可行性与稳定性有了更全面、深入的认识；同时在 Dubbo 3.0 的规划也在全面进行中，如何让应用级服务发现成为未来下一代服务框架 Dubbo 3.0 的基础服务模型，解决云原生、规模化微服务集群扩容与可伸缩性问题，也已经成为我们当前工作的重点。</p><p>既然这套新机制如此重要，那它到底是怎么工作的，今天我们就来详细解读一下。在最开始的社区版本，我们给这个机制取了一个神秘的名字 - 服务自省，下文将进一步解释这个名字的由来，并引用服务自省代指这套应用级服务发现机制。</p><p>熟悉 Dubbo 开发者应该都知道，一直以来都是面向 RPC 方法去定义服务的，并且这也是 Dubbo 开发友好性、治理功能强的基础。既然如此，那我们为什么还要定义个应用粒度的服务发现机制那？这个机制到底是怎么工作的？它与当前机制的区别是什么？它能给我们带来哪些好处那？对适配云原生、性能提升又有哪些帮助？</p><p>带着所有的这些问题，我们开始本文的讲解。</p><a id="more"></a><h2 id="2-服务自省是什么？"><a href="#2-服务自省是什么？" class="headerlink" title="2 服务自省是什么？"></a>2 服务自省是什么？</h2><p>首先，我们先来解释文章开篇提到的问题：</p><ul><li>应用粒度服务发现是到底是一种怎样的模型，它与当前的 Dubbo 服务发现模型的区别是什么？</li><li>我们为什么叫它服务自省？</li></ul><p>所谓“应用/实例粒度” 或者“RPC 服务粒度”强调的是一种地址发现的数据组织格式。</p><p><img src="https://kirito.iocoder.cn/46ddc6ead81d44fe1c68fb189a687f3e3fa60101.png" alt="1"></p><p>以 Dubbo 当前的地址发现数据格式为例，它是“RPC 服务粒度”的，它是以 RPC 服务作为 key，以实例列表作为 value 来组织数据的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&quot;RPC Service1&quot;: [</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instance1&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&quot;timeout&quot;:1000&#125;&#125;,</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instance2&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&quot;timeout&quot;:2000&#125;&#125;,</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instance3&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&quot;timeout&quot;:3000&#125;&#125;,</span><br><span class="line">]</span><br><span class="line">&quot;RPC Service2&quot;: [Instance list of RPC Service2],</span><br><span class="line">&quot;RPC ServiceN&quot;: [Instance list of RPC ServiceN]</span><br></pre></td></tr></table></figure><p>而我们新引入的“应用粒度的服务发现”，它以应用名（Application）作为 key，以这个应用部署的一组实例（Instance）列表作为 value。这带来两点不同：</p><ol><li>数据映射关系变了，从 RPC Service -&gt; Instance 变为 Application -&gt; Instance</li><li>数据变少了，注册中心没有了 RPC Service 及其相关配置信息</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&quot;application1&quot;: [</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instance1&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&#125;&#125;,</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instance2&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&#125;&#125;,</span><br><span class="line">  &#123;&quot;name&quot;:&quot;instanceN&quot;, &quot;ip&quot;:&quot;127.0.0.1&quot;, &quot;metadata&quot;:&#123;&#125;&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>要进一步理解新模型带来的变化，我们看一下应用与 RPC 服务间的关系，显而易见的，1 个应用内可能会定义 n 个 RPC Service。因此 Dubbo 之前的服务发现粒度更细，在注册中心产生的数据条目也会更多（与 RPC 服务成正比），同时也存在一定的数据冗余。</p><p>简单理解了应用级服务发现的基本机制，接着解释它为什么会被叫做“服务自省”？其实这还是得从它的工作原理说起，上面我们提到，应用粒度服务发现的数据模型有几个以下明显变化：数据中心的数据量少了，RPC 服务相关的数据在注册中心没有了，现在只有 application - instance 这两个层级的数据。为了保证这部分缺少的 RPC 服务数据仍然能被 Consumer 端正确的感知，我们在 Consumer 和 Provider 间建立了一条单独的通信通道：<strong>Consumer 和 Provider 两两之间通过特定端口交换信息</strong>，我们把这种 Provider 自己主动暴露自身信息的行为认为是一种内省机制，因此从这个角度出发，我们把整个机制命名为：服务自省。</p><h2 id="3-为什么需要服务自省？"><a href="#3-为什么需要服务自省？" class="headerlink" title="3 为什么需要服务自省？"></a>3 为什么需要服务自省？</h2><p>上面讲服务自省的大概原理的时候也提到了它给注册中心带来的几点不同，这几点不同体现在 Dubbo 框架侧（甚至整个微服务体系中），有以下优势：</p><ol><li>与业界主流微服务模型对齐，比如 SpringCloud、Kubernetes Native Service等。</li><li>提升性能与可伸缩性。注册中心数据的重新组织（减少），能最大幅度的减轻注册中心的存储、推送压力，进而减少 Dubbo Consumer 侧的地址计算压力；集群规模也开始变得可预测、可评估（与 RPC 接口数量无关，只与实例部署规模相关）。</li></ol><h3 id="3-1-对齐主流微服务模型"><a href="#3-1-对齐主流微服务模型" class="headerlink" title="3.1 对齐主流微服务模型"></a>3.1 对齐主流微服务模型</h3><p>自动、透明的实例地址发现（负载均衡）是所有微服务框架需要解决的事情，这能让后端的部署结构对上游微服务透明，上游服务只需要从收到的地址列表中选取一个，发起调用就可以了。要实现以上目标，涉及两个关键点的自动同步：</p><ul><li>实例地址，服务消费方需要知道地址以建立链接</li><li>RPC 方法定义，服务消费方需要知道 RPC 服务的具体定义，不论服务类型是 rest 或 rmi 等。</li></ul><p><img src="https://kirito.iocoder.cn/de21f1ab3366f3c1ecaa648f0ad6e0d84aac7440.png" alt="2"></p><p>对于 RPC 实例间借助注册中心的数据同步，REST 定义了一套非常有意思的成熟度模型，感兴趣的朋友可以参考这里的链接 <a href="https://yq.aliyun.com/go/articleRenderRedirect?url=https%3A%2F%2Fwww.martinfowler.com%2Farticles%2FrichardsonMaturityModel.html" target="_blank" rel="noopener">https://www.martinfowler.com/articles/richardsonMaturityModel.html</a>， 按照文章中的 4 级成熟度定义，Dubbo 当前基于接口粒度的模型可以对应到 L4 级别。</p><p>接下来，我们看看 Dubbo、SpringCloud 以及 Kubernetes 分别是怎么围绕自动化的实例地址发现这个目标设计的。</p><p><strong>1. Spring Cloud</strong></p><p>Spring Cloud 通过注册中心只同步了应用与实例地址，消费方可以基于实例地址与服务提供方建立链接，但是消费方对于如何发起 http 调用（SpringCloud 基于 rest 通信）一无所知，比如对方有哪些 http endpoint，需要传入哪些参数等。</p><p>RPC 服务这部分信息目前都是通过线下约定或离线的管理系统来协商的。这种架构的优缺点总结如下。<br>优势：部署结构清晰、地址推送量小；<br>缺点：地址订阅需要指定应用名， provider 应用变更（拆分）需消费端感知；RPC 调用无法全自动同步。</p><p><img src="https://kirito.iocoder.cn/51d71432a1411e85c0f4ed483c5a6739bcd7909c.png" alt="3"></p><p><strong>2. Dubbo</strong></p><p>Dubbo 通过注册中心同时同步了实例地址和 RPC 方法，因此其能实现 RPC 过程的自动同步，面向 RPC 编程、面向 RPC 治理，对后端应用的拆分消费端无感知，其缺点则是地址推送数量变大，和 RPC 方法成正比。</p><p><img src="https://kirito.iocoder.cn/470ae02261cec1b1884737bd703222507027d184.png" alt="4"></p><p><strong>3. Dubbo + Kubernetes</strong></p><p>Dubbo 要支持 Kubernetes native service，相比之前自建注册中心的服务发现体系来说，在工作机制上主要有两点变化：</p><ul><li>服务注册由平台接管，provider 不再需要关心服务注册</li><li>consumer 端服务发现将是 Dubbo 关注的重点，通过对接平台层的 API-Server、DNS 等，Dubbo client 可以通过一个 <a href="https://yq.aliyun.com/go/articleRenderRedirect?url=https%3A%2F%2Fkubernetes.io%2Fdocs%2Fconcepts%2Fservices-networking%2Fservice%2F" target="_blank" rel="noopener">Service Name</a>（通常对应到 Application Name）查询到一组 <a href="">Endpoints</a>（一组运行 provider 的 pod），通过将 Endpoints 映射到 Dubbo 内部地址列表，以驱动 Dubbo 内置的负载均衡机制工作。</li></ul><blockquote><p>Kubernetes Service 作为一个抽象概念，怎么映射到 Dubbo 是一个值得讨论的点</p><ul><li><p>Service Name - &gt; Application Name，Dubbo 应用和 Kubernetes 服务一一对应，对于微服务运维和建设环节透明，与开发阶段解耦。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">&gt;   apiVersion: v1</span><br><span class="line">&gt;   kind: Service</span><br><span class="line">&gt;   metadata:</span><br><span class="line">&gt;     name: provider-app-name</span><br><span class="line">&gt;   spec:</span><br><span class="line">&gt;     selector:</span><br><span class="line">&gt;   app: provider-app-name</span><br><span class="line">&gt;     ports:</span><br><span class="line">&gt;   - protocol: TCP</span><br><span class="line">&gt;   port: </span><br><span class="line">&gt;   targetPort: 9376</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></li></ul></blockquote><blockquote><ul><li><p>Service Name - &gt; Dubbo RPC Service，Kubernetes 要维护调度的服务与应用内建 RPC 服务绑定，维护的服务数量变多。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">&gt;   ---</span><br><span class="line">&gt;   apiVersion: v1</span><br><span class="line">&gt;   kind: Service</span><br><span class="line">&gt;   metadata:</span><br><span class="line">&gt;     name: rpc-service-1</span><br><span class="line">&gt;   spec:</span><br><span class="line">&gt;     selector:</span><br><span class="line">&gt;       app: provider-app-name</span><br><span class="line">&gt;     ports: ##</span><br><span class="line">&gt;   ...</span><br><span class="line">&gt;   ---</span><br><span class="line">&gt;   apiVersion: v1</span><br><span class="line">&gt;   kind: Service</span><br><span class="line">&gt;   metadata:</span><br><span class="line">&gt;     name: rpc-service-2</span><br><span class="line">&gt;   spec:</span><br><span class="line">&gt;     selector:</span><br><span class="line">&gt;       app: provider-app-name</span><br><span class="line">&gt;     ports: ##</span><br><span class="line">&gt;   ...</span><br><span class="line">&gt;   ---</span><br><span class="line">&gt;   apiVersion: v1</span><br><span class="line">&gt;   kind: Service</span><br><span class="line">&gt;   metadata:</span><br><span class="line">&gt;     name: rpc-service-N</span><br><span class="line">&gt;   spec:</span><br><span class="line">&gt;     selector:</span><br><span class="line">&gt;       app: provider-app-name</span><br><span class="line">&gt;     ports: ##</span><br><span class="line">&gt;   ...</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></li></ul></blockquote><p><img src="https://kirito.iocoder.cn/b986d8df9f93eeb6d985e37604225dd68984f4db.png" alt="5"></p><p>结合以上几种不同微服务框架模型的分析，我们可以发现，Dubbo 与 SpringCloud、Kubernetes 等不同产品在微服务的抽象定义上还是存在很大不同的。SpringCloud 和 Kubernetes 在微服务的模型抽象上还是比较接近的，两者基本都只关心实例地址的同步，如果我们去关心其他的一些服务框架产品，会发现它们绝大多数也是这么设计的；</p><blockquote><p>即 REST 成熟度模型中的 L3 级别。</p></blockquote><p>对比起来 Dubbo 则相对是比较特殊的存在，更多的是从 RPC 服务的粒度去设计的。</p><blockquote><p>对应 REST 成熟度模型中的 L4 级别。</p></blockquote><p>如我们上面针对每种模型做了详细的分析，每种模型都有其优势和不足。而我们最初决定 Dubbo 要做出改变，往其他的微服务发现模型上的对齐，是我们最早在确定 Dubbo 的云原生方案时，我们发现要让 Dubbo 去支持 Kubernetes Native Service，模型对齐是一个基础条件；另一点是来自用户侧对 Dubbo 场景化的一些工程实践的需求，得益于 Dubbo 对多注册、多协议能力的支持，使得 Dubbo 联通不同的微服务体系成为可能，而服务发现模型的不一致成为其中的一个障碍，这部分的场景描述请参见以下文章：<a href="https://yq.aliyun.com/go/articleRenderRedirect?url=https%3A%2F%2Fwww.atatech.org%2Farticles%2F157719" target="_blank" rel="noopener">https://www.atatech.org/articles/157719</a></p><h3 id="3-2-更大规模的微服务集群-解决性能瓶颈"><a href="#3-2-更大规模的微服务集群-解决性能瓶颈" class="headerlink" title="3.2 更大规模的微服务集群 - 解决性能瓶颈"></a>3.2 更大规模的微服务集群 - 解决性能瓶颈</h3><p>这部分涉及到和注册中心、配置中心的交互，关于不同模型下注册中心数据的变化，之前原理部分我们简单分析过。为更直观的对比服务模型变更带来的推送效率提升，我们来通过一个示例看一下不同模型注册中心的对比：</p><p><img src="https://kirito.iocoder.cn/c275bcf54f023eeac0e63d90e5068a6ec2686893.png" alt="6"></p><p>图中左边是微服务框架的一个典型工作流程，Provider 和 Consumer 通过注册中心实现自动化的地址通知。其中，Provider 实例的信息如图中表格所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">应用 DEMO 包含三个接口 DemoService 1 2 3，当前实例的 ip 地址为 10.210.134.30。</span><br></pre></td></tr></table></figure><ul><li>对于 Spring Cloud 和 Kubernetes 模型，注册中心只会存储一条 <code>DEMO - 10.210.134.30+metadata</code> 的数据；</li><li>对于老的 Dubbo 模型，注册中心存储了三条接口粒度的数据，分别对应三个接口 DemoService 1 2 3，并且很多的址数据都是重复的；</li></ul><p>可以总结出，基于应用粒度的模型所存储和推送的数据量是和应用、实例数成正比的，只有当我们的应用数增多或应用的实例数增长时，地址推送压力才会上涨。<br>而对于基于接口粒度的模型，数据量是和接口数量正相关的，鉴于一个应用通常发布多个接口的现状，这个数量级本身比应用粒度是要乘以倍数的；另外一个关键点在于，接口粒度导致的集群规模评估的不透明，相对于实i例、应用增长都通常是在运维侧的规划之中，接口的定义更多的是业务侧的内部行为，往往可以绕过评估给集群带来压力。</p><p>以 Consumer 端服务订阅举例，根据我对社区部分 Dubbo 中大规模头部用户的粗略统计，根据受统计公司的实际场景，一个 Consumer 应用要消费（订阅）的 Provier 应用数量往往要超过 10 个，而具体到其要消费（订阅）的的接口数量则通常要达到 30 个，平均情况下 Consumer 订阅的 3 个接口来自同一个 Provider 应用，如此计算下来，如果以应用粒度为地址通知和选址基本单位，则平均地址推送和计算量将下降 60% 还要多，<br>而在极端情况下，也就是当 Consumer 端消费的接口更多的来自同一个应用时，这个地址推送与内存消耗的占用将会进一步得到降低，甚至可以超过 80% 以上。</p><p>一个典型的几段场景即是 Dubbo 体系中的网关型应用，有些网关应用消费（订阅）达 100+ 应用，而消费（订阅）的服务有 1000+ ，平均有 10 个接口来自同一个应用，如果我们把地址推送和计算的粒度改为应用，则地址推送量从原来的 n <em>1000 变为 n</em> 100，地址数量降低可达近 90%。</p><h2 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h2><h3 id="设计原则"><a href="#设计原则" class="headerlink" title="设计原则"></a>设计原则</h3><p>上面一节我们从<strong>服务模型</strong>及<strong>支撑大规模集群</strong>的角度分别给出了 Dubbo 往应用级服务发现靠拢的好处或原因，但这么做的同时接口粒度的服务治理能力还是要继续保留，这是 Dubbo 框架编程模型易用性、服务治理能力优势的基础。<br>以下是我认为我们做服务模型迁移仍要坚持的设计原则</p><ul><li>新的服务发现模型要实现对原有 Dubbo 消费端开发者的无感知迁移，即 Dubbo 继续面向 RPC 服务编程、面向 RPC 服务治理，做到对用户侧完全无感知。</li><li>建立 Consumer 与 Provider 间的自动化 RPC 服务元数据协调机制，解决传统微服务模型无法同步 RPC 级接口配置的缺点。</li></ul><h3 id="基本原理详解"><a href="#基本原理详解" class="headerlink" title="基本原理详解"></a>基本原理详解</h3><p>应用级服务发现作为一种新的服务发现机制，和以前 Dubbo 基于 RPC 服务粒度的服务发现在核心流程上基本上是一致的：即服务提供者往注册中心注册地址信息，服务消费者从注册中心拉取&amp;订阅地址信息。</p><p>这里主要的不同有以下两点：</p><h4 id="注册中心数据以“应用-实例列表”格式组织，不再包含-RPC-服务信息"><a href="#注册中心数据以“应用-实例列表”格式组织，不再包含-RPC-服务信息" class="headerlink" title="注册中心数据以“应用 - 实例列表”格式组织，不再包含 RPC 服务信息"></a>注册中心数据以“应用 - 实例列表”格式组织，不再包含 RPC 服务信息</h4><p><img src="https://kirito.iocoder.cn/82c0d6c6222e4f582a275a70ff52332dcb5dba20.png" alt="7"></p><p>以下是每个 Instance metadata 的示例数据，总的原则是 metadata 只包含当前 instance 节点相关的信息，不涉及 RPC 服务粒度的信息。</p><p>总体信息概括如下：实例地址、实例各种环境标、metadata service 元数据、其他少量必要属性。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"name"</span>: <span class="string">"provider-app-name"</span>,</span><br><span class="line">    <span class="attr">"id"</span>: <span class="string">"192.168.0.102:20880"</span>,</span><br><span class="line">    <span class="attr">"address"</span>: <span class="string">"192.168.0.102"</span>,</span><br><span class="line">    <span class="attr">"port"</span>: <span class="number">20880</span>,</span><br><span class="line">    <span class="attr">"sslPort"</span>: <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">"payload"</span>: &#123;</span><br><span class="line">        <span class="attr">"id"</span>: <span class="literal">null</span>,</span><br><span class="line">        <span class="attr">"name"</span>: <span class="string">"provider-app-name"</span>,</span><br><span class="line">        <span class="attr">"metadata"</span>: &#123;</span><br><span class="line">            <span class="attr">"metadataService"</span>: <span class="string">"&#123;\"dubbo\":&#123;\"version\":\"1.0.0\",\"dubbo\":\"2.0.2\",\"release\":\"2.7.5\",\"port\":\"20881\"&#125;&#125;"</span>,</span><br><span class="line">            <span class="attr">"endpoints"</span>: <span class="string">"[&#123;\"port\":20880,\"protocol\":\"dubbo\"&#125;]"</span>,</span><br><span class="line">            <span class="attr">"storage-type"</span>: <span class="string">"local"</span>,</span><br><span class="line">            <span class="attr">"revision"</span>: <span class="string">"6785535733750099598"</span>,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">"registrationTimeUTC"</span>: <span class="number">1583461240877</span>,</span><br><span class="line">    <span class="attr">"serviceType"</span>: <span class="string">"DYNAMIC"</span>,</span><br><span class="line">    <span class="attr">"uriSpec"</span>: <span class="literal">null</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="Client-–-Server-自行协商-RPC-方法信息"><a href="#Client-–-Server-自行协商-RPC-方法信息" class="headerlink" title="Client – Server 自行协商 RPC 方法信息"></a>Client – Server 自行协商 RPC 方法信息</h4><p>在注册中心不再同步 RPC 服务信息后，服务自省在服务消费端和提供端之间建立了一条内置的 RPC 服务信息协商机制，这也是“服务自省”这个名字的由来。服务端实例会暴露一个预定义的 MetadataService RPC 服务，消费端通过调用 MetadataService 获取每个实例 RPC 方法相关的配置信息。</p><p><img src="https://kirito.iocoder.cn/89e96f895f76912a7c2d1a6e8b44643176f3c431.png" alt="8"></p><p>当前 MetadataService 返回的数据格式如下，</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  <span class="string">"dubbo://192.168.0.102:20880/org.apache.dubbo.demo.DemoService?anyhost=true&amp;application=demo-provider&amp;deprecated=false&amp;dubbo=2.0.2&amp;dynamic=true&amp;generic=false&amp;interface=org.apache.dubbo.demo.DemoService&amp;methods=sayHello&amp;pid=9585&amp;release=2.7.5&amp;side=provider&amp;timestamp=1583469714314"</span>, </span><br><span class="line"> <span class="string">"dubbo://192.168.0.102:20880/org.apache.dubbo.demo.HelloService?anyhost=true&amp;application=demo-provider&amp;deprecated=false&amp;dubbo=2.0.2&amp;dynamic=true&amp;generic=false&amp;interface=org.apache.dubbo.demo.DemoService&amp;methods=sayHello&amp;pid=9585&amp;release=2.7.5&amp;side=provider&amp;timestamp=1583469714314"</span>,</span><br><span class="line">  <span class="string">"dubbo://192.168.0.102:20880/org.apache.dubbo.demo.WorldService?anyhost=true&amp;application=demo-provider&amp;deprecated=false&amp;dubbo=2.0.2&amp;dynamic=true&amp;generic=false&amp;interface=org.apache.dubbo.demo.DemoService&amp;methods=sayHello&amp;pid=9585&amp;release=2.7.5&amp;side=provider&amp;timestamp=1583469714314"</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><blockquote><p>熟悉 Dubbo 基于 RPC 服务粒度的服务发现模型的开发者应该能看出来，服务自省机制机制将以前注册中心传递的 URL 一拆为二：</p><ul><li>一部分和实例相关的数据继续保留在注册中心，如 ip、port、机器标识等。</li><li>另一部分和 RPC 方法相关的数据从注册中心移除，转而通过 MetadataService 暴露给消费端。</li></ul><p><strong>理想情况下是能达到数据按照实例、RPC 服务严格区分开来，但明显可以看到以上实现版本还存在一些数据冗余，有些也数据还未合理划分。尤其是 MetadataService 部分，其返回的数据还只是简单的 URL 列表组装，这些 URL其实是包含了全量的数据。</strong></p></blockquote><p>以下是服务自省的一个完整工作流程图，详细描述了服务注册、服务发现、MetadataService、RPC 调用间的协作流程。</p><p><img src="https://kirito.iocoder.cn/e8c286b5e79d9c085431840d3ee57537ece95d2e.png" alt="9"></p><ul><li>服务提供者启动，首先解析应用定义的“普通服务”并依次注册为 RPC 服务，紧接着注册内建的 MetadataService 服务，最后打开 TCP 监听端口。</li><li>启动完成后，将实例信息注册到注册中心（仅限 ip、port 等实例相关数据），提供者启动完成。</li><li>服务消费者启动，首先依据其要“消费的 provider 应用名”到注册中心查询地址列表，并完成订阅（以实现后续地址变更自动通知）。</li><li>消费端拿到地址列表后，紧接着对 MetadataService 发起调用，返回结果中包含了所有应用定义的“普通服务”及其相关配置信息。</li><li>至此，消费者可以接收外部流量，并对提供者发起 Dubbo RPC 调用</li></ul><blockquote><p>在以上流程中，我们只考虑了一切顺利的情况，但在更详细的设计或编码实现中，我们还需要严格约定一些异常场景下的框架行为。比如，如果消费者 MetadataService 调用失败，则在重试知道成功之前，消费者将不可以接收外部流量。</p></blockquote><h3 id="服务自省中的关键机制"><a href="#服务自省中的关键机制" class="headerlink" title="服务自省中的关键机制"></a>服务自省中的关键机制</h3><h4 id="元数据同步机制"><a href="#元数据同步机制" class="headerlink" title="元数据同步机制"></a>元数据同步机制</h4><p>Client 与 Server 间在收到地址推送后的配置同步是服务自省的关键环节，目前针对元数据同步有两种具体的可选方案，分别是：</p><ul><li>内建 MetadataService。</li><li>独立的元数据中心，通过中细化的元数据集群协调数据。</li></ul><p><strong>1. 内建 MetadataService</strong><br>MetadataService 通过标准的 Dubbo 协议暴露，根据查询条件，会将内存中符合条件的“普通服务”配置返回给消费者。这一步发生在消费端选址和调用前。</p><p><strong>元数据中心</strong><br>复用 2.7 版本中引入的元数据中心，provider 实例启动后，会尝试将内部的 RPC 服务组织成元数据的格式到元数据中心，而 consumer 则在每次收到注册中心推送更新后，主动查询元数据中心。</p><blockquote><p>注意 consumer 端查询元数据中心的时机，是等到注册中心的地址更新通知之后。也就是通过注册中心下发的数据，我们能明确的知道何时某个实例的元数据被更新了，此时才需要去查元数据中心。</p></blockquote><p><img src="https://kirito.iocoder.cn/7a2c72bc9f5e541e8a155a01b303d4c77590a65a.png" alt="10"></p><h4 id="RPC-服务-lt-gt-应用映射关系"><a href="#RPC-服务-lt-gt-应用映射关系" class="headerlink" title="RPC 服务 &lt; - &gt; 应用映射关系"></a>RPC 服务 &lt; - &gt; 应用映射关系</h4><p>回顾上文讲到的注册中心关于“应用 - 实例列表”结构的数据组织形式，这个变动目前对开发者并不是完全透明的，业务开发侧会感知到查询/订阅地址列表的机制的变化。具体来说，相比以往我们基于 RPC 服务来检索地址，现在 consumer 需要通过指定 provider 应用名才能实现地址查询或订阅。</p><p>老的 Consumer 开发与配置示例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- 框架直接通过 RPC Service 1/2/N 去注册中心查询或订阅地址列表 --&gt;</span><br><span class="line">&lt;dubbo:registry address=&quot;zookeeper://127.0.0.1:2181&quot;/&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service 1&quot; /&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service 2&quot; /&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service N&quot; /&gt;</span><br></pre></td></tr></table></figure><p>新的 Consumer 开发与配置示例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- 框架需要通过额外的 provided-by=&quot;provider-app-x&quot; 才能在注册中心查询或订阅到地址列表 --&gt;</span><br><span class="line">&lt;dubbo:registry address=&quot;zookeeper://127.0.0.1:2181?registry-type=service&quot;/&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service 1&quot; provided-by=&quot;provider-app-x&quot;/&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service 2&quot; provided-by=&quot;provider-app-x&quot; /&gt;</span><br><span class="line">&lt;dubbo:reference interface=&quot;RPC Service N&quot; provided-by=&quot;provider-app-y&quot; /&gt;</span><br></pre></td></tr></table></figure><p>以上指定 provider 应用名的方式是 Spring Cloud 当前的做法，需要 consumer 端的开发者显示指定其要消费的 provider 应用。</p><p>以上问题的根源在于注册中心不知道任何 RPC 服务相关的信息，因此只能通过应用名来查询。</p><p>为了使整个开发流程对老的 Dubbo 用户更透明，同时避免指定 provider 对可扩展性带来的影响（参见下方说明），我们设计了一套 <code>RPC 服务到应用名</code>的映射关系，以尝试在 consumer 自动完成 RPC 服务到 provider 应用名的转换。</p><p><img src="https://kirito.iocoder.cn/4a92841e861c49424a2635dfb695e58ea78f70ee.png" alt="11"></p><blockquote><p>Dubbo 之所以选择建立一套“接口-应用”的映射关系，主要是考虑到 service - app 映射关系的不确定性。一个典型的场景即是应用/服务拆分，如上面提到的配置<code>，PC Service 2 是定义于 provider-app-x 中的一个服务，未来它随时可能会被开发者分拆到另外一个新的应用如 provider-app-x-1 中，这个拆分要被所有的 PC Service 2 消费方感知到，并对应用进行修改升级，如改为</code>，这样的升级成本不可否认还是挺高的。<br>到底是 Dubbo 框架帮助开发者透明的解决这个问题，还是交由开发者自己去解决，当然这只是个策略选择问题，并且 Dubbo 2.7.5+ 版本目前是都提供了的。其实我个人更倾向于交由业务开发者通过组织上的约束来做，这样也可进一步降低 Dubbo 框架的复杂度，提升运行态的稳定性。</p></blockquote><h2 id="总结与展望"><a href="#总结与展望" class="headerlink" title="总结与展望"></a>总结与展望</h2><p>应用级服务发现机制是 Dubbo 面向云原生走出的重要一步，它帮 Dubbo 打通了与其他微服务体系之间在地址发现层面的鸿沟，也成为 Dubbo 适配 Kubernetes Native Service 等基础设施的基础。我们期望 Dubbo 在新模型基础上，能继续保留在编程易用性、服务治理能力等方面强大的优势。但是我们也应该看到应用粒度的模型一方面带来了新的复杂性，需要我们继续去优化与增强；另一方面，除了地址存储与推送之外，应用粒度在帮助 Dubbo 选址层面也有进一步挖掘的潜力。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-概述&quot;&gt;&lt;a href=&quot;#1-概述&quot; class=&quot;headerlink&quot; title=&quot;1 概述&quot;&gt;&lt;/a&gt;1 概述&lt;/h2&gt;&lt;p&gt;社区版本 Dubbo 从 2.7.5 版本开始，新引入了一种基于应用粒度的服务发现机制，这是 Dubbo 为适配云原生基础设施的一步重要探索。版本发布到现在已有近半年时间，经过这段时间的探索与总结，我们对这套机制的可行性与稳定性有了更全面、深入的认识；同时在 Dubbo 3.0 的规划也在全面进行中，如何让应用级服务发现成为未来下一代服务框架 Dubbo 3.0 的基础服务模型，解决云原生、规模化微服务集群扩容与可伸缩性问题，也已经成为我们当前工作的重点。&lt;/p&gt;
&lt;p&gt;既然这套新机制如此重要，那它到底是怎么工作的，今天我们就来详细解读一下。在最开始的社区版本，我们给这个机制取了一个神秘的名字 - 服务自省，下文将进一步解释这个名字的由来，并引用服务自省代指这套应用级服务发现机制。&lt;/p&gt;
&lt;p&gt;熟悉 Dubbo 开发者应该都知道，一直以来都是面向 RPC 方法去定义服务的，并且这也是 Dubbo 开发友好性、治理功能强的基础。既然如此，那我们为什么还要定义个应用粒度的服务发现机制那？这个机制到底是怎么工作的？它与当前机制的区别是什么？它能给我们带来哪些好处那？对适配云原生、性能提升又有哪些帮助？&lt;/p&gt;
&lt;p&gt;带着所有的这些问题，我们开始本文的讲解。&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="DUBBO" scheme="https://lexburner.github.io/tags/DUBBO/"/>
    
  </entry>
  
  <entry>
    <title>平滑迁移 Dubbo 服务的思考</title>
    <link href="https://lexburner.github.io/dubbo-migration/"/>
    <id>https://lexburner.github.io/dubbo-migration/</id>
    <published>2020-05-30T15:17:38.000Z</published>
    <updated>2020-09-22T03:48:18.862Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>近日，有报道称在 HashCorp 的商业软件试用协议上发现，旗下所有商业产品禁止在中国境内使用、部署、安装，这其中就包含了 Terraform, Consul, Vagrant 等众多知名软件，其中 Consul 是一个在微服务领域的开源软件，可以用于做注册发现、配置管理等场景。</p><p>该新闻在国内发酵后，有人在 Twitter上咨询了HashCorp 公司的创始人，得到的回复是影响的软件仅限于 Vault 这款加密软件，目前 HashCorp 公司的官方网站上已经更新了相关的条款，明确了受影响的产品仅限 Vault 这一款产品。</p><h2 id="Consul-开源版是否收到影响？"><a href="#Consul-开源版是否收到影响？" class="headerlink" title="Consul 开源版是否收到影响？"></a>Consul 开源版是否收到影响？</h2><p>上面的条款里只提到了商业软件，那么开源的 Consul 是否受到影响呢？在 Github 的 Consul 仓库上，可以得知项目的 license 是 <code>Mozilla Public License 2.0</code> ，这款许可证在 Apache 官网上是 <code>Category B</code> , 属于 <code>Weak Copy Left</code> 许可，那么它有哪些特点呢？</p><p><img src="https://kirito.iocoder.cn/1590754178611.png" alt="License"></p><ol><li>任何可以使用，复制，修改，重新分发该代码，包括商业目的使用。</li><li>如果修改了 MPL 协议许可下的源码，再重新发布这部分源码的话，必须保留原来 MPL 许可证不得更换。</li><li>如果基于该项目衍生出更大的项目，那么这部分工作可以使用新许可证的方式进行分发，只要没有修改原来 MPL 许可下的代码。（这也是为什么 Apache 项目的分发的源码中可以包含 MPL 协议下二进制文件的原因）</li></ol><p>可以看到，MPL 通常被认为是介于 Apache License 和 GPL/LGPL 之间的一个折中方案。相对于 Apache License，MPL 2.0 要求修改了源码必须保持相同协议；相对于 GPL/LGPL, MPL 2.0 可以商用，同时衍生的作品在一定条件下也可以更换许可证类型。</p><p>总体来看的话，开源版 Consul 无论是私用还是商用都是不受限制的。但这也可能是一个警钟，如果对 Consul 还是有所顾忌的话，如何替代掉它呢？</p><p>在微服务领域，Consul 主要被用来做充当注册中心和配置中心，例如 Dubbo 和 SpringCloud 都有对应的支持。本文便以这个事为一个引子，介绍如何平滑地迁移 Dubbo 服务，达到替换注册中心的效果。</p><a id="more"></a><h2 id="平滑迁移服务的定义和意义"><a href="#平滑迁移服务的定义和意义" class="headerlink" title="平滑迁移服务的定义和意义"></a>平滑迁移服务的定义和意义</h2><p>如果 Dubbo 应用已经部署到生产环境并处于正常运行状态中，此时想将应用的注册中心替换，那么在迁移过程中，保证业务的平稳运行不中断一定是第一要义。我们将保证应用运行不中断，并最终达成注册中心替换的过程称为平滑迁移。可以类比为给飞行中的飞机替换引擎，在项目升级、框架调整等很多时候，现状和终态之间往往都有一个过度方案。</p><ul><li><p>平滑迁移可以避免终态方案一次性上线后出现和原有方案的不兼容性，规避了整体回归的风险</p></li><li><p>没有哪个互联网公司可以承担的起：“自 xx 至 xx，系统维护一小时，期间服务将无法提供，请广大用户谅解” 这种停机升级方案。</p></li></ul><h2 id="平滑迁移过程"><a href="#平滑迁移过程" class="headerlink" title="平滑迁移过程"></a>平滑迁移过程</h2><p>说到注册中心迁移，可能很多人第一时间都能想到双注册双订阅这种方案</p><blockquote><p>双注册和双订阅迁移方案是指在应用迁移时同时接入两个注册中心（原有注册中心和新注册中心）以保证已迁移的应用和未迁移的应用之间的相互调用。</p></blockquote><p>以 Consul 迁移到 Nacos 为例：</p><p>在迁移态下，一共有两种应用类型：未迁移应用，迁移中应用。我们所说的双注册双订阅都是指的【迁移中应用】。明白下面几个点，平滑迁移的过程一下子就清晰了：</p><ul><li>【未迁移应用】不做任何改动</li><li>为了让【未迁移应用】调用到【迁移中应用】，要求【迁移中应用】不仅要将数据写到 Nacos，还要写回旧的 Consul，这是双注册</li><li><p>为了让【迁移中应用】调用到【未迁移应用】，要求【迁移中应用】不仅要订阅 Nacos 的数据，还要监听旧的 Consul，这是双订阅</p></li><li><p>当所有应用变成【迁移中应用】时，旧的 Consul 就可以光荣下岗了，至此平滑迁移完成。</p></li></ul><p>在这个过程中，还可以灵活的变换一些规则，例如在迁移中后期，大部分应用在 Nacos 中已经有服务了，可以切换双订阅为单订阅，以验证迁移情况。并且在真实场景下，还会并存配置中心、元数据中心的迁移，过程会更加复杂。</p><h2 id="Dubbo-平滑迁移方案-–-多注册中心"><a href="#Dubbo-平滑迁移方案-–-多注册中心" class="headerlink" title="Dubbo 平滑迁移方案 – 多注册中心"></a>Dubbo 平滑迁移方案 – 多注册中心</h2><p>Dubbo 多注册中心配置文档地址：<a href="http://dubbo.apache.org/zh-cn/docs/user/demos/multi-registry.html" target="_blank" rel="noopener">http://dubbo.apache.org/zh-cn/docs/user/demos/multi-registry.html</a></p><p>本文的完整代码示例将会在文末提供，其中 Consul 注册中心搭建在本地，而 Nacos 注册中心使用的是阿里云的云产品：微服务引擎 MSE，其可以提供托管的 Nacos/Zookeeper/Eureka 等集群。</p><p>Dubbo 支持多注册中心的配置，这就为我们平滑迁移提供了很多的便利性。在使用 dubbo-spring-boot-starter 时，只需要增加如下的配置，即可配置多注册中心：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">dubbo.registries.first.protocol=consul</span><br><span class="line">dubbo.registries.first.address=localhost:8500</span><br><span class="line"></span><br><span class="line">dubbo.registries.second.protocol=nacos</span><br><span class="line">dubbo.registries.second.address=mse-kirito-p.nacos-ans.mse.aliyuncs.com:8848</span><br></pre></td></tr></table></figure><p>在 Consul 控制台可以看到服务已经注册成功：</p><p><img src="https://kirito.iocoder.cn/222.png" alt="Consul"></p><p>在 MSE 控制台可以看到 Nacos 服务也已经注册成功</p><p><img src="https://kirito.iocoder.cn/111.png" alt="MSE Nacos"></p><p>并且，服务调用一切正常。你可能回想：前面讲了一堆，你告诉我改了两行配置就是平滑迁移了？我还是得好好纠正下这种想法，改代码从来都是最轻松的事，难的是在迁移中，时刻观察业务状况，确保服务不因为迁移有损。除此之外，还需要注意的是，Dubbo 自带的多注册中心方案因为框架实现的问题，存在一定的缺陷。</p><h2 id="Dubbo-多注册中心的缺陷"><a href="#Dubbo-多注册中心的缺陷" class="headerlink" title="Dubbo 多注册中心的缺陷"></a>Dubbo 多注册中心的缺陷</h2><p>在 Dubbo 的实现中，多个注册中心的地址是隔离的，地址不会融合。也就是说，当消费者如下配置后：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">dubbo.registries.first.protocol=consul</span><br><span class="line">dubbo.registries.first.address=localhost:8500</span><br><span class="line"></span><br><span class="line">dubbo.registries.second.protocol=nacos</span><br><span class="line">dubbo.registries.second.address=mse-kirito-p.nacos-ans.mse.aliyuncs.com:8848</span><br></pre></td></tr></table></figure><p>会永远优先从 Consul 中读取服务地址，除非 Consul 中没有服务，才会尝试从 Nacos 中读取，顺序取决于配置文件中注册中心声明的先后。这可能不符合大多数人对多注册中心的直观认知，但没办法，Dubbo 就是这么设计的，我也尝试猜想了几个这么设计的可能性：</p><ul><li>多个注册中心没有感知到对方存在的必要，所以只能串行读取多个注册中心</li><li>Dubbo 本身模型不支持注册中心聚合，除非专门搞一个 AggregationRegistry 代理多个注册中心实现</li><li>多个注册地址的 equals 方案难以确定，官方没有给出契约规范，即 ip 和 port 相同就可以认为同一个地址吗？</li><li>Dubbo 的多注册中心的设计并不只是为了适配平滑迁移方案，其他场景可能恰恰希望使用这种串行读取的策略</li></ul><p>为了让读者有一个直观的感受，我用文末的 demo 进行了测试，让服务提供者 A1（端口号 12346） 只注册到 Nacos，服务提供者 A2（端口号为 12345） 只注册到 Consul，消费者 B 双订阅 Nacos 和 Consul。如下图所示，在测试初期，可以发现，稳定调用到 A1；期间，我手动 kill 了 A1，图中也清晰地打印出了一条地址下线通知，之后稳定调用到 A2。</p><p><img src="https://kirito.iocoder.cn/image-20200531012739193.png" alt="multi-registry"></p><p>这样的缺陷，会导致我们在平滑迁移过程中无法对未迁移应用和迁移中应用进行充分的测试。</p><h2 id="Dubbo-平滑迁移方案-–-注册中心聚合"><a href="#Dubbo-平滑迁移方案-–-注册中心聚合" class="headerlink" title="Dubbo 平滑迁移方案 – 注册中心聚合"></a>Dubbo 平滑迁移方案 – 注册中心聚合</h2><p>注册中心聚合这个词其实是我自己想的，因为 Dubbo 官方文档并没有直接给出这种方案，而是由阿里云的微服务商业化 EDAS 团队提供的开源实现（ps，没错，就是我所在的团队啦）。其基本思路就是前文提到的，聚合多个注册中心的地址。使用方式也同样简单</p><p>引入依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.edas<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>edas-dubbo-migration-bom<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.6.5.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>增加配置：</p><p>在 <code>application.properties</code> 中添加注册中心的地址。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dubbo.registry.address = edas-migration://30.5.124.15:9999?service-registry=consul://localhost:8500,nacos://mse-kirito-p.nacos-ans.mse.aliyuncs.com:8848&amp;reference-registry=consul://localhost:8500,nacos://mse-kirito-p.nacos-ans.mse.aliyuncs.com:8848</span><br></pre></td></tr></table></figure><p><strong>说明</strong> 如果是非 Spring Boot 应用，在 dubbo.properties 或者对应的 Spring 配置文件中配置。</p><ul><li><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">edas-migration://30.5.124.15:9999</span><br></pre></td></tr></table></figure><p>多注册中心的头部信息。可以不做更改，ip 和 port 可以任意填写，主要是为了兼容 Dubbo 对 ip 和 port 的校验。启动时，如果日志级别是 WARN 及以下，可能会抛一个 WARN 的日志，可以忽略。</p></li><li><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">service-registry</span><br></pre></td></tr></table></figure><p>服务注册的注册中心地址。写入多个注册中心地址。每个注册中心都是标准的 Dubbo 注册中心格式；多个用<code>,</code>分隔。</p></li><li><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">reference-registry</span><br></pre></td></tr></table></figure><p>服务订阅的注册中心地址。每个注册中心都是标准的 Dubbo 注册中心格式；多个用<code>,</code>分隔。</p></li></ul><p>验证该方案：</p><p><img src="https://kirito.iocoder.cn/image-20200531015410936.png" alt="migration"></p><p>已经变成了随机调用，解决了多注册中心的缺陷。</p><p>迁移完成后，建议删除原注册中心的配置和迁移过程专用的依赖<code>edas-dubbo-migration-bom</code>，在业务量较小的时间分批重启应用。<code>edas-dubbo-migration-bom</code> 是一个迁移专用的依赖，虽然长期使用对您业务的稳定性没有影响，但其并不会跟随 Dubbo 的版本进行升级，为避免今后框架升级过程中出现兼容问题，推荐您在迁移完毕后清理掉，然后在业务量较小的时间分批重启应用。</p><blockquote><p>说明：edas-dubbo-migration-bom 目前的 release 版本只支持 Dubbo 2.6，我在文末的代码中提供了 2.7 的支持，预计很快两个版本都会贡献给 Dubbo 开源社区。</p></blockquote><h2 id="彩蛋：阿里云微服务引擎-MSE-重磅升级，上线微服务治理中心"><a href="#彩蛋：阿里云微服务引擎-MSE-重磅升级，上线微服务治理中心" class="headerlink" title="彩蛋：阿里云微服务引擎 MSE 重磅升级，上线微服务治理中心"></a>彩蛋：阿里云微服务引擎 MSE 重磅升级，上线微服务治理中心</h2><p>微服务治理中心是一个面向开源微服务框架微服务中心，通过 Java Agent 技术使得您的应用无需修改任何代码和配置，即可享有阿里云提供的微服务治理能力。 已经上线的功能包含 服务查询、无损下线、服务鉴权、离群实例摘除、标签路由。</p><p>微服务治理中心具有如下优势：功能强大，覆盖和增强了开源的治理功能，还提供差异化的功能。零成本接入，支持近五年的 Spring Cloud 和 Dubbo 版本，无需修改任何代码和配置。易被集成，阿里云容器服务用户只需在应用市场安装 微服务中心对应的 pilot ，并修改部署时的配置即可接入。</p><p>微服务中心尤其适合以下场景</p><ul><li>解决应用发布时影响业务的问题。如果您的应用在发布新版本的时候，此应用的服务消费者仍旧调用已经下线的节点，出现业务有损，数据不一致的情况。这时候您需要使用 微服务治理中心，微服务治理中心提供的无损下线功能能够实现服务消费者及时感知服务提供者下线情况，保持业务连续无损。容器服务 K8s 集群的应用在接入 微服务治理中心后，您无需再额外对应用进行任何配置、也无需在 MSC 控制台进行任何操作，即可实现 Dubbo 和 Spring Cloud 流量的无损下线。</li><li>满足应用调用中权限控制的需求。当您的某个微服务应用有权限控制要求，不希望其它所有应用都能调用。比如优惠券部门的优惠券查询接口是默认内部的部门都是可以调用的，但是优惠券发放接口只允许特定的部门的应用才可以调用。这时候您需要使用 微服务治理中心，微服务治理中心提供的服务鉴权功能，既能够对整个应用做一些权限控制，也能对应用中的某个接口和 URL 进行权限控制，满足您不同场景下的权限控制需求</li><li>解决不健康实例影响业务对问题，当节点出现 Full GC、网络分区、机器异常等问题时，这种情况下会导致调用此应用的流量出现异常、影响业务。但是运维人员又很难及时发现问题，且无法判断应该采取何种措施，如重启或者单纯地等待应用恢复。这时候您需要使用微服务治理中心，微服务治理中心 提供的离群实例摘除功能，能够根据您配置的规则自动摘服务调用列表中不健康的应用实例，以免异常的节点影响您的业务。同时还能自动地探测实例是否恢复并恢复流量，以及将实例异常信息触发监控报警，保护您的业务，提升稳定性。</li></ul><p><img src="https://kirito.iocoder.cn/1590756935648.png" alt="MSC"></p><h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><p>本文测试代码地址：<a href="https://github.com/lexburner/dubbo-migration" target="_blank" rel="noopener">https://github.com/lexburner/dubbo-migration</a></p><p><img src="https://www.cnkirito.moe/css/images/wechat_public.jpg" alt="img"></p><p><em>「技术分享」<strong>某种程度上，是让作者和读者，不那么孤独的东西。</strong>欢迎关注我的微信公众号：<strong>「</strong>Kirito的技术分享」</em></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;近日，有报道称在 HashCorp 的商业软件试用协议上发现，旗下所有商业产品禁止在中国境内使用、部署、安装，这其中就包含了 Terraform, Consul, Vagrant 等众多知名软件，其中 Consul 是一个在微服务领域的开源软件，可以用于做注册发现、配置管理等场景。&lt;/p&gt;
&lt;p&gt;该新闻在国内发酵后，有人在 Twitter上咨询了HashCorp 公司的创始人，得到的回复是影响的软件仅限于 Vault 这款加密软件，目前 HashCorp 公司的官方网站上已经更新了相关的条款，明确了受影响的产品仅限 Vault 这一款产品。&lt;/p&gt;
&lt;h2 id=&quot;Consul-开源版是否收到影响？&quot;&gt;&lt;a href=&quot;#Consul-开源版是否收到影响？&quot; class=&quot;headerlink&quot; title=&quot;Consul 开源版是否收到影响？&quot;&gt;&lt;/a&gt;Consul 开源版是否收到影响？&lt;/h2&gt;&lt;p&gt;上面的条款里只提到了商业软件，那么开源的 Consul 是否受到影响呢？在 Github 的 Consul 仓库上，可以得知项目的 license 是 &lt;code&gt;Mozilla Public License 2.0&lt;/code&gt; ，这款许可证在 Apache 官网上是 &lt;code&gt;Category B&lt;/code&gt; , 属于 &lt;code&gt;Weak Copy Left&lt;/code&gt; 许可，那么它有哪些特点呢？&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://kirito.iocoder.cn/1590754178611.png&quot; alt=&quot;License&quot;&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;任何可以使用，复制，修改，重新分发该代码，包括商业目的使用。&lt;/li&gt;
&lt;li&gt;如果修改了 MPL 协议许可下的源码，再重新发布这部分源码的话，必须保留原来 MPL 许可证不得更换。&lt;/li&gt;
&lt;li&gt;如果基于该项目衍生出更大的项目，那么这部分工作可以使用新许可证的方式进行分发，只要没有修改原来 MPL 许可下的代码。（这也是为什么 Apache 项目的分发的源码中可以包含 MPL 协议下二进制文件的原因）&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;可以看到，MPL 通常被认为是介于 Apache License 和 GPL/LGPL 之间的一个折中方案。相对于 Apache License，MPL 2.0 要求修改了源码必须保持相同协议；相对于 GPL/LGPL, MPL 2.0 可以商用，同时衍生的作品在一定条件下也可以更换许可证类型。&lt;/p&gt;
&lt;p&gt;总体来看的话，开源版 Consul 无论是私用还是商用都是不受限制的。但这也可能是一个警钟，如果对 Consul 还是有所顾忌的话，如何替代掉它呢？&lt;/p&gt;
&lt;p&gt;在微服务领域，Consul 主要被用来做充当注册中心和配置中心，例如 Dubbo 和 SpringCloud 都有对应的支持。本文便以这个事为一个引子，介绍如何平滑地迁移 Dubbo 服务，达到替换注册中心的效果。&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="Dubbo" scheme="https://lexburner.github.io/tags/Dubbo/"/>
    
  </entry>
  
  <entry>
    <title>Arthas | 追踪线上耗时方法</title>
    <link href="https://lexburner.github.io/arthas-trace/"/>
    <id>https://lexburner.github.io/arthas-trace/</id>
    <published>2020-04-09T12:49:30.000Z</published>
    <updated>2020-04-09T13:03:43.410Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本文是 Arthas 系列文章的第三篇。</p><p>本文主要介绍 <code>trace</code> 指令，用于定位两种类型的问题：</p><ol><li>线上服务 RT 比较高，但没有打印日志，无法确定具体是哪个方法比较耗时</li><li>线上服务出现异常，需要追踪到方法的堆栈</li></ol><a id="more"></a><h2 id="模拟线上耗时方法"><a href="#模拟线上耗时方法" class="headerlink" title="模拟线上耗时方法"></a>模拟线上耗时方法</h2><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文以 Dubbo 线程池满异常作为引子，介绍了线程类问题该如何分析，以及如何通过 Arthas 快速诊断线程问题。有了 Arthas，基本不再需要 jstack 将 16 进制转来转去了，大大提升了诊断速度。</p><h2 id="Arthas-钉钉交流群"><a href="#Arthas-钉钉交流群" class="headerlink" title="Arthas 钉钉交流群"></a>Arthas 钉钉交流群</h2><p><img src="https://alibaba.github.io/arthas/_images/dingding_qr.jpg" alt="_images/dingding_qr.jpg"></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;本文是 Arthas 系列文章的第三篇。&lt;/p&gt;
&lt;p&gt;本文主要介绍 &lt;code&gt;trace&lt;/code&gt; 指令，用于定位两种类型的问题：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;线上服务 RT 比较高，但没有打印日志，无法确定具体是哪个方法比较耗时&lt;/li&gt;
&lt;li&gt;线上服务出现异常，需要追踪到方法的堆栈&lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
      <category term="Arthas" scheme="https://lexburner.github.io/categories/Arthas/"/>
    
    
      <category term="Arthas" scheme="https://lexburner.github.io/tags/Arthas/"/>
    
  </entry>
  
  <entry>
    <title>Arthas | 定位线上 Dubbo 线程池满异常</title>
    <link href="https://lexburner.github.io/arthas-thread/"/>
    <id>https://lexburner.github.io/arthas-thread/</id>
    <published>2020-02-16T11:30:35.000Z</published>
    <updated>2020-02-16T14:41:27.838Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本文是 Arthas 系列文章的第二篇。</p><p>Dubbo 线程池满异常应该是大多数 Dubbo 用户都遇到过的一个问题，本文以 Arthas 3.1.7 版本为例，介绍如何针对该异常进行诊断，主要使用到 <code>dashboard</code>/<code>thread</code> 两个指令。</p><a id="more"></a><h2 id="Dubbo-线程池满异常介绍"><a href="#Dubbo-线程池满异常介绍" class="headerlink" title="Dubbo 线程池满异常介绍"></a>Dubbo 线程池满异常介绍</h2><p>理解线程池满异常需要首先了解 Dubbo 线程模型，官方文档：<a href="http://dubbo.apache.org/zh-cn/docs/user/demos/thread-model.html。简单概括下" target="_blank" rel="noopener">http://dubbo.apache.org/zh-cn/docs/user/demos/thread-model.html。简单概括下</a> Dubbo 默认的线程模型：Dubbo 服务端每次接收到一个 Dubbo 请求，便交给一个线程池处理，该线程池默认有 200 个线程，如果 200 个线程都不处于空闲状态，则</p><p>客户端会报出如下异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Caused by: java.util.concurrent.ExecutionException: org.apache.dubbo.remoting.RemotingException: <span class="function">Server <span class="title">side</span><span class="params">(<span class="number">192.168</span><span class="number">.1</span><span class="number">.101</span>,<span class="number">20880</span>)</span> threadpool is exhausted ...</span></span><br></pre></td></tr></table></figure><p>服务端会打印 WARN 级别的日志：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[DUBBO] Thread pool is EXHAUSTED!</span><br></pre></td></tr></table></figure><p>引发该异常的原因主要有以下几点：</p><ul><li>客户端/服务端超时时间设置不合理，导致请求无限等待，耗尽了线程数</li><li>客户端请求量过大，服务端无法及时处理，耗尽了线程数</li><li>服务端由于 fullgc 等原因导致处理请求较慢，耗尽了线程数</li><li>服务端由于数据库、Redis、网络 IO 阻塞问题，耗尽了线程数</li><li>…</li></ul><p>原因可能很多，但纠其根本，都是因为业务上出了问题，导致 Dubbo 线程池资源耗尽了。所以出现该问题，首先要做的是：</p><ul><li>排查业务异常</li></ul><p>紧接着针对自己的业务场景对 Dubbo 进行调优：</p><ul><li>调整 Provider 端的 dubbo.provider.threads 参数大小，默认 200，可以适当提高。多大算合适？至少 700 不算大；不建议调的太小，容易出现上述问题</li><li>调整 Consumer 端的 dubbo.consumer.actives 参数，控制消费者调用的速率。这个实践中很少使用，仅仅一提</li><li>客户端限流</li><li>服务端扩容</li><li>Dubbo 目前不支持给某个 service 单独配置一个隔离的线程池，用于保护服务，可能在以后的版本中会增加这个特性</li></ul><p>另外，不止 Dubbo 如此设计线程模型，绝大多数服务治理框架、 HTTP 服务器都有业务线程池的概念，所以理论上它们都会有线程池满异常的可能，解决方案也类似。</p><p>那竟然问题都解释清楚了，我们还需要排查什么呢？一般在线上，有很多运行中的服务，这些服务都是共享一个 Dubbo 服务端线程池，可能因为某个服务的问题，导致整个应用被拖垮，所以需要排查是不是集中出现在某个服务上，再针对排查这个服务的业务逻辑；需要定位到线程堆栈，揪出导致线程池满的元凶。</p><p>定位该问题，我的习惯一般是使用 Arthas 的 <code>dashboard</code> 和 <code>thread</code> 命令，而在介绍这两个命令之前，我们先人为的构造一个 Dubbo 线程池满异常的例子。</p><h2 id="复现-Dubbo-线程池满异常"><a href="#复现-Dubbo-线程池满异常" class="headerlink" title="复现 Dubbo 线程池满异常"></a>复现 Dubbo 线程池满异常</h2><h3 id="配置服务端线程池大小"><a href="#配置服务端线程池大小" class="headerlink" title="配置服务端线程池大小"></a>配置服务端线程池大小</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dubbo.protocol.threads=10</span><br></pre></td></tr></table></figure><p>默认大小是 200，不利于重现该异常</p><h3 id="模拟服务端阻塞"><a href="#模拟服务端阻塞" class="headerlink" title="模拟服务端阻塞"></a>模拟服务端阻塞</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Service</span>(version = <span class="string">"1.0.0"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoServiceImpl</span> <span class="keyword">implements</span> <span class="title">DemoService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sayHello</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        sleep();</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello "</span> + name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">sleep</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>sleep</code> 方法模拟了一个耗时操作，主要是为了让服务端线程池耗尽。</p><h3 id="客户端多线程访问"><a href="#客户端多线程访问" class="headerlink" title="客户端多线程访问"></a>客户端多线程访问</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>)&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                demoService.sayHello(<span class="string">"Provider"</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;).start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="问题复现"><a href="#问题复现" class="headerlink" title="问题复现"></a>问题复现</h3><p>客户端</p><p><img src="https://kirito.iocoder.cn/image-20200216211204396.png" alt="客户端异常"></p><p>服务端</p><p><img src="https://kirito.iocoder.cn/image-20200216211047140.png" alt="服务端异常"></p><p>问题得以复现，保留该现场，并假设我们并不知晓 sleep 的耗时逻辑，使用 Arthas 来进行排查。</p><h2 id="dashboard-命令介绍"><a href="#dashboard-命令介绍" class="headerlink" title="dashboard 命令介绍"></a>dashboard 命令介绍</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ dashboard</span><br></pre></td></tr></table></figure><p>执行效果</p><p><img src="https://kirito.iocoder.cn/image-20200216211837229.png" alt="dashboard"></p><p>可以看到如上所示的面板，显示了一些系统的运行信息，这里主要关注 THREAD 面板，介绍一下各列的含义：</p><ul><li>ID: Java 级别的线程 ID，注意这个 ID 不能跟 jstack 中的 nativeID 一一对应</li><li>NAME: 线程名</li><li>GROUP: 线程组名</li><li>PRIORITY: 线程优先级, 1~10 之间的数字，越大表示优先级越高</li><li>STATE: 线程的状态</li><li>CPU%: 线程消耗的 CPU 占比，采样 100ms，将所有线程在这 100ms 内的 CPU 使用量求和，再算出每个线程的 CPU 使用占比。</li><li>TIME: 线程运行总时间，数据格式为<code>分：秒</code></li><li>INTERRUPTED: 线程当前的中断位状态</li><li>DAEMON: 是否是 daemon 线程</li></ul><p>在空闲状态下线程应该是处于 WAITING 状态，而因为 sleep 的缘故，现在所有的线程均处于 TIME_WAITING 状态，导致后来的请求被处理时，抛出了线程池满的异常。</p><p>在实际排查中，需要抽查一定数量的 Dubbo 线程，记录他们的线程编号，看看它们到底在处理什么服务请求。使用如下命令可以根据线程池名筛选出 Dubbo 服务端线程：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dashboard | grep &quot;DubboServerHandler&quot;</span><br></pre></td></tr></table></figure><h2 id="thread-命令介绍"><a href="#thread-命令介绍" class="headerlink" title="thread 命令介绍"></a>thread 命令介绍</h2><p>使用 <code>dashboard</code> 筛选出个别线程 id 后，它的使命就完成了，剩下的操作交给 <code>thread</code> 命令来完成。其实，<code>dashboard</code> 中的 <code>thread</code> 模块，就是整合了 <code>thread</code> 命令，但是 <code>dashboard</code> 还可以观察内存和 GC 状态，视角更加全面，所以我个人建议，在排查问题时，先使用 <code>dashboard</code> 纵观全局信息。</p><p>thread 使用示例：</p><ol><li><p>查看当前最忙的前 n 个线程</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> thread -n 3</span><br></pre></td></tr></table></figure><p><img src="https://kirito.iocoder.cn/image-20200216220111905.png" alt="thread -n"></p></li><li><p>显示所有线程信息</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> thread</span><br></pre></td></tr></table></figure><p>和 <code>dashboard</code> 中显示一致</p></li><li><p>显示当前阻塞其他线程的线程</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> thread -b</span><br><span class="line">No most blocking thread found!</span><br><span class="line">Affect(row-cnt:0) cost in 22 ms.</span><br></pre></td></tr></table></figure><p>这个命令还有待完善，目前只支持找出 synchronized 关键字阻塞住的线程， 如果是 <code>java.util.concurrent.Lock</code>， 目前还不支持</p></li><li><p>显示指定状态的线程</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> thread --state TIMED_WAITING</span><br></pre></td></tr></table></figure><p><img src="https://kirito.iocoder.cn/image-20200216215300465.png" alt="thread --state"></p><p>线程状态一共有 [RUNNABLE, BLOCKED, WAITING, TIMED_WAITING, NEW, TERMINATED] 6 种</p></li><li><p>查看指定线程的运行堆栈</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> thread 46</span><br></pre></td></tr></table></figure><p><img src="https://kirito.iocoder.cn/image-20200216220024931.png" alt="thread ${thread_id}"></p></li></ol><p>介绍了几种常见的用法，在实际排查中需要针对我们的现场做针对性的分析，也同时考察了我们对线程状态的了解程度。我这里列举了几种常见的线程状态：</p><h3 id="初始-NEW"><a href="#初始-NEW" class="headerlink" title="初始(NEW)"></a>初始(NEW)</h3><p>新创建了一个线程对象，但还没有调用 start() 方法。</p><h3 id="运行-RUNNABLE"><a href="#运行-RUNNABLE" class="headerlink" title="运行(RUNNABLE)"></a>运行(RUNNABLE)</h3><p>Java 线程将就绪（ready）和运行中（running）两种状态笼统的称为“运行”</p><h3 id="阻塞-BLOCKED"><a href="#阻塞-BLOCKED" class="headerlink" title="阻塞(BLOCKED)"></a>阻塞(BLOCKED)</h3><p>线程阻塞于锁</p><h3 id="等待-WAITING"><a href="#等待-WAITING" class="headerlink" title="等待(WAITING)"></a>等待(WAITING)</h3><p>进入该状态的线程需要等待其他线程做出一些特定动作（通知或中断）</p><ol><li>Object#wait() 且不加超时参数</li><li>Thread#join() 且不加超时参数</li><li>LockSupport#park()</li></ol><h3 id="超时等待-TIMED-WAITING"><a href="#超时等待-TIMED-WAITING" class="headerlink" title="超时等待(TIMED_WAITING)"></a>超时等待(TIMED_WAITING)</h3><p>该状态不同于 WAITING，它可以在指定的时间后自行返回</p><ol><li>Thread#sleep()</li><li>Object#wait() 且加了超时参数</li><li>Thread#join() 且加了超时参数</li><li>LockSupport#parkNanos()</li><li>LockSupport#parkUntil()</li></ol><h3 id="终止-TERMINATED"><a href="#终止-TERMINATED" class="headerlink" title="终止(TERMINATED)"></a>终止(TERMINATED)</h3><p>标识线程执行完毕</p><h3 id="状态流转图"><a href="#状态流转图" class="headerlink" title="状态流转图"></a>状态流转图</h3><p><img src="https://kirito.iocoder.cn/2018070117435683.jpeg" alt="线程状态"></p><h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><p>分析线程池满异常并没有通法，需要灵活变通，我们对下面这些 case 一个个分析：</p><ul><li>阻塞类问题。例如数据库连接不上导致卡死，运行中的线程基本都应该处于 BLOCKED 或者 TIMED_WAITING 状态，我们可以借助 <code>thread --state</code> 定位到</li><li>繁忙类问题。例如 CPU 密集型运算，运行中的线程基本都处于 RUNNABLE 状态，可以借助于 <code>thread -n</code> 来定位出最繁忙的线程</li><li>GC 类问题。很多外部因素会导致该异常，例如 GC 就是其中一个因素，这里就不能仅仅借助于 <code>thread</code> 命令来排查了。</li><li>定点爆破。还记得在前面我们通过 grep 筛选出了一批 Dubbo 线程，可以通过 <code>thread ${thread_id}</code> 定向的查看堆栈，如果统计到大量的堆栈都是一个服务时，基本可以断定是该服务出了问题，至于说是该服务请求量突然激增，还是该服务依赖的某个下游服务突然出了问题，还是该服务访问的数据库断了，那就得根据堆栈去判断了。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文以 Dubbo 线程池满异常作为引子，介绍了线程类问题该如何分析，以及如何通过 Arthas 快速诊断线程问题。有了 Arthas，基本不再需要 jstack 将 16 进制转来转去了，大大提升了诊断速度。</p><h2 id="Arthas-钉钉交流群"><a href="#Arthas-钉钉交流群" class="headerlink" title="Arthas 钉钉交流群"></a>Arthas 钉钉交流群</h2><p><img src="https://alibaba.github.io/arthas/_images/dingding_qr.jpg" alt="_images/dingding_qr.jpg"></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;本文是 Arthas 系列文章的第二篇。&lt;/p&gt;
&lt;p&gt;Dubbo 线程池满异常应该是大多数 Dubbo 用户都遇到过的一个问题，本文以 Arthas 3.1.7 版本为例，介绍如何针对该异常进行诊断，主要使用到 &lt;code&gt;dashboard&lt;/code&gt;/&lt;code&gt;thread&lt;/code&gt; 两个指令。&lt;/p&gt;
    
    </summary>
    
      <category term="Arthas" scheme="https://lexburner.github.io/categories/Arthas/"/>
    
    
      <category term="Arthas" scheme="https://lexburner.github.io/tags/Arthas/"/>
    
  </entry>
  
  <entry>
    <title>阿里巴巴中间件技术部春招开始啦</title>
    <link href="https://lexburner.github.io/middleware-hire/"/>
    <id>https://lexburner.github.io/middleware-hire/</id>
    <published>2020-02-10T10:34:44.000Z</published>
    <updated>2020-02-10T11:13:36.079Z</updated>
    
    <content type="html"><![CDATA[<p><strong>阿里巴巴中间件技术部 - aPaaS 产品团队春招意向沟通正式开始啦！三月前我们会完成和同学的意向沟通，三月初正式进入招聘流程。</strong></p><p><strong>简历投递：<a href="mailto:jingfeng.xjf@alibaba-inc.com" target="_blank" rel="noopener">jingfeng.xjf@alibaba-inc.com</a></strong></p><p><strong>意向沟通联系微信：xiayimiaoshenghua</strong></p><a id="more"></a><h2 id="为什么考虑加入我们？"><a href="#为什么考虑加入我们？" class="headerlink" title="为什么考虑加入我们？"></a>为什么考虑加入我们？</h2><h3 id="阿里最核心的技术部门之一"><a href="#阿里最核心的技术部门之一" class="headerlink" title="阿里最核心的技术部门之一"></a>阿里最核心的技术部门之一</h3><p>作为整个集团技术的“枢纽”，中间件技术产品向上支撑了淘宝、天猫、盒马、菜鸟、高德等几乎所有阿里巴巴集团的核心业务，向下整合存储、计算、网络等阿里云基础设施，是历年来阿里巴巴技术架构升级，618/双十一/双十二等大促活动的核心技术部门。业务体量大，技术挑战大，加入以后能力成长飞快。</p><h3 id="阿里业务增长最快的部门之一"><a href="#阿里业务增长最快的部门之一" class="headerlink" title="阿里业务增长最快的部门之一"></a>阿里业务增长最快的部门之一</h3><p>云计算是阿里巴巴业务增长最快的板块之一，中间件技术部隶属于阿里云-基础产品事业部，是阿里的明星团队之一。在这里你绝对不用担心沦为大公司的“螺丝钉” - 中间件这几年处于高速增长期，许多新的产品、新的项目等你来主导，在享受加入阿里集团带来的稳定性、专业性以及平台优势的同时，又能体会到加入初创公司的激情、高效与掌控力。</p><h3 id="阿里技术影响力最大的部门之一"><a href="#阿里技术影响力最大的部门之一" class="headerlink" title="阿里技术影响力最大的部门之一"></a>阿里技术影响力最大的部门之一</h3><p>这里诞生了RocketMQ，Spring Cloud Alibaba、Dubbo、Arthas、Nacos等开源项目，在国内的微服务领域具备深远的影响力。我们每年都会举办中间件技术挑战赛、邀请业界各路大牛切磋技艺；这里是阿里“技术中台”的发源地，我们服务了许多阿里以外的知名互联网企业，我们为许多行业龙头企业提供了微服务相关的最佳实践和解决方案，帮助许多传统企业完成了数字化转型。在这里你能够全面提升你的个人技术影响力、沟通能力和行业知名度。</p><h2 id="加入以后做哪些事？"><a href="#加入以后做哪些事？" class="headerlink" title="加入以后做哪些事？"></a>加入以后做哪些事？</h2><ol><li>微服务框架与架构治理方向 加入Apache ASF成员领军、多位Apache PMC坐镇的阿里巴巴中间件微服务团队，参与顶级开源项目的建设；深入阿里“技术中台“的腹地，作为核心技术骨干参与双十一大促保障，并将阿里多年双十一稳定性保障与架构治理的沉淀产品化，为中国乃至全球的中大型企业赋能。</li><li>分布式链路追踪与故障定位方向 参与建设中国乃至世界最大规模的分布式链路跟踪与监控系统，处理每天数十PB级的分布式调用轨迹数据，通过机器学习技术完成故障自动根因定位、故障自愈，探索最前沿的数据可视化技术。</li><li>分布式应用托管与Serverless技术方向 探索云计算领域最前沿的Serverless技术，结合阿里云容器团队、内核团队的诸多黑科技，颠覆传统应用托管的使用体验，打造极致弹性、超低成本的新一代云计算产品。</li></ol><h2 id="面向群体招聘群体是哪些？"><a href="#面向群体招聘群体是哪些？" class="headerlink" title="面向群体招聘群体是哪些？"></a>面向群体招聘群体是哪些？</h2><ul><li>2021年国内毕业的高校应届毕业生。</li><li>2020年海外毕业的高校应届毕业生。</li></ul><h2 id="岗位有哪些要求？"><a href="#岗位有哪些要求？" class="headerlink" title="岗位有哪些要求？"></a>岗位有哪些要求？</h2><p>熟悉Java、Golang或C++至少一门语言。有良好的计算机基础，基本功扎实：熟悉常用的算法、数据结构，熟悉操作系统、网络等基础知识。</p><p>加分项（任一即可）：</p><ul><li>有参与过开源技术项目经验，或有大赛获奖经验，或有优秀论文发表者。</li><li>熟悉微服务领域相关技术或产品，如：gRPC，Dubbo，Spring Cloud，RocketMQ等。</li><li>熟悉容器相关技术或产品，如Docker, Kubernetes, Istio/Envoy等。</li><li>熟悉监控领域相关技术或产品，如：Prometheus，Zipkin，OpenTracing，Zabbix等。</li><li>熟悉数据处理相关技术或产品，如：Flink，Spark，HBase，ElasticSearch等。</li><li>熟悉机器学习技术，有相关实战经验。</li></ul><p><strong>简历投递：<a href="mailto:jingfeng.xjf@alibaba-inc.com" target="_blank" rel="noopener">jingfeng.xjf@alibaba-inc.com</a></strong></p><p><strong>意向沟通联系微信：xiayimiaoshenghua</strong></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;阿里巴巴中间件技术部 - aPaaS 产品团队春招意向沟通正式开始啦！三月前我们会完成和同学的意向沟通，三月初正式进入招聘流程。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;简历投递：&lt;a href=&quot;mailto:jingfeng.xjf@alibaba-inc.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;jingfeng.xjf@alibaba-inc.com&lt;/a&gt;&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;意向沟通联系微信：xiayimiaoshenghua&lt;/strong&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="技术杂谈" scheme="https://lexburner.github.io/categories/%E6%8A%80%E6%9C%AF%E6%9D%82%E8%B0%88/"/>
    
    
      <category term="招聘" scheme="https://lexburner.github.io/tags/%E6%8B%9B%E8%81%98/"/>
    
  </entry>
  
  <entry>
    <title>Arthas | 热更新线上代码</title>
    <link href="https://lexburner.github.io/arthas-redefine/"/>
    <id>https://lexburner.github.io/arthas-redefine/</id>
    <published>2020-02-09T07:42:45.000Z</published>
    <updated>2020-02-16T14:36:29.857Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本文是我介绍 Arthas 系列文章的第一篇。</p><p>一般线上问题比开发环境的问题更难解决，一个主要的原因便在于开发态可以任意 debug 断点调试，而线上环境一般不允许远程调试，所以在实践中，我一般习惯用 Arthas 来定位线上的问题。</p><blockquote><p>Arthas 是阿里巴巴开源的 Java 应用诊断利器</p></blockquote><p><a href="https://alibaba.github.io/arthas/" target="_blank" rel="noopener">Arthas</a> 可以完成很多骚操作，今天给大家介绍的 Arthas 诊断技巧便是 – 热更新线上代码。在生产环境热更新代码，并不是很好的行为，可能会引发一些问题</p><ul><li>黑屏化的操作可能会导致误操作</li><li>不符合安全生产的规范，不满足可监控、可回滚、可降级</li></ul><p>但有时候也有一些场景可以考虑使用 Arthas 来热更，例如开发环境无法复现的问题、找到修复思路后临时验证等。</p><p>本文以 Arthas 3.1.7 版本为例，主要使用到 <code>jad</code>/<code>mc</code>/<code>redefine</code> 三个指令。</p><a id="more"></a><h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p>在 arthas-demo 示例中，一共有两个类，一个 HelloService 类，sayHello 方法负责不断的打印 <code>hello world</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"hello world"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>HelloService 用于模拟我们日常开发的一些业务 Service，另外还有一个 Main 函数，负责启动进程，并循环调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        HelloService helloService = <span class="keyword">new</span> HelloService();</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            helloService.sayHello();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h2><p>假设这段代码运行在线上，我们希望通过 Arthas 将 <code>hello world</code> 的输出更改为 <code>hello arthas</code>。 </p><p>Arthas 修改热更的逻辑主要分为三步：</p><ul><li>jad 命令反编译出内存中的字节码，生成 class 文件</li><li>修改代码，使用 mc 命令内存编译新的 class 文件</li><li>redefine 重新加载新的 class 文件</li></ul><p>从而达到热更新的效果</p><h2 id="jad-反编译"><a href="#jad-反编译" class="headerlink" title="jad 反编译"></a>jad 反编译</h2><p>当挂载上 Arthas 之后，执行</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> jad --source-only moe.cnkirito.arthas.demo.HelloService &gt; /tmp/HelloService.java</span><br></pre></td></tr></table></figure><p>将字节码文件输出到指定的位置，查看其内容，与示例中的源码内容一致：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Decompiled with CFR.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">package</span> moe.cnkirito.arthas.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.PrintStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"hello world"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>命令中 <code>--source-only</code> 的含义为，只输出源码部分，如果不加这个参数，在反编译出的内容头部会携带类加载器的信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ClassLoader:</span><br><span class="line">+-sun.misc.Launcher$AppClassLoader@<span class="number">18</span>b4aac2</span><br><span class="line">  +-sun.misc.Launcher$ExtClassLoader@<span class="number">20</span>d5ad12</span><br><span class="line"></span><br><span class="line">Location:</span><br><span class="line">/Users/xujingfeng/IdeaProjects/arthas-demo/target/classes/</span><br></pre></td></tr></table></figure><p>在服务器上可以直接使用 vi 等编辑器对源码进行编辑。将 <code>hello world</code> 改为 <code>hello arthas</code>，为下一步做准备。</p><h2 id="sc-查找类加载器"><a href="#sc-查找类加载器" class="headerlink" title="sc 查找类加载器"></a>sc 查找类加载器</h2><p>mc 命令编译文件需要传入该类对应类加载器的 hash 值，需要先使用 sc 命令查看 HelloService 的累加器信息</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> sc -d moe.cnkirito.arthas.demo.HelloService</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">class-info        moe.cnkirito.arthas.demo.HelloService</span><br><span class="line"> code-source       /Users/xujingfeng/IdeaProjects/arthas-demo/target/classes/</span><br><span class="line"> name              moe.cnkirito.arthas.demo.HelloService</span><br><span class="line"> isInterface       false</span><br><span class="line"> isAnnotation      false</span><br><span class="line"> isEnum            false</span><br><span class="line"> isAnonymousClass  false</span><br><span class="line"> isArray           false</span><br><span class="line"> isLocalClass      false</span><br><span class="line"> isMemberClass     false</span><br><span class="line"> isPrimitive       false</span><br><span class="line"> isSynthetic       false</span><br><span class="line"> simple-name       HelloService</span><br><span class="line"> modifier          public</span><br><span class="line"> annotation</span><br><span class="line"> interfaces</span><br><span class="line"> super-class       +-java.lang.Object</span><br><span class="line"> class-loader      +-sun.misc.Launcher$AppClassLoader@18b4aac2</span><br><span class="line">                     +-sun.misc.Launcher$ExtClassLoader@20d5ad12</span><br><span class="line"> classLoaderHash   18b4aac2</span><br></pre></td></tr></table></figure><p>最后一行 <code>classLoaderHash</code> 即为 HelloService 的类加载器 hash 值。</p><blockquote><p>Arthas 支持 grep，你也可以简化该操作为：</p><p>sc -d moe.cnkirito.arthas.demo.HelloService | grep classLoaderHash</p></blockquote><h2 id="mc-内存编译"><a href="#mc-内存编译" class="headerlink" title="mc 内存编译"></a>mc 内存编译</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> mc -c 18b4aac2 /tmp/HelloService.java -d /tmp</span><br><span class="line">Memory compiler output:</span><br><span class="line">/tmp/moe/cnkirito/arthas/demo/HelloService.class</span><br></pre></td></tr></table></figure><p>使用 <code>-c</code> 指定类加载器的 hash 值。编译完成后，/tmp 目录下会生成对应的 class 字节码文件</p><h2 id="redefine-热更新代码"><a href="#redefine-热更新代码" class="headerlink" title="redefine 热更新代码"></a>redefine 热更新代码</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> redefine /tmp/moe/cnkirito/arthas/demo/HelloService.class</span><br></pre></td></tr></table></figure><h2 id="检查结果"><a href="#检查结果" class="headerlink" title="检查结果"></a>检查结果</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">hello world</span><br><span class="line">hello world</span><br><span class="line">hello world</span><br><span class="line">hello world</span><br><span class="line">hello arthas</span><br><span class="line">hello arthas</span><br><span class="line">hello arthas</span><br><span class="line">hello arthas</span><br></pre></td></tr></table></figure><p>热更新成功</p><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="redefine-使用限制"><a href="#redefine-使用限制" class="headerlink" title="redefine 使用限制"></a>redefine 使用限制</h3><ul><li><p>不允许新增或者删除 field/method</p><p>会出现类似下面的提示</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">redefine error! java.lang.UnsupportedOperationException: class redefinition failed: attempted to change the schema (add/remove fields)</span><br></pre></td></tr></table></figure></li><li><p>运行中的方法不会立刻生效，会在下一次进入该方法时才能生效。</p><p>很好理解，并发问题</p></li></ul><h3 id="mc-常见问题"><a href="#mc-常见问题" class="headerlink" title="mc 常见问题"></a>mc 常见问题</h3><ul><li><p>mc 命令有可能失败</p><p>因为运行时环境和编译时环境的 JDK 可能有版本差异，mc 可能会失败。如果编译失败可以在本地编译好 <code>.class</code> 文件，再上传到服务器</p></li><li><p>当存在内部类时，一次会生成多个 class 文件</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Inner.test();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Inner</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"hello inner"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行 mc</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> mc -c 18b4aac2 /tmp/HelloService.java -d /tmp</span><br><span class="line">Memory compiler output:</span><br><span class="line">/tmp/moe/cnkirito/arthas/demo/HelloService$Inner.class</span><br><span class="line">/tmp/moe/cnkirito/arthas/demo/HelloService.class</span><br></pre></td></tr></table></figure><p>注意 redefine 时也可以同时传入多个入参</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> redefine /tmp/moe/cnkirito/arthas/demo/HelloService$Inner.class /tmp/moe/cnkirito/arthas/demo/HelloService.class</span><br><span class="line">redefine success, size: 2</span><br></pre></td></tr></table></figure></li></ul><h2 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h2><p><a href="https://blog.csdn.net/hengyunabc/article/details/87718469" target="_blank" rel="noopener">https://blog.csdn.net/hengyunabc/article/details/87718469</a></p><h2 id="Arthas-钉钉交流群"><a href="#Arthas-钉钉交流群" class="headerlink" title="Arthas 钉钉交流群"></a>Arthas 钉钉交流群</h2><p><img src="https://alibaba.github.io/arthas/_images/dingding_qr.jpg" alt="_images/dingding_qr.jpg"></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;本文是我介绍 Arthas 系列文章的第一篇。&lt;/p&gt;
&lt;p&gt;一般线上问题比开发环境的问题更难解决，一个主要的原因便在于开发态可以任意 debug 断点调试，而线上环境一般不允许远程调试，所以在实践中，我一般习惯用 Arthas 来定位线上的问题。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Arthas 是阿里巴巴开源的 Java 应用诊断利器&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;a href=&quot;https://alibaba.github.io/arthas/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Arthas&lt;/a&gt; 可以完成很多骚操作，今天给大家介绍的 Arthas 诊断技巧便是 – 热更新线上代码。在生产环境热更新代码，并不是很好的行为，可能会引发一些问题&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;黑屏化的操作可能会导致误操作&lt;/li&gt;
&lt;li&gt;不符合安全生产的规范，不满足可监控、可回滚、可降级&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但有时候也有一些场景可以考虑使用 Arthas 来热更，例如开发环境无法复现的问题、找到修复思路后临时验证等。&lt;/p&gt;
&lt;p&gt;本文以 Arthas 3.1.7 版本为例，主要使用到 &lt;code&gt;jad&lt;/code&gt;/&lt;code&gt;mc&lt;/code&gt;/&lt;code&gt;redefine&lt;/code&gt; 三个指令。&lt;/p&gt;
    
    </summary>
    
      <category term="Arthas" scheme="https://lexburner.github.io/categories/Arthas/"/>
    
    
      <category term="Arthas" scheme="https://lexburner.github.io/tags/Arthas/"/>
    
  </entry>
  
  <entry>
    <title>EDAS 微服务治理解密</title>
    <link href="https://lexburner.github.io/edas-microservice/"/>
    <id>https://lexburner.github.io/edas-microservice/</id>
    <published>2020-02-01T03:08:59.000Z</published>
    <updated>2020-02-01T09:43:19.911Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>2020 是多事的一年，新冠状性病毒的肆虐，其次是自己也生了一场病，希望随着天气暖和起来，一起都能变得更好。</p><p>前一段时间真的很忙，一直没有抽出时间，也没有什么思路给大家分享优质的文章，今天这篇文章很久之前就想写了，抓住这次假期的尾巴，总结一下我最近这一年的工作。</p><a id="more"></a><h2 id="EDAS-是什么"><a href="#EDAS-是什么" class="headerlink" title="EDAS 是什么"></a>EDAS 是什么</h2><p>有很多读者问我在阿里是做什么的，我一般会回答：“我在阿里主要负责 Dubbo、HSF、SpringCloud Alibaba 这几个微服务框架研发和商业化相关的工作”，这样回答，如果对方是 Java 开发，一般都能知道个大概。熟悉阿里云的朋友会了解到阿里云上有很多的 PaaS 产品，我主要就是负责开发 “企业级分布式应用服务 EDAS” 这款产品；不熟悉阿里云的朋友，会对阿里云上一堆产品、一堆名词感到奇怪，什么 EDAS、MSE、ACM、OAM… 我开始接触 EDAS 时，也是吐槽了一下这次名词，但随着逐渐了解，也就觉得这些云产品像 Redis、DB、MQ 这些东西一样亲切了。这篇文章将围绕 EDAS 这款阿里云云产品进行介绍。</p><blockquote><p>企业级分布式应用服务 EDAS（Enterprise Distributed Application Service）是一个应用托管和微服务管理的 PaaS 平台，提供应用开发、部署、监控、运维等全栈式解决方案，同时支持 Spring Cloud、Apache Dubbo（以下简称 Dubbo ）、HSF 等微服务运行环境，助力您的各类应用轻松上云。</p></blockquote><p>可以从简介中提取出 EDAS 的核心能力：应用开发、应用部署、应用监控、应用运维。我这一篇文章自然是没办法介绍整个 PaaS 平台的能力，而且说实话，应用部署和应用监控等等能力也都是我同事们开发的，我真的一无所知，想介绍都难，所以在设计到细节时，我会主要介绍 EDAS 和微服务治理相关的那部分能力。</p><h2 id="为什么用-EDAS"><a href="#为什么用-EDAS" class="headerlink" title="为什么用 EDAS"></a>为什么用 EDAS</h2><p><img src="https://kirito.iocoder.cn/image-20200201125734612.png" alt="云计算"></p><p>上图中，我将微服务框架的不同部署方式和云计算进行了绑定，以方便下面的介绍。</p><p>“种子”用户最容易理解了，例如使用开源的 Apache Dubbo 作为微服务框架，部署在自建机房的机器上，通常以中小公司为主，会有专门的运维搭建操作系统，设计网络拓扑，这也是云计算之前大多数公司的玩法。</p><p>但大家都知道，在现实生活中，“种子”变成“小麦”一般是农民伯伯的活，应该很少有人会因为想吃一个馒头，而去播撒一波种子的。例如很多互联网创业公司肯定接受不了这种玩法，想要尽快落地产品，第一步一定不是搞一堆机器，于是 IaaS 成了他们的救星，很多云计算公司提供了云服务器（ECS），解决了基础硬件问题，剩下的东西研发工程师都能搞定，所以“我有一个很好的 idea，就差一个程序员了”就变得流行了起来。在 IaaS 阶段，可以用 ECS 部署数据库，部署 Redis，部署 Dubbo 节点，机器出了问题可以提工单给云厂商，大大减少了运维成本，可以让研发人员更加专注在业务开发上。</p><p>很多 Dubbo 用户都是在“种子”和“小麦”阶段，还有人跟我说：我们公司在腾讯云上使用 Dubbo。老铁没毛病，开源框架没有强制跟任何云厂商绑定，Pivotal 的 SpringCloud 和 Alibaba 主导的 Dubbo 都是如此。可是，那我又有什么理由去进阶为“面粉”阶段的用户呢？我从 Dubbo 微信交流群的一些讨论去尝试回答这个问题：</p><ul><li>请教大家一个问题，开源的 Dubbo 控制台有一个 xx 的问题，大家有遇到吗？</li><li>请教一下大家，Dubbo 的限流你们是怎么做的？</li><li>有没有人做过 Dubbo 的全链路跟踪的？</li><li>Dubbo 的灰度发布有人在生产实践过吗？</li><li>Dubbo 的分布式事务怎么做？</li><li>Dubbo 的网关怎么做？</li><li>…</li></ul><p>以 Dubbo 为例，大家可以看到一些端倪，开源框架也会存在一些问题。例如开源框架优先考虑的是功能的普适性，对例如灰度发布这种定制化的需求支持较少，且功能越高端，越难以支持。再例如 Dubbo 的全链路跟踪，基本上是每次 Dubbo Meetup 之后收集到的高频问题。有一些公司有基础架构团队，虽然使用的是开源 Dubbo，但他们有能力去进行改造，例如当当、考拉都拉了自己的分支在进行迭代，但我相信，他们自己的扩展也一定是优先适配各自公司的场景。还有一类问题，开源产品也会存在 bug，有时候自己定位出了 bug 提给社区，还得等其他人 review &amp; merge request，必然会有时间成本，这也是很多公司自己拉分支自己维护的重要原因。说了这么多，其实都是在说“小麦”的缺点，“面粉”阶段又是如何解决这些问题的呢？</p><p>我在图中将 EDAS 这款阿里云的产品定位在了“面粉”阶段，主要原因便是：</p><ul><li>IaaS 层解决的问题，它都解决了，甚至做的更好。EDAS 甚至不需要用户去购买机器，可以进行诸如：弹性伸缩，限流降级，优雅上下线等应用生命周期的管理</li><li>在微服务侧，它支持 Dubbo、SpringCloud、HSF 等主流的微服务框架，并且对微服务能力进行了增强。有很多商业化的特性，例如：白屏化的服务治理界面、分布式链路追踪、灰度发布、离群摘除、服务限流、注册中心的高可用…等等，并且随着后续的迭代，会继续支持分布式事务、网关等特性</li><li>背靠阿里云，有专门的团队保障微服务的稳定性，让专业的人做专业的事</li></ul><p>我认为”面粉“是一个恰到好处的阶段，你可以烹饪成面包、面条、馒头，却又不会束缚你的产品形态。</p><h2 id="EDAS-支持微服务的发展史"><a href="#EDAS-支持微服务的发展史" class="headerlink" title="EDAS 支持微服务的发展史"></a>EDAS 支持微服务的发展史</h2><p>在阿里云的角度，如何吸引用户使用阿里云的服务呢？阿里云的服务实在太多了，还是来看微服务这个点。在 N 年前，EDAS 刚刚公测时，EDAS 主打的微服务框架是 HSF（High Speed Framework），熟悉它的朋友知道 HSF 是阿里集团内部使用的自研微服务框架，在历年双 11 支持整个阿里各个产品的平稳运行，有了双 11 这样量级的考验和阿里的背书，EDAS 支持 HSF 也就成了理所当然的事。这时候玩法是这样的：</p><p><img src="https://kirito.iocoder.cn/image-20200201144306917.png" alt="旧"></p><p>如果是 Dubbo 既存用户想要上云使用 EDAS，就必须要改用 HSF 框架改写自己的业务代码，这个迁移成本是很高的。那如果是全新的业务使用 HSF，是不是就没有这个问题了呢？也不尽然。站在开发的角度，开源 Dubbo 的社区非常活跃，文档详细，遇到问题时，搜索引擎也基本能提供解决方案。但 HSF 不同，它是一款闭源的框架，即使是阿里内部用户，可能对其也是一知半解，遇到问题，只能借助于工单、答疑群解决，看不到源码是原罪。尽管 HSF 非常稳定，但用户把自己的代码托管在一个黑匣子中，多多少少会有所顾忌。</p><blockquote><p>很多云平台都有类似的问题，业务上云往往意味着迁移技术栈，这个成本可想而知。</p></blockquote><p>随着阿里重启了 Dubbo 的开源，EDAS 开始支持了开源 Dubbo，这一下子解决了很多上面提到的问题。对于那些处于”种子“、”小麦“阶段的 Dubbo 用户而言，使用 EDAS 不需要修改任何一行代码，即可获得 EDAS 承诺的诸多增强能力。</p><p>紧接着是 SpringCloud Alibaba 的开源，EDAS 也提供了 100% 的兼容。至此，Dubbo 和 SpringCloud 应用上云，EDAS 都能够 cover。</p><h2 id="开源与商业化与云原生"><a href="#开源与商业化与云原生" class="headerlink" title="开源与商业化与云原生"></a>开源与商业化与云原生</h2><p>从刚刚的发展史可以发现云厂商的一个玩法，Dubbo、SpringCloud Alibaba 最早都是由阿里主导开源的，在商业化云产品上都得到了优先的支持，不仅阿里这一家这么玩，华为开源了 ServiceComb，蚂蚁开源了 Sofa Stack，腾讯开源了 Tars，这几家都有各自的云平台，基本都会让用户迁移至自家的微服务框架。开源一方面是培养了用户习惯，积累影响力，一方面也是在为商业化铺路。</p><p>对接开源 SDK 逐渐成为了云厂商们的共识，只不过这里的开源 SDK 现如今没有统一，各家都有各自的玩法。不过这已经是一个很大的进步了，因为开源不会绑架用户，哪一天这家云厂商用的不爽了，随时迁走。相比使用云厂商提供的 SDK 来说，使用开源 SDK 这个理念不知道先进到哪里去了。</p><p>说道这里，不免会有人提出云原生的概念，开源 SDK 实在太多了，如果只有一套大家都遵守的规范就好了，云原生大概就是在做这么一件事。俗话说的好，三等码农写代码，二等码农写框架，一等码农定规范。但目前来说，还没有一统江湖的”云原生开源 SDK“，但已经有人再按照这样的思路再推进了，有兴趣的可以去了解下阿里提出的 OAM。</p><h2 id="EDAS-微服务治理的难点"><a href="#EDAS-微服务治理的难点" class="headerlink" title="EDAS 微服务治理的难点"></a>EDAS 微服务治理的难点</h2><p>前面我们介绍到 EDAS 不仅仅是作为一个跑微服务框架的运行时容器，还为微服务框架提供了很多增强能力。科普完发展史之后，下面对微服务增强的细节进行介绍，但在此之前，为了不让读者知其然而不知其所以然，我先梳理下 EDAS 微服务治理的难点。</p><h3 id="难点-1"><a href="#难点-1" class="headerlink" title="难点 1"></a>难点 1</h3><p>EDAS 支持的微服务框架种类多，目前支持三种微服务框架：Dubbo、SpringCloud、HSF</p><h3 id="难点-2"><a href="#难点-2" class="headerlink" title="难点 2"></a>难点 2</h3><p>微服务框架版本多：</p><ul><li><p>Dubbo 支持 2.5.x，2.6.x，2.7.x</p></li><li><p>SpringCloud 支持 D 以上的版本</p></li></ul><h3 id="难点-3"><a href="#难点-3" class="headerlink" title="难点 3"></a>难点 3</h3><p>例如传统的服务查询，需要访问注册中心，但用户使用的注册中心种类多</p><ul><li>Zookeeper</li><li>Nacos</li><li>Eureka</li></ul><h3 id="难点-4"><a href="#难点-4" class="headerlink" title="难点 4"></a>难点 4</h3><p>部署形态多，EDAS 支持两种部署形态</p><ul><li><p>ECS Jar 包部署</p></li><li><p>K8s 镜像部署</p></li></ul><h3 id="难点-5"><a href="#难点-5" class="headerlink" title="难点 5"></a>难点 5</h3><p>需要考虑存量用户的迁移问题和改造成本</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>EDAS 微服务增强的实现方式必须要考虑以上众多因素，在不得不进行 trade off 时，也应该尽可能解决较多的痛点，避免用户进行过多的改造。</p><h2 id="EDAS-微服务增强实现"><a href="#EDAS-微服务增强实现" class="headerlink" title="EDAS 微服务增强实现"></a>EDAS 微服务增强实现</h2><p>用户部署在 EDAS 中的代码使用的是开源的 SDK，EDAS 又承诺用户不需要改动代码，那是如何做到微服务能力增强的呢？满足这个要求的正是 JDK 提供的 Instrument 机制，熟悉 pinpoint 和 skywalking 等分布式链路追踪框架的读者应该对这个技术不会感到陌生。很久之前我曾经写过一篇文章对它进行过介绍：<a href="https://www.cnkirito.moe/instrument/" target="_blank" rel="noopener">JAVA 拾遗 – Instrument 机制</a>。</p><p>借助一个 Demo，通过 Instrument 来实现无侵入式的 AOP。 </p><p><strong>MicroServiceTransformer</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MicroServiceTransformer</span> <span class="keyword">implements</span> <span class="title">ClassFileTransformer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">byte</span>[] transform(ClassLoader loader, String className, Class&lt;?&gt; classBeingRedefined, ProtectionDomain protectionDomain, <span class="keyword">byte</span>[] classfileBuffer) <span class="keyword">throws</span> IllegalClassFormatException &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">"org/apache/dubbo/config/ReferenceConfig"</span>.equals(className)) &#123;</span><br><span class="line">            System.out.println(<span class="string">"microservice improve"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对类进行装配的第一步是编写一个 GreetingTransformer 类，其继承自：<code>java.lang.instrument.ClassFileTransformer</code>。</p><p><strong>MicroServiceAgent</strong></p><p>除了上述的 Transformer，我们还需要有一个容器去加载它。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MicroService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">premain</span><span class="params">(String options, Instrumentation ins)</span> </span>&#123;</span><br><span class="line">        ins.addTransformer(<span class="keyword">new</span> MicroServiceTransformer());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>MicroServiceAgent 便是最终用户的 Jar 运行时挂载的 agent。具体如何加载 agent ，可以参考上述的文章链接，有完整的 demo 和教程。这里主要是为了引出装配机制。</p><p>EDAS 的微服务增强逻辑便如同 AOP 一样，利用无侵入式的挂载，以达到增强的逻辑，这个过程需要 agent 对不同版本进行逐一的适配，从而实现服务查询、灰度发布、分布式链路跟踪、离群摘除等能力。</p><h2 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h2><p>本文其实也是一个主观视角，从一个对 EDAS 一无所知的角度，侧重于 EDAS 的微服务能力介绍了一遍 EDAS。EDAS 本身是一个商业化的云产品，但结合开源我们可以从它的演进历史看到一些软件架构演进的规律，这对于我们把握今后的技术发展趋势也有一定的指导意义。</p><p>别的也不多了，我们 Dubbo、SpringCloud 商业化团队招人，对微服务、中间件感兴趣的同学欢迎私聊我，一起干有意思的事。私聊或者投简历~ 微信号：xiayimiaoshenghua</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;2020 是多事的一年，新冠状性病毒的肆虐，其次是自己也生了一场病，希望随着天气暖和起来，一起都能变得更好。&lt;/p&gt;
&lt;p&gt;前一段时间真的很忙，一直没有抽出时间，也没有什么思路给大家分享优质的文章，今天这篇文章很久之前就想写了，抓住这次假期的尾巴，总结一下我最近这一年的工作。&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="微服务" scheme="https://lexburner.github.io/tags/%E5%BE%AE%E6%9C%8D%E5%8A%A1/"/>
    
      <category term="EDAS" scheme="https://lexburner.github.io/tags/EDAS/"/>
    
  </entry>
  
  <entry>
    <title>Dubbo 稳定性案例：Nacos 注册中心可用性问题复盘</title>
    <link href="https://lexburner.github.io/dubbo-nacos-stability/"/>
    <id>https://lexburner.github.io/dubbo-nacos-stability/</id>
    <published>2019-12-02T13:44:00.000Z</published>
    <updated>2020-12-18T07:42:50.629Z</updated>
    
    <content type="html"><![CDATA[<h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>上周四晚刚回到家，就接到了软负载同学的电话，说是客户线上出了故障，我一听”故障“两个字，立马追问是什么情况，经过整理，还原出线上问题的原貌：</p><p>客户使用了 Dubbo，注册中心使用的是 Nacos，在下午开始不断有调用报错，查看日志，发现了 Nacos 心跳请求返回 502</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2019-11-15 03:02:41.973 [com.alibaba.nacos.client.naming454] -ERROR [com.alibaba.nacos.naming.beat.sender] request xx.xx.xx.xx failed.</span><br><span class="line">com.alibaba.nacos.api.exception.NacosException: failed to req  API: xx.xx.xx.xx:8848/nacos/v1/ns/instance/beat. code:502 msg:</span><br></pre></td></tr></table></figure><p>此时还没有大范围的报错。随后，用户对部分机器进行了重启，开始出现大规模的 Nacos 连接不上的报错，并且调用开始出现大量 no provider 的报错。</p><a id="more"></a><h2 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h2><p>Nacos 出现心跳报错，一般会有两种可能：</p><ul><li>用户机器出现问题，如网络不通</li><li>Nacos Server 宕机</li></ul><p>但由于是大面积报错，所以很快定位到是 Nacos Server 本身出了问题：由于磁盘老旧导致 IO 效率急剧下降，Nacos Server 无法响应客户端的请求，客户端直接接收到 502 错误响应。这个事件本身并不复杂，是一起注册中心磁盘故障引发的血案，但从这起事件，却可以窥探到很多高可用的问题，下面来跟大家一起聊聊这当中的细节。</p><h2 id="问题复现"><a href="#问题复现" class="headerlink" title="问题复现"></a>问题复现</h2><p><strong>Dubbo 版本</strong>：2.7.4</p><p><strong>Nacos 版本</strong>：1.1.4</p><p><strong>复现目标</strong>：在本地模拟 Nacos Server 宕机，检查 Dubbo 的调用是否会受到影响。</p><p><strong>复现步骤</strong>：</p><ol><li>本地启动 Nacos Server、Provider、Consumer，触发 Consumer 调用 Provider</li><li>kill -9 Nacos Server，模拟 Nacos Server 宕机，触发 Consumer 调用 Provider</li><li>重启 Consumer，触发 Consumer 调用 Provider</li></ol><p><strong>期望</strong>：</p><p>3 个步骤均可以调用成功</p><p><strong>实际结果</strong>:</p><p>1、2 调用成功，3 调用失败</p><p>问题成功复现，重启 Consumer 之后，没有调用成功，客户恰好遇到了这个问题。大家可能对这其中的细节还是有一些疑问，我设想了一些疑惑点，来和大家一起进行探讨。</p><h3 id="为什么-Nacos-宕机后，仍然可以调用成功"><a href="#为什么-Nacos-宕机后，仍然可以调用成功" class="headerlink" title="为什么 Nacos 宕机后，仍然可以调用成功"></a>为什么 Nacos 宕机后，仍然可以调用成功</h3><p>我们都知道，一般聊到 Dubbo，有三个角色是必须要聊到的：服务提供者、服务消费者、注册中心。他们的关系不用我赘述，可以从下面的连通性列表得到一个比较全面的认识：</p><ul><li>注册中心负责服务地址的注册与查找，相当于目录服务，服务提供者和消费者只在启动时与注册中心交互，注册中心不转发请求，压力较小</li><li>服务提供者向注册中心注册其提供的服务，此时间不包含网络开销</li><li>服务消费者向注册中心获取服务提供者地址列表，并根据负载算法直接调用提供者，此时间包含网络开销</li><li>注册中心，服务提供者，服务消费者三者之间均为长连接</li><li>注册中心通过长连接感知服务提供者的存在，服务提供者宕机，注册中心将立即推送事件通知消费者</li><li><strong>注册中心宕机，不影响已运行的提供者和消费者，消费者在本地缓存了提供者列表</strong></li><li>注册中心可选的，服务消费者可以直连服务提供者</li></ul><p>重点关注倒数第二条，Dubbo 其实在内存中缓存了一份提供者列表，这样可以方便地在每次调用时，直接从本地内存拿地址做负载均衡，而不避免每次调用都访问注册中心。只有当服务提供者节点发生上下线时，才会推送到本地，进行更新。所以，Nacos 宕机后，Dubbo 仍然可以调用成功。</p><h3 id="Nacos-宕机不影响服务调用，为什么日志中仍然有调用报错"><a href="#Nacos-宕机不影响服务调用，为什么日志中仍然有调用报错" class="headerlink" title="Nacos 宕机不影响服务调用，为什么日志中仍然有调用报错"></a>Nacos 宕机不影响服务调用，为什么日志中仍然有调用报错</h3><p>宕机期间，已有的服务提供者节点可能突然下线，但由于注册中心无法通知给消费者，所以客户端调用到下线的 IP 就会出现报错。</p><p>对于此类问题，Dubbo 也可以进行兜底</p><ul><li>Dubbo 会在连接级别进行心跳检测，当 channel 本身不可用时，即使没有注册中心通知，也会对其进行断连，并设置定时器，当该连接恢复后，再恢复其可用性</li><li>在阿里云商业版的 Dubbo – EDAS 中，提供了「离群摘除」功能，可以在调用层面即时摘除部分有问题的节点，保证服务的可用性。</li></ul><h3 id="为什么期望-Consumer-重启之后，调用成功"><a href="#为什么期望-Consumer-重启之后，调用成功" class="headerlink" title="为什么期望 Consumer 重启之后，调用成功"></a>为什么期望 Consumer 重启之后，调用成功</h3><p>Nacos Server 宕机后，Consumer 依旧可以调用成功，这个大家应该都比较清楚。但是为什么期望 Consumer 重启之后，依旧调用成功，有些人可能就会有疑问了，注册中心都宕机了，重启之后一定连不上，理应调用失败，怎么会期望成功呢？这就要涉及到 Nacos 的本地缓存了。</p><p><strong>Nacos 本地缓存的作用</strong>：当应用与服务注册中心发生网络分区或服务注册中心完全宕机后，应用进行了重启操作，内存里没有数据，此时应用可以通过读取本地缓存文件的数据来获取到最后一次订阅到的内容。</p><p>例如在 Dubbo 应用中定义了如下服务：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:service</span> <span class="attr">interface</span>=<span class="string">"com.alibaba.edas.xml.DemoService"</span> <span class="attr">group</span>=<span class="string">"DUBBO"</span> <span class="attr">version</span>=<span class="string">"1.0.0"</span> <span class="attr">ref</span>=<span class="string">"demoService"</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>可以在本机的 <code>/home/${user}/​nacos/naming/</code> 下看到各个命名空间发布的所有服务的信息，其内容格式如下：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;<span class="attr">"metadata"</span>:&#123;&#125;,<span class="attr">"dom"</span>:<span class="string">"DEFAULT_GROUP@@providers:com.alibaba.edas.xml.DemoService:1.0.0:DUBBO"</span>,<span class="attr">"cacheMillis"</span>:<span class="number">10000</span>,<span class="attr">"useSpecifiedURL"</span>:<span class="literal">false</span>,<span class="attr">"hosts"</span>:[&#123;<span class="attr">"valid"</span>:<span class="literal">true</span>,<span class="attr">"marked"</span>:<span class="literal">false</span>,<span class="attr">"metadata"</span>:&#123;<span class="attr">"side"</span>:<span class="string">"provider"</span>,<span class="attr">"methods"</span>:<span class="string">"sayHello"</span>,<span class="attr">"release"</span>:<span class="string">"2.7.4"</span>,<span class="attr">"deprecated"</span>:<span class="string">"false"</span>,<span class="attr">"dubbo"</span>:<span class="string">"2.0.2"</span>,<span class="attr">"pid"</span>:<span class="string">"5275"</span>,<span class="attr">"interface"</span>:<span class="string">"com.alibaba.edas.xml.DemoService"</span>,<span class="attr">"version"</span>:<span class="string">"1.0.0"</span>,<span class="attr">"generic"</span>:<span class="string">"false"</span>,<span class="attr">"revision"</span>:<span class="string">"1.0.0"</span>,<span class="attr">"path"</span>:<span class="string">"com.alibaba.edas.xml.DemoService"</span>,<span class="attr">"protocol"</span>:<span class="string">"dubbo"</span>,<span class="attr">"dynamic"</span>:<span class="string">"true"</span>,<span class="attr">"category"</span>:<span class="string">"providers"</span>,<span class="attr">"anyhost"</span>:<span class="string">"true"</span>,<span class="attr">"bean.name"</span>:<span class="string">"com.alibaba.edas.xml.DemoService"</span>,<span class="attr">"group"</span>:<span class="string">"DUBBO"</span>,<span class="attr">"timestamp"</span>:<span class="string">"1575355563302"</span>&#125;,<span class="attr">"instanceId"</span>:<span class="string">"30.5.122.3#20880#DEFAULT#DEFAULT_GROUP@@providers:com.alibaba.edas.xml.DemoService:1.0.0:DUBBO"</span>,<span class="attr">"port"</span>:<span class="number">20880</span>,<span class="attr">"healthy"</span>:<span class="literal">true</span>,<span class="attr">"ip"</span>:<span class="string">"30.5.122.3"</span>,<span class="attr">"clusterName"</span>:<span class="string">"DEFAULT"</span>,<span class="attr">"weight"</span>:<span class="number">1.0</span>,<span class="attr">"ephemeral"</span>:<span class="literal">true</span>,<span class="attr">"serviceName"</span>:<span class="string">"DEFAULT_GROUP@@providers:com.alibaba.edas.xml.DemoService:1.0.0:DUBBO"</span>,<span class="attr">"enabled"</span>:<span class="literal">true</span>&#125;],<span class="attr">"name"</span>:<span class="string">"DEFAULT_GROUP@@providers:com.alibaba.edas.xml.DemoService:1.0.0:DUBBO"</span>,<span class="attr">"checksum"</span>:<span class="string">"69c4eb7e03c03d4b18df129829a486a"</span>,<span class="attr">"lastRefTime"</span>:<span class="number">1575355563862</span>,<span class="attr">"env"</span>:<span class="string">""</span>,<span class="attr">"clusters"</span>:<span class="string">""</span>&#125;</span><br></pre></td></tr></table></figure><p>为什么期望重启后调用成功？因为经过检查，发现线上出现问题的机器上，缓存文件一切正常。虽然 Nacos Server 宕机了，本地的缓存文件依旧可以作为一个兜底，所以期望调用成功。</p><h3 id="为什么-Consumer-重启后，没有按照预期加载本地缓存文件"><a href="#为什么-Consumer-重启后，没有按照预期加载本地缓存文件" class="headerlink" title="为什么 Consumer 重启后，没有按照预期加载本地缓存文件"></a>为什么 Consumer 重启后，没有按照预期加载本地缓存文件</h3><p>缓存文件正常，问题只有可能出现在读取缓存文件的逻辑上。</p><ul><li>可能是 nacos-client 出了问题</li><li>可能是 Dubbo 的 nacos-registry 出了问题</li></ul><p>一番排查，在 Nacos 研发的协助下，找到了 naocs-client 的一个参数：  <code>namingLoadCacheAtStart</code>，该配置参数控制启动时是否加载缓存文件，默认值为 false。也就是说，使用 nacos-client，默认是不会加载本地缓存文件的。终于定位到线上问题的原因了：需要手动开启加载本地缓存，才能让 Nacos 加载本地缓存文件。</p><p>该参数设置为 true 和 false 的利弊：</p><ul><li>设置为 true，认为可用性 &amp; 稳定性优先，宁愿接受可能出错的数据，也不能因为没有数据导致调用完全出错</li><li>设置为 false，则认为 Server 的可用性较高，更能够接受没有数据，也不能接受错误的数据</li></ul><p>无论是 true 还是 false，都是对一些极端情况的兜底，而不是常态。对于注册发现场景，设置成 true，可能更合适一点，这样可以利用 Nacos 的本地缓存文件做一个兜底。</p><h3 id="Dubbo-传递注册中心参数"><a href="#Dubbo-传递注册中心参数" class="headerlink" title="Dubbo 传递注册中心参数"></a>Dubbo 传递注册中心参数</h3><p>Dubbo 中使用统一 URL 模型进行参数的传递，当我们需要在配置文件传递注册中心相关的配置参数时，可以通过键值对的形式进行拼接，当我们想要在 Dubbo 中开启加载注册中心缓存的开关时，可以如下配置：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:registry</span> <span class="attr">address</span>=<span class="string">"nacos://127.0.0.1:8848?namingLoadCacheAtStart=true"</span>/&gt;</span></span><br></pre></td></tr></table></figure><p>遗憾的是，<strong>最新版本的 Dubbo 只传递了部分参数给 Nacos Server，即使用户配置了 <code>namingLoadCacheAtStart</code> 也不会被服务端识别，进而无法加载本地缓存</strong>。我在本地修改了 Dubbo 2.7.5-SNAPSHOT，传递上述参数后，可以使得 1、2、3 三个阶段都调用成功，证明了 <code>namingLoadCacheAtStart</code> 的确可以使得 Dubbo 加载本地缓存文件。该问题将会在 Dubbo 2.7.5 得到修复，届时 Dubbo 中使用 Nacos 的稳定性将会得到提升。</p><h2 id="问题总结"><a href="#问题总结" class="headerlink" title="问题总结"></a>问题总结</h2><p>该线上问题反映出了 Nacos 注册中心可用性对 Dubbo 应用的影响，以及系统在某个组件宕机时，整体系统需要进行的一些兜底逻辑，不至于因为某个组件导致整个系统的瘫痪。</p><p>总结下现有代码的缺陷以及一些最佳实践：</p><ul><li>Dubbo 传递注册中心参数给 Nacos 时，只能够识别部分参数，这会导致用户的部分配置失效，在接下来的版本会进行修复。</li><li>nacos-client 加载本地缓存文件的开关等影响到系统稳定性的参数最好设计成 -D 启动参数，或者环境变量参数，这样方便发现问题，及时止血。例如此次的事件，有缺陷的 Dubbo 代码仅仅依赖于参数的传递，无法加载本地缓存文件，而如果有 -D 参数，可以强行开始加载缓存，大大降低了问题的影响面。</li><li><code>namingLoadCacheAtStart</code> 是否默认开启，还需要根据场景具体确定，但 nacos-server 宕机等极端场景下，开启该参数，可以尽可能地降低问题的影响面。顺带一提，Nacos 本身还提供了一个本地灾备文件，与本地缓存文件有一些差异，有兴趣的朋友也可以去了解一下。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;问题描述&quot;&gt;&lt;a href=&quot;#问题描述&quot; class=&quot;headerlink&quot; title=&quot;问题描述&quot;&gt;&lt;/a&gt;问题描述&lt;/h2&gt;&lt;p&gt;上周四晚刚回到家，就接到了软负载同学的电话，说是客户线上出了故障，我一听”故障“两个字，立马追问是什么情况，经过整理，还原出线上问题的原貌：&lt;/p&gt;
&lt;p&gt;客户使用了 Dubbo，注册中心使用的是 Nacos，在下午开始不断有调用报错，查看日志，发现了 Nacos 心跳请求返回 502&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;2019-11-15 03:02:41.973 [com.alibaba.nacos.client.naming454] -ERROR [com.alibaba.nacos.naming.beat.sender] request xx.xx.xx.xx failed.&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;com.alibaba.nacos.api.exception.NacosException: failed to req  API: xx.xx.xx.xx:8848/nacos/v1/ns/instance/beat. code:502 msg:&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;此时还没有大范围的报错。随后，用户对部分机器进行了重启，开始出现大规模的 Nacos 连接不上的报错，并且调用开始出现大量 no provider 的报错。&lt;/p&gt;
    
    </summary>
    
      <category term="Nacos" scheme="https://lexburner.github.io/categories/Nacos/"/>
    
    
      <category term="Dubbo" scheme="https://lexburner.github.io/tags/Dubbo/"/>
    
      <category term="Nacos" scheme="https://lexburner.github.io/tags/Nacos/"/>
    
  </entry>
  
  <entry>
    <title>一文聊透 Dubbo 优雅上线</title>
    <link href="https://lexburner.github.io/dubbo-gracefully-startup/"/>
    <id>https://lexburner.github.io/dubbo-gracefully-startup/</id>
    <published>2019-11-17T09:25:04.000Z</published>
    <updated>2020-05-30T18:27:01.593Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1 前言"></a>1 前言</h2><p>在此文之前，我写过一篇 《一文聊透 Dubbo 优雅停机》，这篇文章算是一个续集，优雅停机和优雅上线两者都是微服务生命周期中，开发者必须关心的环节。</p><p>优雅上线还有很多称呼：「无损上线」，「延迟发布」，「延迟暴露」。它们的对立面自然是：「有损上线」，「直接发布」。</p><p>我最近写的「一文聊透 Dubbo xx」系列文章，都有一个特点，即当你不注重文章中实践，你的 Dubbo 应用依旧可以正常运行，但总归在某些场景 case 下，你的系统会出现问题。做不到优雅上线，你的系统将会出现：在应用刚启动时，就有流量进入，而此时应用尚未初始化完毕，导致调用失败，在集群规模较大时，影响会变得很明显。</p><h2 id="2-方案一：延迟发布"><a href="#2-方案一：延迟发布" class="headerlink" title="2 方案一：延迟发布"></a>2 方案一：延迟发布</h2><p>以 SpingBoot 下使用 Dubbo 为例，被 Dubbo 的 @Service 注解修饰的服务，会按照 Spring 中初始化 Bean 的顺序，串行执行发布逻辑。Dubbo 框架会完成一系列的操作：</p><ul><li>创建远程调用的 Proxy</li><li>把代理对象注册到 ProviderConsumerRegTable，方便远程调用到来时寻找到对应的服务</li><li>向注册中心注册</li></ul><p>一旦服务信息注册到注册中心，在消费者看来该服务就是可以被调用的。然而，此时可能出现一些数据库、缓存资源尚未加载完毕的场景，这取决于你的系统有没有对应的组件，它们何时加载完毕，也完全取决于你的业务。如果你担心你的系统存在这种隐患，可以尝试多次重启集群中的任意一台机器，查看调用方是否存在报错，如果有报错，一种可能性是没有实现优雅停机，一种可能性是没有实现优雅上线。</p><p>Dubbo 服务暴露的起点一般是以 Spring 容器启动完毕后发出的 <code>ContextRefreshedEvent</code> 事件为准，Dubbo 的 <code>ServiceBean</code> 实现了 <code>ApplicationListener&lt;E extends ApplicationEvent&gt;</code> 接口，用以接收这一容器刷新事件。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ContextRefreshedEvent event)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 是否有延迟导出 &amp;&amp; 是否已导出 &amp;&amp; 是不是已被取消导出</span></span><br><span class="line">    <span class="keyword">if</span> (isDelay() &amp;&amp; !isExported() &amp;&amp; !isUnexported()) &#123;</span><br><span class="line">        <span class="comment">// 导出服务</span></span><br><span class="line">        export();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Dubbo 为服务提供了 <code>delay</code> 配置：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:service</span> <span class="attr">delay</span>=<span class="string">"5000"</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>如上配置后，Dubbo 服务将会在 Spring 容器启动后 5s，再执行暴露逻辑。这里 delay 的时长，取决于你系统资源初始化的耗时，没有一个经验值。如果不配置改值，Dubbo 将会在收到 <code>ContextRefreshedEvent</code> 事件后，立即执行发布逻辑。</p><blockquote><p>Dubbo 2.6.5 版本对服务延迟发布逻辑进行了细微的调整，将需要延迟暴露（delay &gt; 0）服务的倒计时动作推迟到了 Spring 初始化完成后进行。在此之前的版本的逻辑不太合理，如果想要让 2.6.5 之前的版本延迟到 Spring 初始化完成后，再暴露服务，可以这样配置：&lt;dubbo:service delay=”-1” /&gt;</p></blockquote><blockquote><p>本节参考 Dubbo 官方文档</p><p>延迟暴露：<a href="http://dubbo.apache.org/zh-cn/docs/user/demos/delay-publish.html" target="_blank" rel="noopener">http://dubbo.apache.org/zh-cn/docs/user/demos/delay-publish.html</a></p></blockquote><h2 id="3-方案二：QOS-命令上线"><a href="#3-方案二：QOS-命令上线" class="headerlink" title="3 方案二：QOS 命令上线"></a>3 方案二：QOS 命令上线</h2><p>Dubbo 还为服务提供了另一个配置项：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:service</span> <span class="attr">register</span>=<span class="string">"false"</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>该配置项配置后，服务将不会发布到注册中心，可能很多 Dubbo 用户不会注意到这个配置，它的作用恰恰是 QOS 指令使用的。</p><p>Dubbo 2.5.8 及以上的版本，还提供了一些在线运维命令。为了演示该命令，我们准备一个 <code>GreetingService</code> 的 demo：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DubboProvider</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        ServiceConfig&lt;GreetingsService&gt; service = <span class="keyword">new</span> ServiceConfig&lt;&gt;();</span><br><span class="line">        service.setApplication(<span class="keyword">new</span> ApplicationConfig(<span class="string">"dubbo-provider"</span>));</span><br><span class="line">        service.setRegistry(<span class="keyword">new</span> RegistryConfig(<span class="string">"nacos://127.0.0.1:8848"</span>));</span><br><span class="line">        service.setInterface(GreetingsService.class);</span><br><span class="line">        service.setRef(<span class="keyword">new</span> GreetingsServiceImpl());</span><br><span class="line">        service.setRegister(<span class="keyword">false</span>);</span><br><span class="line">        service.export();</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">"dubbo service started"</span>);</span><br><span class="line">        <span class="keyword">new</span> CountDownLatch(<span class="number">1</span>).await();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意我们配置了不发布：<code>service.setRegister(false)</code>，由于 QOS 配置是默认打开的，在本地的 22222 端口，可以进入 QOS 控制台。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"> krito git:(master) ✗ telnet localhost 22222</span><br><span class="line">Trying ::1...</span><br><span class="line">Connected to localhost.</span><br><span class="line">Escape character is &apos;^]&apos;.</span><br><span class="line">   ___   __  __ ___   ___   ____     </span><br><span class="line">  / _ \ / / / // _ ) / _ ) / __ \  </span><br><span class="line"> / // // /_/ // _  |/ _  |/ /_/ /    </span><br><span class="line">/____/ \____//____//____/ \____/   </span><br><span class="line">dubbo&gt;ls</span><br><span class="line">As Provider side:</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">|        Provider Service Name        |PUB|</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">|com.alibaba.edas.api.GreetingsService| N |</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">As Consumer side:</span><br><span class="line">+---------------------+---+</span><br><span class="line">|Consumer Service Name|NUM|</span><br><span class="line">+---------------------+---+</span><br><span class="line"></span><br><span class="line">dubbo&gt;online  </span><br><span class="line">OK</span><br><span class="line">dubbo&gt;ls</span><br><span class="line">As Provider side:</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">|        Provider Service Name        |PUB|</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">|com.alibaba.edas.api.GreetingsService| Y |</span><br><span class="line">+-------------------------------------+---+</span><br><span class="line">As Consumer side:</span><br><span class="line">+---------------------+---+</span><br><span class="line">|Consumer Service Name|NUM|</span><br><span class="line">+---------------------+---+</span><br><span class="line"></span><br><span class="line">dubbo&gt;</span><br></pre></td></tr></table></figure><p>如上图所示，我们首先查看到 <code>com.alibaba.edas.api.GreetingsService</code> 服务是未发布的，通过 online 命令手动将服务发布，再使用 <code>ls</code> 查看服务列表时，已经显示服务处于发布状态了。</p><p>除了使用 telnet，还可以通过 HTTP 访问：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl localhost:22222/online</span><br></pre></td></tr></table></figure><p>小 tips：在使用 SpringBoot 注解式声明一个 Service 时，register 属性会失效，在 xml 或者 API 方式下声明则运行正常，怀疑是 dubbo-spring-boot-starter 的一个 bug。</p><p><img src="https://kirito.iocoder.cn/image-20191117205139227.png" alt="image-20191117205139227"></p><p><img src="https://kirito.iocoder.cn/image-20191117205207044.png" alt="image-20191117205207044"></p><p>大家在 SpringBoot 下使用 Dubbo 需要留意类似的问题，之前有过一些属性在 SpringBoot 注解中未解析或为提供注解配置的案例，在使用时需要注意。</p><blockquote><p>本节参考 Dubbo 官方文档</p><p>服务配置说明：<a href="http://dubbo.apache.org/zh-cn/docs/user/references/xml/dubbo-service.html" target="_blank" rel="noopener">http://dubbo.apache.org/zh-cn/docs/user/references/xml/dubbo-service.html</a></p><p>QOS：<a href="http://dubbo.apache.org/zh-cn/docs/user/references/qos.html" target="_blank" rel="noopener">http://dubbo.apache.org/zh-cn/docs/user/references/qos.html</a></p></blockquote><h2 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h2><p>本文介绍了两种 Dubbo 的机制：</p><ul><li>方案一：延迟发布（delay=5000）</li><li>方案二：不发布 + QOS 指令发布（register=false）</li></ul><p>想要实现优雅上线，可以采取适合你系统的方式。方案一延迟发布的优势在于实现简单，但具体 delay 多少秒，比较依赖系统维护者的经验。方案二使用 QOS 指令，一般依靠于发布系统，当发布系统检测到固定的资源加载完毕这样一个信号时，自动触发上线命令，更加灵活。</p><p>当你系统遇到应用启动时流量有所损失时，就应该考虑一下优雅上线的问题了，更多 Dubbo 使用的注意点，请持续关注公众号。</p><p><img src="https://www.cnkirito.moe/css/images/wechat_public.jpg" alt="img"></p><p><em>「技术分享」<strong>某种程度上，是让作者和读者，不那么孤独的东西。</strong>欢迎关注我的微信公众号：<strong>「</strong>Kirito的技术分享」</em></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;1-前言&quot;&gt;&lt;a href=&quot;#1-前言&quot; class=&quot;headerlink&quot; title=&quot;1 前言&quot;&gt;&lt;/a&gt;1 前言&lt;/h2&gt;&lt;p&gt;在此文之前，我写过一篇 《一文聊透 Dubbo 优雅停机》，这篇文章算是一个续集，优雅停机和优雅上线两者都是微服务生命周期中
      
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="Dubbo" scheme="https://lexburner.github.io/tags/Dubbo/"/>
    
  </entry>
  
  <entry>
    <title>一文聊透 Dubbo 元数据中心</title>
    <link href="https://lexburner.github.io/dubbo-metadata/"/>
    <id>https://lexburner.github.io/dubbo-metadata/</id>
    <published>2019-11-03T07:05:50.000Z</published>
    <updated>2019-11-04T02:40:13.942Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>如果让你在本地构建一个 Dubbo 应用，你会需要额外搭建哪些中间件呢？如果没猜错的话，你的第一反应应该是注册中心，类 Dubbo 的大多数服务治理框架都有注册中心的概念。你可以部署一个 Zookeeper，或者一个 Nacos，看你的喜好。但在 Apache Dubbo 的 2.7 版本后，额外引入了两个中间件：元数据中心和配置中心。</p><p>在今年年初 Dubbo 2.7 刚发布时，我就写了一篇文章 《<a href="https://www.cnkirito.moe/dubbo27-features/" target="_blank" rel="noopener">Dubbo 2.7 三大新特性详解</a>》，介绍了包含元数据中心改造在内的三大新特性，但一些细节介绍没有详细呈现出来，在这篇文章中，我将会以 Dubbo 为例，跟大家一起探讨一下服务治理框架中元数据中心的意义与集成细节。</p><p><img src="https://kirito.iocoder.cn/image-20191103160946348.png" alt="Dubbo 2.7 架构"></p><a id="more"></a><h2 id="元数据中心介绍"><a href="#元数据中心介绍" class="headerlink" title="元数据中心介绍"></a>元数据中心介绍</h2><p>服务治理中的元数据（Metadata）指的是服务分组、服务版本、服务名、方法列表、方法参数列表、超时时间等，这些信息将会存储在元数据中心之中。与元数据平起平坐的一个概念是服务的注册信息，即：服务分组、服务版本、服务名、地址列表等，这些信息将会存储在注册中心中。稍微一对比可以发现，元数据中心和注册中心存储了一部分共同的服务信息，例如服务名。两者也有差异性，元数据中心还会存储方法列表即参数列表，注册中心存储了服务地址。上述的概述，体现出了元数据中心和注册中心在服务治理过程中，担任着不同的角色。为了有一个直观的对比，我整理出了下面的表格：</p><table><thead><tr><th></th><th>元数据</th><th>注册信息</th></tr></thead><tbody><tr><td><strong>职责</strong></td><td>描述服务，定义服务的基本属性</td><td>存储地址列表</td></tr><tr><td><strong>变化频繁度</strong></td><td>基本不变</td><td>随着服务上下线而不断变更</td></tr><tr><td><strong>数据量</strong></td><td>大</td><td>小</td></tr><tr><td><strong>数据交互/存储模型</strong></td><td>消费者/提供者上报，控制台查询</td><td>PubSub 模型，提供者上报，消费者订阅</td></tr><tr><td><strong>主要使用场景</strong></td><td>服务测试、服务 MOCK</td><td>服务调用</td></tr><tr><td><strong>可用性要求</strong></td><td>元数据中心可用性要求不高，不影响主流程</td><td>注册中心可用性要求高，影响到服务调用的主流程</td></tr></tbody></table><p>下面我会对每个对比点进行单独分析，以加深对元数据中心的理解。</p><h3 id="职责"><a href="#职责" class="headerlink" title="职责"></a>职责</h3><p>在 Dubbo 2.7 版本之前，并没有元数据中心的概念，那时候注册信息和元数据都耦合在一起。Dubbo Provider 的服务配置有接近 30 个配置项，排除一部分注册中心服务治理需要的参数，很大一部分配置项仅仅是 Provider 自己使用，不需要透传给消费者；Dubbo Consumer 也有 20 多个配置项。在注册中心之中，服务消费者列表中只需要关注 application，version，group，ip，dubbo 版本等少量配置。这部分数据不需要进入注册中心，而只需要以 key-value 形式持久化存储在元数据中心即可。从职责来看，将不同职责的数据存储在对应的组件中，会使得逻辑更加清晰。</p><h3 id="变化频繁度"><a href="#变化频繁度" class="headerlink" title="变化频繁度"></a>变化频繁度</h3><p>注册信息和元数据耦合在一起会导致注册中心数据量的膨胀，进而增大注册中心的网络开销，直接造成了服务地址推送慢等负面影响。服务上下线会随时发生，变化的其实是注册信息，元数据是相对不变的。</p><h3 id="数据量"><a href="#数据量" class="headerlink" title="数据量"></a>数据量</h3><p>由于元数据包含了服务的方法列表以及参数列表，这部分数据会导致元数据要比注册信息大很多。注册信息被设计得精简会直接直接影响到服务推送的 SLA。</p><h3 id="数据交互-存储模型"><a href="#数据交互-存储模型" class="headerlink" title="数据交互/存储模型"></a>数据交互/存储模型</h3><p>注册中心采用的是 PubSub 模型，这属于大家的共识，所以注册中心组件的选型一般都会要求其有 notify 的机制。而元数据中心并没有 notify 的诉求，一般只需要组件能够提供 key-value 的存储结构即可。</p><h3 id="主要使用场景"><a href="#主要使用场景" class="headerlink" title="主要使用场景"></a>主要使用场景</h3><p>在服务治理中，注册中心充当了通讯录的职责，在复杂的分布式场景下，让消费者能找到提供者。而元数据中心存储的元数据，主要适用于服务测试、服务 MOCK 等场景，这些场景都对方法列表、参数列表有所诉求。在下面的小节中，我也会对使用场景进行更加详细的介绍。</p><h3 id="可用性要求"><a href="#可用性要求" class="headerlink" title="可用性要求"></a>可用性要求</h3><p>注册中心宕机或者网络不通会直接影响到服务的可用性，它影响了服务调用的主路径。但一般而言，元数据中心出现问题，不会影响到服务调用，它提供的能力是可被降级的。这也阐释了一点，为什么很多用户在 Dubbo 2.7 中没有配置元数据中心，也没有影响到正常的使用。元数据中心在服务治理中扮演的是锦上添花的一个角色。在组件选型时，我们一般也会对注册中心的可用性要求比较高，元数据中心则可以放宽要求。</p><h2 id="元数据中心的价值"><a href="#元数据中心的价值" class="headerlink" title="元数据中心的价值"></a>元数据中心的价值</h2><p>小孩子才分对错，成年人只看利弊。额外引入一个元数据中心，必然带来运维成本、理解成本、迁移成本等问题，那么它具备怎样的价值，来说服大家选择它呢？上面我们介绍元数据中心时已经提到了服务测试、服务 MOCK 等场景，这一节我们重点探讨一下元数据中心的价值。</p><h3 id="降低地址推送的时延"><a href="#降低地址推送的时延" class="headerlink" title="降低地址推送的时延"></a>降低地址推送的时延</h3><p>由于注册中心采用的是 PubSub 模型，数据量的大小会直接影响到服务地址推送时间，不知道你有没有遇到过 <code>No provider available</code> 的报错呢？明明提供者已经启动了，但由于注册中心推送慢会导致很多问题，一方面会影响到服务的可用性，一方面也会增加排查问题的难度。</p><p>在一次杭州 Dubbo Meetup 中，网易考拉分享了他们对 Zookeeper 的改造，根源就是</p><blockquote><p>推送量大 -&gt; 存储数据量大 -&gt; 网络传输量大 -&gt; 延迟严重</p></blockquote><p>这一实际案例佐证了元数据改造并不是凭空产生的需求，而是切实解决了一个痛点。</p><h3 id="服务测试-amp-服务-MOCK"><a href="#服务测试-amp-服务-MOCK" class="headerlink" title="服务测试 &amp; 服务 MOCK"></a>服务测试 &amp; 服务 MOCK</h3><p>在 Dubbo 2.7 之前，虽然注册中心耦合存储了不少本应属于元数据的数据，但也漏掉了一部分元数据，例如服务的方法列表，参数列表。这些是服务测试和服务 MOCK 必备的数据，想要使用这些能力，就必须引入元数据中心。例如开源的 <a href="https://github.com/apache/dubbo-admin" target="_blank" rel="noopener">Dubbo Admin</a> 就实现了服务测试功能，用户可以在控制台上对已经发布的服务提供者进行功能测试。可能你之前有过这样的疑惑：为什么只有 Dubbo 2.7 才支持了服务测试呢？啊哈，原因就是 Dubbo 2.7 才有了元数据中心的概念。当然，服务 MOCK 也是如此。</p><p><img src="https://kirito.iocoder.cn/image-20191103180851464.png" alt="服务测试"></p><h3 id="其他场景"><a href="#其他场景" class="headerlink" title="其他场景"></a>其他场景</h3><p>可以这么理解，任何依赖元数据的功能，都需要元数据中心的支持。其他场景还包括了网关应用获取元数据来进行泛化调用、服务自动化测试等等。再描述一个可能的场景，抛砖引玉。在一次南京 Dubbo Meetup 上，dubbo.js 的作者提及的一个场景，希望根据元数据自动生成 NodeJs 代码，以简化前端的开发量，也是元数据的作用之一。这里就需要发挥各位的想象力了</p><h2 id="Dubbo-配置元数据中心"><a href="#Dubbo-配置元数据中心" class="headerlink" title="Dubbo 配置元数据中心"></a>Dubbo 配置元数据中心</h2><p>目前 Dubbo 最新的版本为 2.7.4，目前支持的几种元数据中心可以从源码中得知（官方文档尚未更新）：</p><p><img src="https://kirito.iocoder.cn/image-20191103204744161.png" alt="元数据中心扩展"></p><p>支持 consul、etcd、nacos、redis、zookeeper 这五种组件。</p><p>配置方式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dubbo.metadata-report.address=nacos://127.0.0.1:8848</span><br></pre></td></tr></table></figure><h2 id="元数据存储格式剖析"><a href="#元数据存储格式剖析" class="headerlink" title="元数据存储格式剖析"></a>元数据存储格式剖析</h2><p>前面我们介绍了元数据中心的由来以及价值，还是飘在天上的概念，这一节将会让概念落地。元数据是以怎么样一个格式存储的呢？</p><p>以 DemoService 服务为例：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:service</span> <span class="attr">interface</span>=<span class="string">"com.alibaba.dubbo.demo.DemoService"</span> <span class="attr">ref</span>=<span class="string">"demoService"</span> <span class="attr">executes</span>=<span class="string">"4500"</span> <span class="attr">retries</span>=<span class="string">"7"</span> <span class="attr">owner</span>=<span class="string">"kirito"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dubbo:registry</span> <span class="attr">address</span>=<span class="string">"zookeeper://127.0.0.1:2181"</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>首先观察在 Dubbo 2.6.x 中，注册中心如何存储这个服务的信息：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">dubbo://30.5.120.185:20880/com.alibaba.dubbo.demo.DemoService?</span><br><span class="line">anyhost=true&amp;</span><br><span class="line">application=demo-provider&amp;</span><br><span class="line">interface=com.alibaba.dubbo.demo.DemoService&amp;</span><br><span class="line">methods=sayHello&amp;</span><br><span class="line">bean.name=com.alibaba.dubbo.demo.DemoService&amp;</span><br><span class="line">dubbo=2.0.2&amp;executes=4500&amp;</span><br><span class="line">generic=false&amp;owner=kirito&amp;</span><br><span class="line">pid=84228&amp;retries=7&amp;side=provider&amp;timestamp=1552965771067</span><br></pre></td></tr></table></figure><p>例如 <code>bean.name</code> 和 <code>owner</code> 这些属性，肯定是没必要注册上来的。</p><p>接着，我们在 Dubbo 2.7 中使用最佳实践，为 registry 配置 simplified=true：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dubbo:service</span> <span class="attr">interface</span>=<span class="string">"com.alibaba.dubbo.demo.DemoService"</span> <span class="attr">ref</span>=<span class="string">"demoService"</span> <span class="attr">executes</span>=<span class="string">"4500"</span> <span class="attr">retries</span>=<span class="string">"7"</span> <span class="attr">owner</span>=<span class="string">"kirito"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dubbo:registry</span> <span class="attr">address</span>=<span class="string">"zookeeper://127.0.0.1:2181"</span> <span class="attr">simplified</span>=<span class="string">"true"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dubbo:metadata-report</span> <span class="attr">address</span>=<span class="string">"nacos://127.0.0.1:8848"</span>/&gt;</span></span><br></pre></td></tr></table></figure><p>之后再观察注册中心的数据，已经变得相当精简了：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">dubbo://30.5.120.185:20880/org.apache.dubbo.demo.api.DemoService?</span><br><span class="line">application=demo-provider&amp;</span><br><span class="line">dubbo=2.0.2&amp;</span><br><span class="line">release=2.7.0&amp;</span><br><span class="line">timestamp=1552975501873</span><br></pre></td></tr></table></figure><p>被精简省略的数据不代表没有用了，而是转移到了元数据中心之中，我们观察一下此时元数据中心中的数据：</p><p><img src="https://kirito.iocoder.cn/image-20191103202821635.png" alt="元数据存储格式"></p><h2 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h2><p>元数据中心是服务治理中的一个关键组件，但对于大多数用户来说还是一个比较新的概念，我整理了一些我认为的最佳实践，分享给大家。</p><ul><li>从 Dubbo 2.6 迁移到 Dubbo 2.7 时，可以采取三步走的策略来平滑迁移元数据。第一步：Dubbo 2.6 + 注册中心，第二步：Dubbo 2.7 + 注册中心 + 元数据中心，第三步：Dubbo 2.7 + 注册中心（simplified=true） + 元数据中心。在未来 Dubbo 的升级版本中，registry 的 simplified 默认值将会变成 true，目前是 false，预留给用户一个升级的时间。</li><li>应用在启动时，会发布一次元数据，在此之后会有定时器，一天同步一次元数据，以上报那些运行时生成的 Bean，目前用户不可以配置元数据上报的周期，但可以通过 <code>-Dcycle.report</code> 关闭这一定时器。</li><li>元数据中心推荐选型：Nacos 和 Redis。</li></ul><p>Dubbo 2.7 还有很多有意思的特性，如果你对 Dubbo 有什么感兴趣的问题，欢迎在文末或者后台进行留言，后面我会继续更新 Dubbo 系列的文章。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;如果让你在本地构建一个 Dubbo 应用，你会需要额外搭建哪些中间件呢？如果没猜错的话，你的第一反应应该是注册中心，类 Dubbo 的大多数服务治理框架都有注册中心的概念。你可以部署一个 Zookeeper，或者一个 Nacos，看你的喜好。但在 Apache Dubbo 的 2.7 版本后，额外引入了两个中间件：元数据中心和配置中心。&lt;/p&gt;
&lt;p&gt;在今年年初 Dubbo 2.7 刚发布时，我就写了一篇文章 《&lt;a href=&quot;https://www.cnkirito.moe/dubbo27-features/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Dubbo 2.7 三大新特性详解&lt;/a&gt;》，介绍了包含元数据中心改造在内的三大新特性，但一些细节介绍没有详细呈现出来，在这篇文章中，我将会以 Dubbo 为例，跟大家一起探讨一下服务治理框架中元数据中心的意义与集成细节。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://kirito.iocoder.cn/image-20191103160946348.png&quot; alt=&quot;Dubbo 2.7 架构&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="Dubbo" scheme="https://lexburner.github.io/tags/Dubbo/"/>
    
  </entry>
  
  <entry>
    <title>Linux 环境写文件如何稳定跑满磁盘 I/O 带宽?</title>
    <link href="https://lexburner.github.io/linux-io-benchmark/"/>
    <id>https://lexburner.github.io/linux-io-benchmark/</id>
    <published>2019-10-09T11:01:38.000Z</published>
    <updated>2019-11-03T07:20:20.636Z</updated>
    
    <content type="html"><![CDATA[<ul><li>准备<ul><li>要求</li><li>机器配置</li><li>测试磁盘 IO 性能</li></ul></li><li>实验一: Buffer IO 写入</li><li>实验二: 4K 单次 Direct IO 写入</li><li>实验三: mmap 写入</li><li>实验四: 改进的 mmap 写入</li><li>结论</li></ul><a id="more"></a><h2 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h2><h3 id="要求"><a href="#要求" class="headerlink" title="要求"></a>要求</h3><p>在 <strong> 限制内存 </strong> 的情况下，假定我们每次写入 4k 的数据，如何保证 <code>kill -9</code> 不丢数据的情况下，仍然稳定的跑满磁盘的 IO？因为需要保证 <code>kill -9</code> 不丢数据，所以 <code>fwrite()</code> 就不在我们的考虑范围之内了. 又因为限制内存，所以直观的想法是直接 Direct IO, 但 Direct IO 能否跑满磁盘 IO 呢?</p><h3 id="机器配置"><a href="#机器配置" class="headerlink" title="机器配置"></a>机器配置</h3><p><strong>CPU</strong>: 64 核 Intel(R) Xeon(R) CPU E5-2682 v4 @ 2.50GHz</p><p><strong> 磁盘 </strong>: Intel Optane SSD</p><h3 id="测试磁盘-IO-性能"><a href="#测试磁盘-IO-性能" class="headerlink" title="测试磁盘 IO 性能"></a>测试磁盘 IO 性能</h3><p>官方称读 / 写带宽是 <code>2400/2000 MB/s</code>, 我们利用 <code>fio</code> 来进行实测:</p><p>顺序读性能:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo fio --filename=test -iodepth=64 -ioengine=libaio --direct=1 --rw=read --bs=2m --size=2g --numjobs=4 --runtime=10 --group_reporting --name=test-read</span><br></pre></td></tr></table></figure><p>结果:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">READ: bw=2566MiB/s (2691MB/s), 2566MiB/s-2566MiB/s (2691MB/s-2691MB/s), io=8192MiB (8590MB), run=3192-3192msec</span><br></pre></td></tr></table></figure><p>顺序写性能:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo fio --filename=test -iodepth=64 -ioengine=libaio -direct=1 -rw=write -bs=1m -size=2g -numjobs=4 -runtime=20 -group_reporting -name=test-write</span><br></pre></td></tr></table></figure><p>结果:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">WRITE: bw=2181MiB/s (2287MB/s), 2181MiB/s-2181MiB/s (2287MB/s-2287MB/s), io=8192MiB (8590MB), run=3756-3756msec</span><br></pre></td></tr></table></figure><p>实测读写带宽: <strong>2566/2181 MB/s</strong></p><h2 id="实验一-Buffer-IO-写入"><a href="#实验一-Buffer-IO-写入" class="headerlink" title="实验一: Buffer IO 写入"></a>实验一: Buffer IO 写入</h2><p>因为是限制内存，所以 Buffer IO 不在我们的考虑范围内，但是我们先来测试一下 Buffer IO 的具体性能到底如何? 我们使用最简单的方法，因为我们的 CPU 核数是 64，所以直接 64 线程单次 <code>4K</code> 字节 Buffer IO 写入, 即通过操作系统的 Page Cache 的策略来缓存，刷盘:</p><p><strong> 代码片段 </strong>: <a href="https://gist.github.com/Leviathan1995/640bb1631001409c828ca19d40c7b7f6" target="_blank" rel="noopener">完整代码</a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">static char data[4096] attribute((aligned(4096))) = &#123;&apos;a&apos;&#125;; </span><br><span class="line"></span><br><span class="line">void writer(int index) &#123; </span><br><span class="line">std::string fname = &quot;data&quot; + std::to_string(index); </span><br><span class="line">int data_fd = ::open(fname.c_str(), O_RDWR | O_CREAT | O_APPEND, 0645); </span><br><span class="line">for (int32_t i = 0; i &lt; 1000000; i++) &#123; </span><br><span class="line">::write(data_fd, data, 4096); </span><br><span class="line">&#125; </span><br><span class="line">close(data_fd); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">int main() &#123; </span><br><span class="line">std::vectorstd::thread threads; </span><br><span class="line">for(int i = 0; i &lt; 64; i++) &#123; </span><br><span class="line">std::thread worker(writer, i); </span><br><span class="line">threads.push_back(std::move(worker)); </span><br><span class="line">&#125; </span><br><span class="line">for (int i = 0; i &lt; 64; i++) &#123; </span><br><span class="line">threads[i].join(); </span><br><span class="line">&#125; </span><br><span class="line">return 0; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们通过 <code>O_APPEND</code> 单次 4k 追加写入，之后通过 <code>vmstat</code> 来保留 <code>120s</code> 的写入带宽:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vmstat 1 120 &gt; buffer_io</span><br></pre></td></tr></table></figure><p>经过最后的测试数据整理，我们发现 Buffer IO 的性能基本能稳定跑满带宽, 其中只有一次 I/O 抖动:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/buffer_io.png" alt="buffer_io"></p><h2 id="实验二-4K-单次-Direct-IO-写入"><a href="#实验二-4K-单次-Direct-IO-写入" class="headerlink" title="实验二: 4K 单次 Direct IO 写入"></a>实验二: 4K 单次 Direct IO 写入</h2><p>Buffer IO 利用 Page Cache 帮助我们缓存了大量的数据，其实必然提高了写入带宽，但假如在限制内存的情况下，Buffer IO 就不是正确的解决方案了，这次我们绕过 Page Cache, 直接 Direct IO 单次 <code>4K</code> 写入:</p><p><strong> 代码片段 </strong>: <a href="https://gist.github.com/Leviathan1995/048f89fb679ef1ecc13978029b20643c" target="_blank" rel="noopener">完整代码</a></p><p>唯一需要修改的地方就是在 <code>open()</code> 中加入 <code>O_DIRECT</code> 标志:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int data_fd = ::open(fname.c_str(), O_RDWR | O_CREAT | O_APPEND | O_DIRECT, 0645);</span><br></pre></td></tr></table></figure><p>通过 <code>vmstat</code> 获取写入带宽数据, 整理如下:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/direct_io.png" alt="direct_io"></p><p>通过数据我们发现，单次 4k 的 Direct IO 写入无法跑满磁盘的 I/O 带宽，仅仅只有 <code>800MB/S</code></p><h2 id="实验三-mmap-写入"><a href="#实验三-mmap-写入" class="headerlink" title="实验三: mmap 写入"></a>实验三: mmap 写入</h2><p>通过前面这两个实验我们发现，Buffer IO 是可以跑满磁盘 I/O 的，那我们可以尝试模拟 Buffer IO 的写入方式，使用较少的内存来达到 Buffer IO 的写入效果.</p><p>我们使用 <code>mmap</code> 来实现 Buffer IO 写入，通过限定的 Buffer Block 来模拟 Page Cache 的聚合效果, 实验中我们使用 <code>memcpy</code> 来完成数据拷贝，Buffer Block 我们设定为 <code>4K * 4</code>, 与 Direct IO 的不同，我们这次限定即 <code>16KB</code> 的单次写入:</p><p><strong>代码片段</strong>: <a href="https://gist.github.com/Leviathan1995/9c54f76bc22668289126a7898562a86d" target="_blank" rel="noopener">完整代码</a></p><p><code>main()</code> 函数不变，修改线程的 <code>writer()</code> 函数:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line">static char data[4096] attribute((aligned(4096))) = &#123;&apos;a&apos;&#125;;</span><br><span class="line">static int32_t map_size = 4096 * 4;</span><br><span class="line"></span><br><span class="line">void MapRegion(int fd, uint64_t file_offset, char** base) &#123; </span><br><span class="line">void* ptr = mmap(nullptr, map_size, PROT_READ | PROT_WRITE, </span><br><span class="line">MAP_SHARED, </span><br><span class="line">fd, </span><br><span class="line">file_offset); </span><br><span class="line">if (unlikely(ptr == MAP_FAILED)) &#123; </span><br><span class="line">*base = nullptr; </span><br><span class="line">return; </span><br><span class="line">&#125;</span><br><span class="line">base = reinterpret_cast&lt;char&gt;(ptr); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">void UnMapRegion(char* base) &#123; </span><br><span class="line">munmap(base, map_size); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">void writer(int index) &#123; </span><br><span class="line">std::string fname = &quot;data&quot; + std::to_string(index); </span><br><span class="line">char* base = nullptr; </span><br><span class="line">char* cursor = nullptr; </span><br><span class="line">uint64_t mmap_offset = 0, file_offset = 0; </span><br><span class="line">int data_fd = ::open(fname.c_str(), O_RDWR | O_CREAT, 0645); </span><br><span class="line">posix_fallocate(data_fd, 0, (4096UL * 1000000)); </span><br><span class="line">MapRegion(data_fd, 0, &amp;base); </span><br><span class="line">if (unlikely(base == nullptr)) &#123; </span><br><span class="line">return; </span><br><span class="line">&#125; </span><br><span class="line">cursor = base; </span><br><span class="line">file_offset += map_size; </span><br><span class="line">for (int32_t i = 0; i &lt; 1000000; i++) &#123; </span><br><span class="line">if (unlikely(mmap_offset &gt;= map_size)) &#123; </span><br><span class="line">UnMapRegion(base); </span><br><span class="line">MapRegion(data_fd, file_offset, &amp;base); </span><br><span class="line">if (unlikely(base == nullptr)) &#123; </span><br><span class="line">return; </span><br><span class="line">&#125; </span><br><span class="line">cursor = base; </span><br><span class="line">file_offset += map_size; </span><br><span class="line">mmap_offset = 0; </span><br><span class="line">&#125; </span><br><span class="line">memcpy(cursor, data, 4096); </span><br><span class="line">cursor += 4096; </span><br><span class="line">mmap_offset += 4096; </span><br><span class="line">&#125; </span><br><span class="line">UnMapRegion(base); </span><br><span class="line">close(data_fd); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们通过 <code>vmstat</code> 来获取写入带宽数据，我们发现 <code>mmap</code> 的 <code>16K</code> 写入可以跑满磁盘带宽，但 I/O 抖动较大，无法类似于 Buffer IO 稳定的写入.</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/mmap_io.png" alt="mmap_io"></p><p>我们通过 <code>perf</code> 生成火焰图分析:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/perf.png" alt="mmap_perf"></p><p>通过 <code>pref</code> 生成分析瓶颈时发现，写入 <code>writer()</code> 时触发了大量的 <code>Page Fault</code>, 即缺页中断，而 <code>mmap()</code> 本身的调用也有一定的消耗 (关于 <code>mmap()</code> 的源码分析，我们在后面的文章会详细分析 )，我们实验三的思路是: 首先 <code>fallocate</code> 一个大文件，然后 <code>mmap()</code> 内存映射 <code>16k</code> 的 Block, <code>memcpy()</code> 写满之后，游标右移重新 <code>mmap()</code>，以此循环.</p><h2 id="实验四-改进的-mmap-写入"><a href="#实验四-改进的-mmap-写入" class="headerlink" title="实验四: 改进的 mmap 写入"></a>实验四: 改进的 mmap 写入</h2><p>为了避免 <code>mmap()</code> 的开销，我们使用临时文件在写入之前 <code>mmap()</code> 映射，之后循环利用这 <code>16K</code> 的 Block, 避免 <code>mmap()</code> 的巨大开销:</p><p><strong>代码片段</strong>: <a href="https://gist.github.com/Leviathan1995/9c54f76bc22668289126a7898562a86d" target="_blank" rel="noopener">完整代码</a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line">void MapRegion(int fd, uint64_t file_offset, char** base) &#123;</span><br><span class="line">  void* ptr = mmap(nullptr, map_size, PROT_READ | PROT_WRITE,</span><br><span class="line">   MAP_SHARED,</span><br><span class="line">   fd,</span><br><span class="line">   file_offset);</span><br><span class="line">  if (unlikely(ptr == MAP_FAILED)) &#123;</span><br><span class="line">    *base = nullptr;</span><br><span class="line">    return;</span><br><span class="line">  &#125;</span><br><span class="line">  *base = reinterpret_cast&lt;char*&gt;(ptr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void UnMapRegion(char* base) &#123;</span><br><span class="line">  munmap(base, map_size);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void writer(int index) &#123;</span><br><span class="line">  std::string fname = &quot;data&quot; + std::to_string(index);</span><br><span class="line">  std::string batch = &quot;batch&quot; + std::to_string(index);</span><br><span class="line"></span><br><span class="line">  char* base = nullptr;</span><br><span class="line">  char* cursor = nullptr;</span><br><span class="line">  uint64_t mmap_offset = 0, file_offset = 0;</span><br><span class="line"></span><br><span class="line">  int data_fd = ::open(fname.c_str(), O_RDWR | O_CREAT | O_DIRECT, 0645);</span><br><span class="line">  int batch_fd = ::open(batch.c_str(), O_RDWR | O_CREAT | O_DIRECT, 0645);</span><br><span class="line">  posix_fallocate(data_fd, 0, (4096UL * 1000000));</span><br><span class="line">  posix_fallocate(batch_fd, 0, map_size);</span><br><span class="line"></span><br><span class="line">  MapRegion(batch_fd, 0, &amp;base);</span><br><span class="line">  if (unlikely(base == nullptr)) &#123;</span><br><span class="line">    return;</span><br><span class="line">  &#125;</span><br><span class="line">  cursor = base;</span><br><span class="line">  file_offset += map_size;</span><br><span class="line"></span><br><span class="line">  for (int32_t i = 0; i &lt; 1000000; i++) &#123;</span><br><span class="line">    if (unlikely(mmap_offset &gt;= map_size)) &#123;</span><br><span class="line">      pwrite64(data_fd, base, map_size, file_offset);</span><br><span class="line">      cursor = base;</span><br><span class="line"></span><br><span class="line">      file_offset += map_size;</span><br><span class="line">      mmap_offset = 0;</span><br><span class="line">    &#125;</span><br><span class="line">    memcpy(cursor, data, 4096);</span><br><span class="line">    cursor += 4096;</span><br><span class="line">    mmap_offset += 4096;</span><br><span class="line">  &#125;</span><br><span class="line">  UnMapRegion(base);</span><br><span class="line"></span><br><span class="line">  close(data_fd);</span><br><span class="line">  close(batch_fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 <code>vmstat</code> 来获取写入速度的数据, 整理如下:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/mmap_batch_io.png" alt="mmap_batch_io"></p><p>这次避免了 <code>mmap()</code> 的开销，写入速度可以稳定保持在 <code>2180 MB/S</code> 左右，且没有 I/O 抖动.</p><p>内存使用也仅仅只有 <code>18000KB</code>, 大约 <code>18M</code>:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/mmap_io_mem_usage.png" alt="memory_usage"></p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>下面是四种方式的写入速度对比:</p><p><img src="http://www.leviathan.vip/2019/01/02/Linux%E7%8E%AF%E5%A2%83%E5%86%99%E6%96%87%E4%BB%B6%E5%A6%82%E4%BD%95%E7%A8%B3%E5%AE%9A%E8%B7%91%E6%BB%A1%E7%A3%81%E7%9B%98I-O%E5%B8%A6%E5%AE%BD/summary.png" alt="summary"><br>在限制内存，且需要 <code>kill -9</code> 不丢数据的情况下，我们可以使用 <code>mmap()</code> 来模拟 Buffer IO，但为了避免频繁 <code>mmap()</code> 的开销，我们需要临时文件来做我们的内存映射. 这种方法可以保证我们的写入速度稳定且 <code>kill -9</code> 不至于丢失数据.</p>]]></content>
    
    <summary type="html">
    
      &lt;ul&gt;
&lt;li&gt;准备&lt;ul&gt;
&lt;li&gt;要求&lt;/li&gt;
&lt;li&gt;机器配置&lt;/li&gt;
&lt;li&gt;测试磁盘 IO 性能&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;实验一: Buffer IO 写入&lt;/li&gt;
&lt;li&gt;实验二: 4K 单次 Direct IO 写入&lt;/li&gt;
&lt;li&gt;实验三: mmap 写入&lt;/li&gt;
&lt;li&gt;实验四: 改进的 mmap 写入&lt;/li&gt;
&lt;li&gt;结论&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="数据库" scheme="https://lexburner.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
      <category term="文件 IO" scheme="https://lexburner.github.io/tags/%E6%96%87%E4%BB%B6-IO/"/>
    
  </entry>
  
  <entry>
    <title>一个看板娘入住你的个人博客只需要三步</title>
    <link href="https://lexburner.github.io/live2d/"/>
    <id>https://lexburner.github.io/live2d/</id>
    <published>2019-10-09T11:01:38.000Z</published>
    <updated>2019-10-23T07:11:43.493Z</updated>
    
    <content type="html"><![CDATA[<p>最近在浏览别人博客时，一个萌物给了我意外的惊喜，原来博客还可以这么玩</p><p><img src="https://kirito.iocoder.cn/%E5%B0%8F%E6%81%B6%E9%AD%94" alt="小恶魔"></p><p>于是果断审查元素，发现这个萌萌哒的看板娘背后使用的是一个叫 live2d 的技术，并且凭借 Google 迅速找到对应的开源代码：<a href="https://github.com/xiazeyu/live2d-widget.js，https://github.com/EYHN/hexo-helper-live2d" target="_blank" rel="noopener">https://github.com/xiazeyu/live2d-widget.js，https://github.com/EYHN/hexo-helper-live2d</a></p><p>你可以在我的博客中先目睹下它的实际效果：<a href="https://www.cnkirito.moe/" target="_blank" rel="noopener">https://www.cnkirito.moe/</a>，点击会有音效哦~</p><p>在浏览 live2-widget.js 的说明文档时，发现它对 hexo 的支持非常友好，恰好我的博客是通过 hexo 搭建的，所以本文会介绍一下如何为 hexo 集成一只看板娘。</p><a id="more"></a><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>使用 npm 在 hexo 下安装 hexo-helper-live2d，它将 live2d-widget.js 与 hexo 进行了整合，使得我们只需要通过简单的配置，即可生效</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install --save hexo-helper-live2d</span><br></pre></td></tr></table></figure><p>接着下载一个 Kirito 良心推荐的看板娘：shizuku，也是我正在用的</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install live2d-widget-model-shizuku</span><br></pre></td></tr></table></figure><p>如果安装成功，我们可以在 node_modules 目录中找到 live2d-widget 和 live2d-widget-model-shizuku 两个目录</p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>向 hexo 的 <code>_config.yml</code> 添加如下的配置</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">live2d:</span></span><br><span class="line"><span class="attr">  enable:</span> <span class="literal">true</span></span><br><span class="line"><span class="attr">  scriptFrom:</span> <span class="string">local</span></span><br><span class="line"><span class="attr">  pluginRootPath:</span> <span class="string">node_modules/</span></span><br><span class="line"><span class="attr">  pluginJsPath:</span> <span class="string">lib/</span></span><br><span class="line"><span class="attr">  pluginModelPath:</span> <span class="string">assets/</span></span><br><span class="line"><span class="attr">  tagMode:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">  debug:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">  model:</span></span><br><span class="line"><span class="attr">    use:</span> <span class="string">live2d-widget-model-shizuku</span></span><br><span class="line"><span class="attr">  display:</span></span><br><span class="line"><span class="attr">    position:</span> <span class="string">right</span></span><br><span class="line"><span class="attr">    width:</span> <span class="number">150</span></span><br><span class="line"><span class="attr">    height:</span> <span class="number">300</span></span><br><span class="line"><span class="attr">  mobile:</span></span><br><span class="line"><span class="attr">    show:</span> <span class="literal">true</span></span><br><span class="line"><span class="attr">  react:</span></span><br><span class="line"><span class="attr">    opacity:</span> <span class="number">0.7</span></span><br></pre></td></tr></table></figure><p>需要留意的是 live2d.model.use 这个标签的值，是用于指定博客使用哪一个看板娘模型的。</p><h2 id="发布"><a href="#发布" class="headerlink" title="发布"></a>发布</h2><p>接着只需要正常编译并发布，你的 hexo 博客就获得了一枚萌妹子了</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure><h2 id="模型推荐"><a href="#模型推荐" class="headerlink" title="模型推荐"></a>模型推荐</h2><p>除了上述推荐的 shizuku 看板娘，作者还提供了其他一些不错的模型，下面罗列一部分，不知道你会 pick 哪一个呢？</p><p><strong>miku</strong></p><p><img src="https://huaji8.top/img/live2d/miku.gif" alt="img"></p><p><strong>haru</strong></p><p><img src="https://huaji8.top/img/live2d/haru.gif?imageMogr2/thumbnail/640x640/format/webp/blur/1x0/quality/75|imageslim" alt="haru"></p><p><strong>koharu</strong></p><p><img src="https://huaji8.top/img/live2d/koharu.gif" alt="koharu"></p><p><strong>haruto</strong></p><p><img src="https://huaji8.top/img/live2d/haruto.gif" alt="haruto"></p><p><strong>nipsilon</strong></p><p><img src="https://huaji8.top/img/live2d/nipsilon.gif" alt="nipsilon"></p><p><strong>shizuku</strong></p><p><img src="https://huaji8.top/img/live2d/shizuku.gif" alt="shizuku"></p><p><strong>z16</strong></p><p><img src="https://huaji8.top/img/live2d/z16.gif" alt="z16"></p><p><strong>hibiki</strong></p><p><img src="https://huaji8.top/img/live2d/hibiki.gif" alt="hibiki"></p><p><strong>Unitychan</strong></p><p><img src="https://huaji8.top/img/live2d/Unitychan.gif" alt="Unitychan"></p><p><strong>hijiki</strong></p><p><img src="https://huaji8.top/img/live2d/hijiki.gif" alt="hijiki"></p><h2 id="尾记"><a href="#尾记" class="headerlink" title="尾记"></a>尾记</h2><p>Live2d 的确是很有意思的一个技术，可以让静态博客有了一些生机，如果你对其感兴趣，还可以自己采集模型，自己发布模型，甚至在了解这样技术的同时，我还见识到一些科技感满满的博主把 AI 机器人的特性添加给了看板娘，让其可以与正在使用鼠标浏览的你进行互动，这些我就不过多介绍了，如果你也有一个博客，那就赶紧试试这个 idea 吧。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近在浏览别人博客时，一个萌物给了我意外的惊喜，原来博客还可以这么玩&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://kirito.iocoder.cn/%E5%B0%8F%E6%81%B6%E9%AD%94&quot; alt=&quot;小恶魔&quot;&gt;&lt;/p&gt;
&lt;p&gt;于是果断审查元素，发现这个萌萌哒的看板娘背后使用的是一个叫 live2d 的技术，并且凭借 Google 迅速找到对应的开源代码：&lt;a href=&quot;https://github.com/xiazeyu/live2d-widget.js，https://github.com/EYHN/hexo-helper-live2d&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://github.com/xiazeyu/live2d-widget.js，https://github.com/EYHN/hexo-helper-live2d&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;你可以在我的博客中先目睹下它的实际效果：&lt;a href=&quot;https://www.cnkirito.moe/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://www.cnkirito.moe/&lt;/a&gt;，点击会有音效哦~&lt;/p&gt;
&lt;p&gt;在浏览 live2-widget.js 的说明文档时，发现它对 hexo 的支持非常友好，恰好我的博客是通过 hexo 搭建的，所以本文会介绍一下如何为 hexo 集成一只看板娘。&lt;/p&gt;
    
    </summary>
    
      <category term="技术杂谈" scheme="https://lexburner.github.io/categories/%E6%8A%80%E6%9C%AF%E6%9D%82%E8%B0%88/"/>
    
    
      <category term="live2d" scheme="https://lexburner.github.io/tags/live2d/"/>
    
  </entry>
  
  <entry>
    <title>一文聊透 Dubbo 优雅停机</title>
    <link href="https://lexburner.github.io/dubbo-gracefully-shutdown/"/>
    <id>https://lexburner.github.io/dubbo-gracefully-shutdown/</id>
    <published>2019-09-29T14:46:55.000Z</published>
    <updated>2019-11-04T14:15:54.437Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1 前言"></a>1 前言</h2><p>一年之前，我曾经写过一篇《研究优雅停机时的一点思考》，主要介绍了 kill -9，kill -15 两个 Linux 指令的含义，并且针对性的聊到了 Spring Boot 应用如何正确的优雅停机，算是本文的前置文章，如果你对上述概念不甚了解，建议先去浏览一遍，再回头来看这篇文章。这篇文章将会以 Dubbo 为例，既聊架构设计，也聊源码，聊聊服务治理框架要真正实现优雅停机，需要注意哪些细节。</p><p>本文的写作思路是从 Dubbo 2.5.x 开始，围绕优雅停机这个优化点，一直追溯到最新的 2.7.x。先对 Dubbo 版本做一个简单的科普：2.7.x 和 2.6.x 是目前官方推荐使用的版本，其中 2.7.x 是捐献给 Apache 的版本，具备了很多新的特性，目前最新的 release 版本是 2.7.4，处于生产基本可用的状态；2.6.x 处于维护态，主要以 bugfix 为主，但经过了很多公司线上环境的验证，所以求稳的话，可以使用 2.6.x 分支最新的版本。至于 2.5.x，社区已经放弃了维护，并且 2.5.x 存在一定数量的 bug，本文介绍的 Dubbo 优雅停机特性便体现了这一点。</p><a id="more"></a><h2 id="2-优雅停机的意义"><a href="#2-优雅停机的意义" class="headerlink" title="2 优雅停机的意义"></a>2 优雅停机的意义</h2><p>优雅停机一直是一个非常严谨的话题，但由于其仅仅存在于重启、下线这样的部署阶段，导致很多人忽视了它的重要性，但没有它，你永远不能得到一个完整的应用生命周期，永远会对系统的健壮性持怀疑态度。</p><p>同时，优雅停机又是一个庞大的话题</p><ul><li>操作系统层面，提供了 kill -9 （SIGKILL）和 kill -15（SIGTERM） 两种停机策略</li><li>语言层面，Java 应用有 JVM shutdown hook 这样的概念</li><li>框架层面，Spring Boot 提供了 actuator 的下线 endpoint，提供了 ContextClosedEvent 事件</li><li>容器层面，Docker ：当执行 docker stop 命令时，容器内的进程会收到 SIGTERM 信号，那么 Docker Daemon 会在 10s 后，发出 SIGKILL 信号；K8S 在管理容器生命周期阶段中提供了 prestop 钩子方法</li><li>应用架构层面，不同架构存在不同的部署方案。单体式应用中，一般依靠 nginx 这样的负载均衡组件进行手动切流，逐步部署集群；微服务架构中，各个节点之间有复杂的调用关系，上述这种方案就显得不可靠了，需要有自动化的机制。</li></ul><p>为避免该话题过度发散，本文的重点将会集中在框架和应用架构层面，探讨以 Dubbo 为代表的微服务架构在优雅停机上的最佳实践。Dubbo 的优雅下线主要依赖于注册中心组件，由其通知消费者摘除下线的节点，如下图所示：</p><p><img src="https://kirito.iocoder.cn/image-20191001173747692.png" alt="image-20191001173747692"></p><p>上述的操作旨在让服务消费者避开已经下线的机器，但这样就算实现了优雅停机了吗？似乎还漏掉了一步，在应用停机时，可能还存在执行到了一半的任务，试想这样一个场景：一个 Dubbo 请求刚到达提供者，服务端正在处理请求，收到停机指令后，提供者直接停机，留给消费者的只会是一个没有处理完毕的超时请求。</p><p>结合上述的案例，我们总结出 Dubbo 优雅停机需要满足两点基本诉求：</p><ol><li>服务消费者不应该请求到已经下线的服务提供者</li><li>在途请求需要处理完毕，不能被停机指令中断</li></ol><p>优雅停机的意义：应用的重启、停机等操作，不影响业务的连续性。</p><h2 id="3-优雅停机初始方案-—-2-5-x"><a href="#3-优雅停机初始方案-—-2-5-x" class="headerlink" title="3 优雅停机初始方案 — 2.5.x"></a>3 优雅停机初始方案 — 2.5.x</h2><p>为了让读者对 Dubbo 的优雅停机有一个最基础的理解，我们首先研究下 Dubbo 2.5.x 的版本，这个版本实现优雅停机的方案相对简单，容易理解。</p><h3 id="3-1-入口类：AbstractConfig"><a href="#3-1-入口类：AbstractConfig" class="headerlink" title="3.1 入口类：AbstractConfig"></a>3.1 入口类：AbstractConfig</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractConfig</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line"><span class="keyword">static</span> &#123;</span><br><span class="line">        Runtime.getRuntime().addShutdownHook(<span class="keyword">new</span> Thread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                ProtocolConfig.destroyAll();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;, <span class="string">"DubboShutdownHook"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 AbstractConfig 的静态块中，Dubbo 注册了一个 shutdown hook，用于执行 Dubbo 预设的一些停机逻辑，继续跟进 <code>ProtocolConfig.destroyAll()</code> 。</p><h3 id="3-2-ProtocolConfig"><a href="#3-2-ProtocolConfig" class="headerlink" title="3.2 ProtocolConfig"></a>3.2 ProtocolConfig</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">destroyAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!destroyed.compareAndSet(<span class="keyword">false</span>, <span class="keyword">true</span>)) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    AbstractRegistryFactory.destroyAll(); <span class="comment">// ①注册中心注销</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Wait for registry notification</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(ConfigUtils.getServerShutdownTimeout()); <span class="comment">// ② sleep 等待</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        logger.warn(<span class="string">"Interrupted unexpectedly when waiting for registry notification during shutdown process!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ExtensionLoader&lt;Protocol&gt; loader = ExtensionLoader.getExtensionLoader(Protocol.class);</span><br><span class="line">    <span class="keyword">for</span> (String protocolName : loader.getLoadedExtensions()) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Protocol protocol = loader.getLoadedExtension(protocolName);</span><br><span class="line">            <span class="keyword">if</span> (protocol != <span class="keyword">null</span>) &#123;</span><br><span class="line">                protocol.destroy(); <span class="comment">// ③协议/流程注销</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">            logger.warn(t.getMessage(), t);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Dubbo 中的 <code>Protocol</code> 这个词不太能望文生义，它一般被翻译为”协议”，但我更习惯将它理解为“流程”，从 <code>Protocol</code> 接口的三个方法反而更加容易理解。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Protocol</span> </span>&#123;</span><br><span class="line">    &lt;T&gt; <span class="function">Exporter&lt;T&gt; <span class="title">export</span><span class="params">(Invoker&lt;T&gt; invoker)</span> <span class="keyword">throws</span> RpcException</span>;</span><br><span class="line">    &lt;T&gt; <span class="function">Invoker&lt;T&gt; <span class="title">refer</span><span class="params">(Class&lt;T&gt; type, URL url)</span> <span class="keyword">throws</span> RpcException</span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它定义了暴露、订阅、注销这三个生命周期方法，所以不难理解为什么 Dubbo 会把 shutdown hook 触发后的注销方法定义在 <code>ProtocolConfig</code> 中了。</p><p>回到 <code>ProtocolConfig</code> 的源码中，我把 <code>ProtocolConfig</code> 中执行的优雅停机逻辑分成了三部分，其中第 1，2 部分和注册中心（Registry）相关，第 3 部分和协议/流程（Protocol）相关，分成下面的 3.3 和 3.4 两部分来介绍。</p><h3 id="3-3-注册中心注销逻辑"><a href="#3-3-注册中心注销逻辑" class="headerlink" title="3.3 注册中心注销逻辑"></a>3.3 注册中心注销逻辑</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractRegistryFactory</span> <span class="keyword">implements</span> <span class="title">RegistryFactory</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">destroyAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        LOCK.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (Registry registry : getRegistries()) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    registry.destroy();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Throwable e) &#123;</span><br><span class="line">                    LOGGER.error(e.getMessage(), e);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            REGISTRIES.clear();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// Release the lock</span></span><br><span class="line">            LOCK.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码对应了 3.2 小节 ProtocolConfig 源码的第 1 部分，代表了注册中心的注销逻辑，更深一层的源码不需要 debug 进去了，大致的逻辑就是删除掉注册中心中本节点对应的服务提供者地址。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Wait for registry notification</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    Thread.sleep(ConfigUtils.getServerShutdownTimeout());</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">    logger.warn(<span class="string">"Interrupted unexpectedly when waiting for registry notification during shutdown process!"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码对应了 3.2 小节 ProtocolConfig 源码的第 2 部分，<code>ConfigUtils.getServerShutdownTimeout()</code> 默认值是 10s，为什么需要在 shutdown hook 中等待 10s 呢？在注释中可以发现这段代码的端倪，原来是为了给服务消费者一点时间，确保等到注册中心的通知。10s 显然是一个经验值，这里也不妨和大家探讨一下，如何稳妥地设置这个值呢？</p><ul><li>设置的过短。由于注册中心通知消费者取消订阅某个地址是异步通知过去的，可能消费者还没收到通知，提供者这边就停机了，这就违背了我们的诉求 1：<strong>服务消费者不应该请求到已经下线的服务提供者</strong>。</li><li>设置的过长。这会导致发布时间变长，带来不必要的等待。</li></ul><p>两个情况对比下，起码可以得出一个实践经验：如果拿捏不准等待时间，尽量设置一个宽松的一点的等待时间。</p><p>这个值主要取决三点因素：</p><ul><li>集群规模的大小。如果只有几个服务，每个服务只有几个实例，那么再弱鸡的注册中心也能很快的下发通知。</li><li>注册中心的选型。以 Naocs 和 Zookeeper 为例，同等规模服务实例下 Nacos 在推送地址方面的能力远超 Zookeeper。</li><li>网络状况。服务提供者和服务消费者与注册中心的交互逻辑走的 TCP 通信，网络状况也会影响到推送时间。</li></ul><p>所以需要根据实际部署场景测量出最合适的值。</p><h3 id="3-4-协议-流程注销逻辑"><a href="#3-4-协议-流程注销逻辑" class="headerlink" title="3.4 协议/流程注销逻辑"></a>3.4 协议/流程注销逻辑</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">ExtensionLoader&lt;Protocol&gt; loader = ExtensionLoader.getExtensionLoader(Protocol.class);</span><br><span class="line"><span class="keyword">for</span> (String protocolName : loader.getLoadedExtensions()) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Protocol protocol = loader.getLoadedExtension(protocolName);</span><br><span class="line">        <span class="keyword">if</span> (protocol != <span class="keyword">null</span>) &#123;</span><br><span class="line">            protocol.destroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">        logger.warn(t.getMessage(), t);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码对应了 3.2 小节 ProtocolConfig 源码的第 3 部分，在运行时，<code>loader.getLoadedExtension(protocolName)</code>  这段代码会加载到两个协议 ：<code>DubboProtocol</code> 和 <code>Injvm</code> 。后者 <code>Injvm</code> 实在没啥好讲的，主要来分析一下 <code>DubboProtocol</code> 的逻辑。</p><p><code>DubboProtocol</code> 实现了我们前面提到的 <code>Protocol</code> 接口，它的 destory 方法是我们重点要看的。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DubboProtocol</span> <span class="keyword">extends</span> <span class="title">AbstractProtocol</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (String key : <span class="keyword">new</span> ArrayList&lt;String&gt;(serverMap.keySet())) &#123;</span><br><span class="line">            ExchangeServer server = serverMap.remove(key);</span><br><span class="line">            <span class="keyword">if</span> (server != <span class="keyword">null</span>) &#123;</span><br><span class="line">            server.close(ConfigUtils.getServerShutdownTimeout());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (String key : <span class="keyword">new</span> ArrayList&lt;String&gt;(referenceClientMap.keySet())) &#123;</span><br><span class="line">            ExchangeClient client = referenceClientMap.remove(key);</span><br><span class="line">            <span class="keyword">if</span> (client != <span class="keyword">null</span>) &#123;</span><br><span class="line">            client.close(ConfigUtils.getServerShutdownTimeout());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (String key : <span class="keyword">new</span> ArrayList&lt;String&gt;(ghostClientMap.keySet())) &#123;</span><br><span class="line">            ExchangeClient client = ghostClientMap.remove(key);</span><br><span class="line">            <span class="keyword">if</span> (client != <span class="keyword">null</span>) &#123;</span><br><span class="line">                client.close(ConfigUtils.getServerShutdownTimeout());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        stubServiceMethodsMap.clear();</span><br><span class="line">        <span class="keyword">super</span>.destroy();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>主要分成了两部分注销逻辑：server 和 client，注意这里是先注销了服务提供者后，再注销了服务消费者，这样做是有意为之。在 RPC 调用中，经常是一个远程调用触发一个远程调用，所以在关闭一个节点时，应该先切断上游的流量，所以这里是先注销了服务提供者，这样从一定程度上，降低了后面服务消费者被调用到的可能性（当然，服务消费者也有可能被单独调用到）。由于 server 和 client 的流程类似，所以我只选取了 server 部分来分析具体的注销逻辑。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> timeout)</span> </span>&#123;</span><br><span class="line">       startClose();</span><br><span class="line">       <span class="keyword">if</span> (timeout &gt; <span class="number">0</span>) &#123;</span><br><span class="line">           <span class="keyword">final</span> <span class="keyword">long</span> max = (<span class="keyword">long</span>) timeout;</span><br><span class="line">           <span class="keyword">final</span> <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line"></span><br><span class="line">           <span class="keyword">if</span> (getUrl().getParameter(Constants.CHANNEL_SEND_READONLYEVENT_KEY, <span class="keyword">true</span>)) &#123;</span><br><span class="line">               <span class="comment">// 如果注册中心有延迟，会立即受到readonly事件，下次不会再调用这台机器，当前已经调用的会处理完</span></span><br><span class="line">               sendChannelReadOnlyEvent();</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">while</span> (HeaderExchangeServer.<span class="keyword">this</span>.isRunning() <span class="comment">// ①</span></span><br><span class="line">                   &amp;&amp; System.currentTimeMillis() - start &lt; max) &#123;</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                   Thread.sleep(<span class="number">10</span>);</span><br><span class="line">               &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                   logger.warn(e.getMessage(), e);</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       doClose(); <span class="comment">// ②</span></span><br><span class="line">       server.close(timeout); <span class="comment">// ③</span></span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">isRunning</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       Collection&lt;Channel&gt; channels = getChannels();</span><br><span class="line">       <span class="keyword">for</span> (Channel channel : channels) &#123;</span><br><span class="line">           <span class="keyword">if</span> (DefaultFuture.hasFuture(channel)) &#123;</span><br><span class="line">               <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doClose</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       <span class="keyword">if</span> (!closed.compareAndSet(<span class="keyword">false</span>, <span class="keyword">true</span>)) &#123;</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       stopHeartbeatTimer();</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           scheduled.shutdown();</span><br><span class="line">       &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">           logger.warn(t.getMessage(), t);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><p>化繁为简，这里只挑选上面代码中打标的两个地方进行分析</p><ol><li>判断服务端是否还在处理请求，在超时时间内一直等待到所有任务处理完毕</li><li>关闭心跳检测</li><li>关闭 NettyServer</li></ol><p>特别需要关注第一点，正符合我们在一开始提出的优雅停机的诉求 2：<strong>“在途请求需要处理完毕，不能被停机指令中断”</strong>。</p><h3 id="3-5-优雅停机初始方案总结"><a href="#3-5-优雅停机初始方案总结" class="headerlink" title="3.5 优雅停机初始方案总结"></a>3.5 优雅停机初始方案总结</h3><p>上述介绍的几个类构成了 Dubbo 2.5.x 的优雅停机方案，简单做一下总结，Dubbo 的优雅停机逻辑时序如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Registry 注销</span><br><span class="line">等待 -Ddubbo.service.shutdown.wait 秒，等待消费方收到下线通知</span><br><span class="line">Protocol 注销</span><br><span class="line">    DubboProtocol 注销</span><br><span class="line">NettyServer 注销</span><br><span class="line">等待处理中的请求完毕</span><br><span class="line">            停止发送心跳</span><br><span class="line">            关闭 Netty 相关资源</span><br><span class="line">NettyClient 注销</span><br><span class="line">            停止发送心跳</span><br><span class="line">            等待处理中的请求完毕</span><br><span class="line">            关闭 Netty 相关资源</span><br></pre></td></tr></table></figure><blockquote><p>Dubbo 2.5.3 优雅停机的缺陷</p><p>如果你正在使用的 Dubbo 版本 &lt;= 2.5.3，一些并发问题和代码缺陷会导致你的应用不能很好的实现优雅停机功能，请尽快升级。</p><p>详情可以参考该 pull request 的变更：<a href="https://github.com/apache/dubbo/pull/568" target="_blank" rel="noopener">https://github.com/apache/dubbo/pull/568</a></p></blockquote><h2 id="4-Spring-容器下-Dubbo-的优雅停机"><a href="#4-Spring-容器下-Dubbo-的优雅停机" class="headerlink" title="4 Spring 容器下 Dubbo 的优雅停机"></a>4 Spring 容器下 Dubbo 的优雅停机</h2><p>上述的方案在不使用 Spring 时的确是无懈可击的，但由于现在大多数开发者选择使用 Spring 构建 Dubbo 应用，上述的方案会存在一些缺陷。</p><p>由于 Spring 框架本身也依赖于 shutdown hook 执行优雅停机，并且与 Dubbo 的优雅停机会并发执行，而 Dubbo 的一些 Bean 受 Spring 托管，当 Spring 容器优先关闭时，会导致 Dubbo 的优雅停机流程无法获取相关的 Bean，从而优雅停机失效。</p><p>Dubbo 开发者们迅速意识到了 shutdown hook 并发执行的问题，开始了一系列的补救措施。</p><h3 id="4-1-增加-ShutdownHookListener"><a href="#4-1-增加-ShutdownHookListener" class="headerlink" title="4.1 增加 ShutdownHookListener"></a>4.1 增加 ShutdownHookListener</h3><p>Spring 如此受欢迎的原因之一便是它的扩展点非常丰富，例如它提供了 <code>ApplicationListener</code> 接口，开发者可以实现这个接口监听到 Spring 容器的关闭事件，为解决 shutdown hook 并发执行的问题，在 Dubbo 2.6.3 中新增了 <code>ShutdownHookListener</code> 类，用作 Spring 容器下的关闭 Dubbo 应用的钩子。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ShutdownHookListener</span> <span class="keyword">implements</span> <span class="title">ApplicationListener</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ApplicationEvent event)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ContextClosedEvent) &#123;</span><br><span class="line">            <span class="comment">// we call it anyway since dubbo shutdown hook make sure its destroyAll() is re-entrant.</span></span><br><span class="line">            <span class="comment">// pls. note we should not remove dubbo shutdown hook when spring framework is present, this is because</span></span><br><span class="line">            <span class="comment">// its shutdown hook may not be installed.</span></span><br><span class="line">            DubboShutdownHook shutdownHook = DubboShutdownHook.getDubboShutdownHook();</span><br><span class="line">            shutdownHook.destroyAll();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当服务提供者 <code>ServiceBean</code> 和服务消费者 <code>ReferenceBean</code> 被初始化时，会触发该钩子被创建。</p><p>再来看看 AbstractConfig 中的代码，依旧保留了 JVM 的 shutdown hook</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractConfig</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        Runtime.getRuntime().addShutdownHook(DubboShutdownHook.getDubboShutdownHook());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也就是说，在 Spring 环境下会注册两个钩子，在 Non-Spring 环境下只会有一个钩子，但看到 2.6.x 的实现大家是否意识到了两个问题呢？</p><ol><li>两个钩子并发执行不会报错吗？</li><li>为什么在 Spring 下不取消 JVM 的钩子，只保留 Spring 的钩子不就可以工作了吗？</li></ol><p>先解释第一个问题，这个按照我的理解，这段代码的 Commiter 可能认为只需要有一个 Spring 的钩子能正常注销就完事了，不需要考虑另外一个报不报错，因为都是独立的线程，不会有很大的影响。</p><p>再解释第二个问题，其实这个疑问的答案就藏在上面 <code>ShutdownHookListener</code> 代码的注释中，这段注释的意思是说：在 Spring 框架下不能直接移除原先的 JVM 钩子，因为 Spring 框架可能没有注册 ContextClosed 事件。啥意思呢？这里涉及到 Spring 框架生命周期的一个细节，我打算单独介绍一下。</p><h3 id="4-2-Spring-的容器关闭事件详解"><a href="#4-2-Spring-的容器关闭事件详解" class="headerlink" title="4.2 Spring 的容器关闭事件详解"></a>4.2 Spring 的容器关闭事件详解</h3><p>在 Spring 中，我们可以使用至少三种方式来注册容器关闭时一些收尾工作：</p><ol><li><p>使用 DisposableBean 接口</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDisposableBean</span> <span class="keyword">implements</span> <span class="title">DisposableBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"== invoke DisposableBean =="</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>使用 @PreDestroy 注解</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestPreDestroy</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PreDestroy</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">preDestroy</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"== invoke preDestroy =="</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>使用 ApplicationListener 监听 ContextClosedEvent</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">applicationContext.addApplicationListener(<span class="keyword">new</span> ApplicationListener&lt;ApplicationEvent&gt;() &#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ApplicationEvent applicationEvent)</span> </span>&#123;</span><br><span class="line"><span class="keyword">if</span> (applicationEvent <span class="keyword">instanceof</span> ContextClosedEvent) &#123;</span><br><span class="line">System.out.println(<span class="string">"== receive context closed event =="</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li></ol><p>但需要注意的是，在使用 SpringBoot 内嵌 Tomcat 容器时，容器关闭钩子是自动被注册，但使用纯粹的 Spring 框架或者外部 Tomcat 容器，需要显式的调用 <code>context.registerShutdownHook();</code> 接口进行注册</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">ClassPathXmlApplicationContext context = <span class="keyword">new</span> ClassPathXmlApplicationContext(<span class="string">"spring/beans.xml"</span>);</span><br><span class="line">context.start();</span><br><span class="line">context.registerShutdownHook();</span><br><span class="line">context.addApplicationListener(<span class="keyword">new</span> ApplicationListener&lt;ApplicationEvent&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ApplicationEvent applicationEvent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (applicationEvent <span class="keyword">instanceof</span> ContextClosedEvent) &#123;</span><br><span class="line">            System.out.println(<span class="string">"== receive context closed event =="</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>否则，上述三种回收方法都无法工作。我们来看看 <code>registerShutdownHook()</code> 都干了啥</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractApplicationContext</span> <span class="keyword">extends</span> <span class="title">DefaultResourceLoader</span></span></span><br><span class="line"><span class="class"><span class="keyword">implements</span> <span class="title">ConfigurableApplicationContext</span>, <span class="title">DisposableBean</span></span>&#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">registerShutdownHook</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">this</span>.shutdownHook == <span class="keyword">null</span>) &#123;</span><br><span class="line"><span class="comment">// No shutdown hook registered yet.</span></span><br><span class="line"><span class="keyword">this</span>.shutdownHook = <span class="keyword">new</span> Thread() &#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="keyword">synchronized</span> (startupShutdownMonitor) &#123;</span><br><span class="line">doClose();</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;;</span><br><span class="line">Runtime.getRuntime().addShutdownHook(<span class="keyword">this</span>.shutdownHook); <span class="comment">// 重点！</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其实也就是显式注册了一个属于 Spring 的钩子。这也解释上了 4.1 小节中，为什么有那段注释了，注册了事件不一定管用，还得保证 Spring 容器注册了它自己的钩子。</p><h3 id="4-3-Dubbo-优雅停机中级方案总结"><a href="#4-3-Dubbo-优雅停机中级方案总结" class="headerlink" title="4.3 Dubbo 优雅停机中级方案总结"></a>4.3 Dubbo 优雅停机中级方案总结</h3><p>第 4 节主要介绍了 Dubbo 开发者们在 Spring 环境下解决 Dubbo 优雅停机并发执行 shutdown hook 时的缺陷问题，但其实还不完善，因为在 Spring 环境下，如果没有显式注册 Spring 的 shutdown， 还是会存在缺陷的，准确的说，Dubbo 2.6.x 版本可以很好的在 Non-Spring、Spring Boot、Spring + ContextClosedEvent 环境下很好的工作。</p><h2 id="5-Dubbo-2-7-最终方案"><a href="#5-Dubbo-2-7-最终方案" class="headerlink" title="5 Dubbo 2.7 最终方案"></a>5 Dubbo 2.7 最终方案</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringExtensionFactory</span> <span class="keyword">implements</span> <span class="title">ExtensionFactory</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">addApplicationContext</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">        CONTEXTS.add(context);</span><br><span class="line">        <span class="keyword">if</span> (context <span class="keyword">instanceof</span> ConfigurableApplicationContext) &#123;</span><br><span class="line">            ((ConfigurableApplicationContext) context).registerShutdownHook();</span><br><span class="line">            DubboShutdownHook.getDubboShutdownHook().unregister();</span><br><span class="line">        &#125;</span><br><span class="line">        BeanFactoryUtils.addApplicationListener(context, SHUTDOWN_HOOK_LISTENER);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码寥寥数行，却是经过了深思熟虑之后的产物，期间迭代了 3 个大版本，真是不容易。这段代码很好地解决了第 4 节提出的两个问题</p><ol><li>担心两个钩子并发执行有问题？那就在可以注册 Spring 钩子的时候取消掉 JVM 的钩子。</li><li>担心当前 Spring 容器没有注册 Spring 钩子？那就显示调用 registerShutdownHook 进行注册。</li></ol><p>其他细节方面的优化和 bugfix 我就不进行详细介绍了，可以见得实现一个优雅停机需要考虑的点非常之多。</p><h2 id="6-总结"><a href="#6-总结" class="headerlink" title="6 总结"></a>6 总结</h2><p>优雅停机看似是一个不难的技术点，但在一个通用框架中，使用者的业务场景类型非常多，这会大大加剧整个代码实现的复杂度。</p><p>摸清楚整个 Dubbo 优雅停机演化的过程，也着实花费了我一番功夫，有很多实现需要 checkout 到非常古老的分支，同时翻阅了很多 issue、pull request 的讨论，最终才形成了这篇文章，虽然研究的过程是困难的，但获取到真相是让人喜悦的。</p><p>在开源产品的研发过程中，服务到每一个类型的用户真的是非常难的一件事，能做的是满足大部分用户。例如 2.6.x 在大多数环境下其实已经没问题了，在 2.7.x 中则是得到了更加的完善，但是我相信，在使用 Dubbo 的部分用户中，可能还是会存在优雅停机的问题，只不过还没有被发现。</p><p>商业化的思考：和开源产品一样，商业化产品的研发也同样是一个逐渐迭代的过程，需要数代开发者一起维护一份代码，使用者发现问题，开发者修复问题，这样的正反馈可以形成一个正反馈，促使产品更加优秀。</p><p><strong>相关 pull request:</strong></p><p>修复 2.5.3 bug 的 pr：</p><p><a href="https://github.com/apache/dubbo/pull/568" target="_blank" rel="noopener">https://github.com/apache/dubbo/pull/568</a> 作者：@<strong>qinliujie</strong> </p><p>2.6.x Spring Shutdown Hook Enhancement: </p><p><a href="https://github.com/apache/dubbo/pull/1763" target="_blank" rel="noopener">https://github.com/apache/dubbo/pull/1763</a> 作者：@<strong>ralf0131</strong></p><p><a href="https://github.com/apache/dubbo/pull/1820" target="_blank" rel="noopener">https://github.com/apache/dubbo/pull/1820</a> 作者：@<strong>ralf0131</strong></p><p>2.7.x Spring Shutdown Hook Enhancement: </p><p><a href="https://github.com/apache/dubbo/pull/3008/" target="_blank" rel="noopener">https://github.com/apache/dubbo/pull/3008/</a> 作者：@<strong>beiwei30</strong></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-前言&quot;&gt;&lt;a href=&quot;#1-前言&quot; class=&quot;headerlink&quot; title=&quot;1 前言&quot;&gt;&lt;/a&gt;1 前言&lt;/h2&gt;&lt;p&gt;一年之前，我曾经写过一篇《研究优雅停机时的一点思考》，主要介绍了 kill -9，kill -15 两个 Linux 指令的含义，并且针对性的聊到了 Spring Boot 应用如何正确的优雅停机，算是本文的前置文章，如果你对上述概念不甚了解，建议先去浏览一遍，再回头来看这篇文章。这篇文章将会以 Dubbo 为例，既聊架构设计，也聊源码，聊聊服务治理框架要真正实现优雅停机，需要注意哪些细节。&lt;/p&gt;
&lt;p&gt;本文的写作思路是从 Dubbo 2.5.x 开始，围绕优雅停机这个优化点，一直追溯到最新的 2.7.x。先对 Dubbo 版本做一个简单的科普：2.7.x 和 2.6.x 是目前官方推荐使用的版本，其中 2.7.x 是捐献给 Apache 的版本，具备了很多新的特性，目前最新的 release 版本是 2.7.4，处于生产基本可用的状态；2.6.x 处于维护态，主要以 bugfix 为主，但经过了很多公司线上环境的验证，所以求稳的话，可以使用 2.6.x 分支最新的版本。至于 2.5.x，社区已经放弃了维护，并且 2.5.x 存在一定数量的 bug，本文介绍的 Dubbo 优雅停机特性便体现了这一点。&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="Dubbo" scheme="https://lexburner.github.io/tags/Dubbo/"/>
    
  </entry>
  
  <entry>
    <title>使用 JMeter 进行 Dubbo 性能测试</title>
    <link href="https://lexburner.github.io/dubbo-perf-benchmark/"/>
    <id>https://lexburner.github.io/dubbo-perf-benchmark/</id>
    <published>2019-09-05T11:45:52.000Z</published>
    <updated>2019-11-04T14:16:42.708Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1 前言"></a>1 前言</h2><p>说道性能测试工具，你会立刻联想到哪一个？ab（ApacheBench）、JMeter、LoadRunner、wrk…可以说市面上的压测工具实在是五花八门。那如果再问一句，对 Dubbo 进行性能压测，你会 pick 哪一个？可能大多数人就懵逼了。可以发现，大多数的压测工具对开放的协议支持地比较好，例如：HTTP 协议，但对于 Dubbo 框架的私有协议：<code>dubbo</code>，它们都显得力不从心了。</p><p>如果不从通用的压测工具上解决 Dubbo 的压测需求问题，可以自己写 Dubbo 客户端，自己统计汇总结果，但总归不够优雅，再加上很多开发同学没有丰富的测试经验，很容易出现一些偏差。说到底，还是压测工具靠谱，于是便引出了本文的主角 —— <strong><a href="https://github.com/thubbo/jmeter-plugins-for-apache-dubbo" target="_blank" rel="noopener">jmeter-plugins-for-apache-dubbo</a></strong>。这是一款由 Dubbo 社区 Commiter – <a href="https://ningyu1.github.io/blog/about/" target="_blank" rel="noopener">凝雨</a> 同学开发的 JMeter 插件，可以非常轻松地对 Dubbo 实现性能测试。</p><a id="more"></a><h2 id="2-JMeter-介绍"><a href="#2-JMeter-介绍" class="headerlink" title="2 JMeter 介绍"></a>2 JMeter 介绍</h2><p>在开始压测 Dubbo 之前，先简单介绍一下这款开源的性能测试工具 —— JMeter。JMeter 是 Apache 组织基于 Java 开发的一款性能测试工具。它最初被设计用于 Web 应用测试，但后来扩展到其他测试领域，并可以在 Windows、Mac、Linux 环境下安装使用。JMeter 还提供了图形界面，这使得编写测试用例变得非常简单，具有易学和易操作的特点。</p><blockquote><p>JMeter 官网：<a href="http://jmeter.apache.org/download_jmeter.cgi" target="_blank" rel="noopener">http://jmeter.apache.org/download_jmeter.cgi</a></p></blockquote><h3 id="2-1-安装-JMeter"><a href="#2-1-安装-JMeter" class="headerlink" title="2.1 安装 JMeter"></a>2.1 安装 JMeter</h3><p>截止本文发布，官方的最新版本为：<a href="http://mirrors.tuna.tsinghua.edu.cn/apache//jmeter/binaries/apache-jmeter-5.1.1.zip" target="_blank" rel="noopener">apache-jmeter-5.1.1.zip</a> , 下载后直接解压即可。</p><p><img src="https://kirito.iocoder.cn/image-20190905204943326.png" alt="jmeter 目录"></p><p>在 ${JMETER_HOME}/bin 下找到启动脚本，可以打开图形化界面</p><ul><li>Mac/Linux 用户可以直接使用 jmeter 可执行文件，或者 jmeter.sh 启动脚本</li><li>Windows 用户可以使用 jmeter.bat 启动脚本</li></ul><h3 id="2-2-命令行提示信息"><a href="#2-2-命令行提示信息" class="headerlink" title="2.2 命令行提示信息"></a>2.2 命令行提示信息</h3><p>启动过程中会有一段命令行日志输出：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">================================================================================</span><br><span class="line">Don't use GUI mode for load testing !, only for Test creation and Test debugging.</span><br><span class="line">For load testing, use CLI Mode (was NON GUI):</span><br><span class="line">   jmeter -n -t [jmx file] -l [results file] -e -o [Path to web report folder]</span><br><span class="line">&amp; increase Java Heap to meet your test requirements:</span><br><span class="line">   Modify current env variable HEAP="-Xms1g -Xmx1g -XX:MaxMetaspaceSize=256m" in the jmeter batch file</span><br><span class="line">Check : https://jmeter.apache.org/usermanual/best-practices.html</span><br><span class="line">================================================================================</span><br></pre></td></tr></table></figure><p>注意到第一行的提示，GUI 仅仅能够用于调试和创建测试计划，实际的性能测试需要使用命令行工具进行。</p><p><code>jmeter -n -t [jmx file] -l [results file] -e -o [Path to web report folder]</code></p><ul><li>【jmx file】：使用 GUI 创建的测试计划文件，后缀名为 <code>.jmx</code></li><li>【results file】：测试结果文本文件输出路径</li><li>【Path to web report folder】：测试报告输出路径，JMeter 的强大之处，可以生成图文并茂的测试报告</li></ul><h3 id="2-3-GUI-界面展示"><a href="#2-3-GUI-界面展示" class="headerlink" title="2.3 GUI 界面展示"></a>2.3 GUI 界面展示</h3><p><img src="https://kirito.iocoder.cn/image-20190905211412101.png" alt="image-20190905211412101"></p><p>上图所示为 JMeter 的主界面。官方提供了国际化支持，通过 【Options】-&gt;【Choose Language】可以将界面语言变更为简体中文。</p><h2 id="3-JMeter-压测-HTTP"><a href="#3-JMeter-压测-HTTP" class="headerlink" title="3 JMeter 压测 HTTP"></a>3 JMeter 压测 HTTP</h2><p>本节以 JMeter 压测 HTTP 为引子，介绍 JMeter 的使用方式，让没有使用过 JMeter 的读者对这款工具有一个较为直观的感受。</p><h3 id="3-1-创建线程组"><a href="#3-1-创建线程组" class="headerlink" title="3.1 创建线程组"></a>3.1 创建线程组</h3><p>在“测试计划”上右键 【添加】–&gt;【线程（用户）】–&gt;【线程组】。</p><p><img src="https://kirito.iocoder.cn/image-20190905211637435.png" alt="image-20190905211637435"></p><p>给线程组起一个名字，方便记忆。</p><p><img src="https://kirito.iocoder.cn/image-20190905211831670.png" alt="image-20190905211831670"></p><ul><li>线程数：决定了由多少线程并发压测</li><li>Ramp-Up：代表了 JMeter 创建所有线程所需要的时间，如图所示则代表每 0.1s 创建一个线程</li><li>循环次数：在运行所设置的次数之后，压测将会终止。如果想要运行固定时长的压测，可以设置为：永远，并在下面的调度器中指定持续时间</li></ul><h3 id="3-2-增加-HTTP-取样器"><a href="#3-2-增加-HTTP-取样器" class="headerlink" title="3.2 增加 HTTP 取样器"></a>3.2 增加 HTTP 取样器</h3><p>在刚刚创建的线程组上右键 【添加】–&gt;【取样器】–&gt;【HTTP 请求】。</p><p><img src="https://kirito.iocoder.cn/image-20190905211606505.png" alt="image-20190905211606505"></p><p>为 HTTP 取样器配置上压测地址和必要的参数</p><p><img src="https://kirito.iocoder.cn/image-20190905212937824.png" alt="image-20190905212937824"></p><h3 id="3-3-添加察看结果树"><a href="#3-3-添加察看结果树" class="headerlink" title="3.3 添加察看结果树"></a>3.3 添加察看结果树</h3><p>在刚刚创建的线程组上右键 【添加】–&gt;【监听器】–&gt;【察看结果树】。</p><p><img src="https://kirito.iocoder.cn/image-20190905213114409.png" alt="image-20190905213114409"></p><p>只有添加了【察看结果树】才能让我们看到 GUI 中测试的结果。</p><h3 id="3-4-准备-HTTP-Server"><a href="#3-4-准备-HTTP-Server" class="headerlink" title="3.4 准备 HTTP Server"></a>3.4 准备 HTTP Server</h3><p>使用 SpringBoot 可以快速构建一个 RestController，其暴露了 <code>localhost:8080/queryOrder/{orderNo}</code> 做为压测入口</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OrderRestController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    OrderService orderService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/queryOrder/&#123;orderNo&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> OrderDTO <span class="title">queryOrder</span><span class="params">(@PathVariable(<span class="string">"orderNo"</span>)</span> <span class="keyword">long</span> orderNo) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> orderService.queryOrder(orderNo);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>被压测的服务 OrderService ：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OrderService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> OrderDTO <span class="title">queryOrder</span><span class="params">(<span class="keyword">long</span> orderNo)</span> </span>&#123;</span><br><span class="line">        OrderDTO orderDTO = <span class="keyword">new</span> OrderDTO();</span><br><span class="line">        orderDTO.setOrderNo(orderNo);</span><br><span class="line">        orderDTO.setTotalPrice(<span class="keyword">new</span> BigDecimal(ThreadLocalRandom.current().nextDouble(<span class="number">100.0</span>D)));</span><br><span class="line">        orderDTO.setBody(<span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1000</span>]);</span><br><span class="line">        <span class="keyword">return</span> orderDTO;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-5-验证结果"><a href="#3-5-验证结果" class="headerlink" title="3.5 验证结果"></a>3.5 验证结果</h3><p> 在刚刚创建的线程组上右键 【验证】，执行单次验证，可以用来测试与服务端的连通性。在【察看结果树】选项卡中可以看到【响应数据】已经正常返回了。</p><p><img src="https://kirito.iocoder.cn/image-20190905214317033.png" alt="image-20190905214317033"></p><h3 id="3-6-执行测试计划"><a href="#3-6-执行测试计划" class="headerlink" title="3.6 执行测试计划"></a>3.6 执行测试计划</h3><p>还记得之前启动 GUI 时控制台曾经提示过我们，GUI 只负责创建测试计划并验证，不能用于执行实际的并发压测。在 GUI 中准备就绪之后，我们可以在【文件】-&gt;【保存测试计划为】中将测试计划另存为 <code>rest-order-thread-group.jmx</code> 测试文件，以便我们在命令行进行压测：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jmeter -n -t ./rest-order-thread-group.jmx -l ./result.txt -e -o ./webreport</span><br></pre></td></tr></table></figure><p>下图展示了最终生成的测试报告，主要汇总了执行次数、响应时间、吞吐量、网络传输速率。</p><p><img src="https://kirito.iocoder.cn/image-20190905215339406.png" alt="image-20190905215339406"></p><p>在实际的测试报告中，还有更加详细的维度可以展示，上述只是展示了汇总信息。</p><h2 id="4-JMeter-压测-Dubbo"><a href="#4-JMeter-压测-Dubbo" class="headerlink" title="4 JMeter 压测 Dubbo"></a>4 JMeter 压测 Dubbo</h2><p>JMeter 默认并不支持私有的 dubbo 协议，但其优秀的扩展机制使得只需要添加插件，就可以完成 Dubbo 压测，这一节也是本文重点介绍的部分。</p><h3 id="4-1-安装-jmeter-plugins-for-apache-dubbo"><a href="#4-1-安装-jmeter-plugins-for-apache-dubbo" class="headerlink" title="4.1 安装 jmeter-plugins-for-apache-dubbo"></a>4.1 安装 jmeter-plugins-for-apache-dubbo</h3><blockquote><p>插件地址：<a href="https://github.com/thubbo/jmeter-plugins-for-apache-dubbo" target="_blank" rel="noopener">https://github.com/thubbo/jmeter-plugins-for-apache-dubbo</a></p></blockquote><p>目前该插件支持对最新版本的 Dubbo 进行压测，推荐的安装方式：</p><ol><li><p>克隆项目：<code>git clone https://github.com/thubbo/jmeter-plugins-for-apache-dubbo.git</code></p></li><li><p>打包项目，构建 JMeter 插件：<code>mvn clean install</code> ，得到：jmeter-plugins-dubbo-2.7.3-jar-with-dependencies.jar</p></li><li>将插件添加到 <code>${JMETER_HOME}\lib\ext</code></li></ol><p><img src="https://kirito.iocoder.cn/image-20190906140927770.png" alt="安装插件后的 ext 目录"></p><h3 id="4-2-增加-Dubbo-取样器"><a href="#4-2-增加-Dubbo-取样器" class="headerlink" title="4.2 增加 Dubbo 取样器"></a>4.2 增加 Dubbo 取样器</h3><p>之前的小结已经介绍了如何添加线程组和 HTTP 取样器，现在想要对 Dubbo 应用进行性能测试，可以直接复用之前的线程组配置，在线程组上右键 【添加】–&gt;【取样器】–&gt;【Dubbo Sample】。</p><p><img src="https://kirito.iocoder.cn/image-20190906141506679.png" alt="image-20190906141506679"></p><p>创建 Dubbo 取样器之后，可以对其进行配置</p><p><img src="https://kirito.iocoder.cn/image-20190906143444779.png" alt="image-20190906143444779"></p><h3 id="4-3-准备-Dubbo-Provider"><a href="#4-3-准备-Dubbo-Provider" class="headerlink" title="4.3 准备 Dubbo Provider"></a>4.3 准备 Dubbo Provider</h3><p>复用 HTTP 取样器时的 <code>OrderService</code> </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OrderDubboProvider</span> <span class="keyword">implements</span> <span class="title">OrderApi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    OrderService orderService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> OrderDTO <span class="title">queryOrder</span><span class="params">(<span class="keyword">long</span> orderNo)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> orderService.queryOrder(orderNo);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>配置 application.properties，注册服务到 Zookeeper 注册中心:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">dubbo.scan.basePackages=com.alibaba.edas.benchmark</span><br><span class="line">dubbo.application.name=dubbo-provider-demo</span><br><span class="line">dubbo.registry.address=zookeeper://127.0.0.1:2181</span><br><span class="line">dubbo.protocol.port=20880</span><br></pre></td></tr></table></figure><h3 id="4-4-验证结果"><a href="#4-4-验证结果" class="headerlink" title="4.4 验证结果"></a>4.4 验证结果</h3><p>在 JMeter 中配置好 Dubbo 服务所连接的注册中心，接着通过 <code>Get Provider List</code> 可以获取到服务提供者列表，以供压测选择。在线程组上右键 【验证】，执行单次验证，可以用来测试与服务端的连通性。在【察看结果树】选项卡中可以看到【响应数据】可以正常执行 Dubbo 调用了。</p><p><img src="https://kirito.iocoder.cn/image-20190906143425928.png" alt="image-20190906143425928"></p><h3 id="4-5-执行测试计划"><a href="#4-5-执行测试计划" class="headerlink" title="4.5 执行测试计划"></a>4.5 执行测试计划</h3><p>可以将 Dubbo 取样器和 HTTP 取样器包含在同一个测试计划中一起执行，同时进行了 Dubbo 接口与 Rest 接口的性能对比。在命令行进行压测：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jmeter -n -t ./rest-order-thread-group.jmx -l ./result.txt -e -o ./webreport</span><br></pre></td></tr></table></figure><p>下图展示了最终生成的测试报告：</p><p><img src="https://kirito.iocoder.cn/image-20190906144422407.png" alt="image-20190906144422407"></p><p>Dubbo 接口与 Rest 接口所封装的业务接口均为 <code>OrderService</code>，所以压测上的差距直接体现出了 Dubbo 和 Rest 的差距。从报告对比上来看，Dubbo 接口的平均 RT 远低于 Rest 接口。</p><h2 id="5-总结"><a href="#5-总结" class="headerlink" title="5 总结"></a>5 总结</h2><p>本文从零到一介绍了使用 JMeter 压测 HTTP 的方法，让读者熟悉 JMeter 的使用方式，并着重介绍了使用 jmeter-plugins-for-apache-dubbo 插件压测 Dubbo 的方法。</p><p>由于 JMeter Plugin 的限制，目前 Dubbo 的压测请求是通过泛化调用进行发送的，会有一定程度的性能下降，所以在实际评估 Dubbo 接口性能时，接口实际性能会比压测结果更加乐观。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-前言&quot;&gt;&lt;a href=&quot;#1-前言&quot; class=&quot;headerlink&quot; title=&quot;1 前言&quot;&gt;&lt;/a&gt;1 前言&lt;/h2&gt;&lt;p&gt;说道性能测试工具，你会立刻联想到哪一个？ab（ApacheBench）、JMeter、LoadRunner、wrk…可以说市面上的压测工具实在是五花八门。那如果再问一句，对 Dubbo 进行性能压测，你会 pick 哪一个？可能大多数人就懵逼了。可以发现，大多数的压测工具对开放的协议支持地比较好，例如：HTTP 协议，但对于 Dubbo 框架的私有协议：&lt;code&gt;dubbo&lt;/code&gt;，它们都显得力不从心了。&lt;/p&gt;
&lt;p&gt;如果不从通用的压测工具上解决 Dubbo 的压测需求问题，可以自己写 Dubbo 客户端，自己统计汇总结果，但总归不够优雅，再加上很多开发同学没有丰富的测试经验，很容易出现一些偏差。说到底，还是压测工具靠谱，于是便引出了本文的主角 —— &lt;strong&gt;&lt;a href=&quot;https://github.com/thubbo/jmeter-plugins-for-apache-dubbo&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;jmeter-plugins-for-apache-dubbo&lt;/a&gt;&lt;/strong&gt;。这是一款由 Dubbo 社区 Commiter – &lt;a href=&quot;https://ningyu1.github.io/blog/about/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;凝雨&lt;/a&gt; 同学开发的 JMeter 插件，可以非常轻松地对 Dubbo 实现性能测试。&lt;/p&gt;
    
    </summary>
    
      <category term="RPC" scheme="https://lexburner.github.io/categories/RPC/"/>
    
    
      <category term="DUBBO" scheme="https://lexburner.github.io/tags/DUBBO/"/>
    
      <category term="JMeter" scheme="https://lexburner.github.io/tags/JMeter/"/>
    
  </entry>
  
</feed>
